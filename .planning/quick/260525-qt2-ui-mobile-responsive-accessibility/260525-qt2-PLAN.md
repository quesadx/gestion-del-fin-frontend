# Quick Task 260525-qt2: Make UI Mobile Responsive + Accessibility

**Mode:** quick
**Directory:** `260525-qt2-ui-mobile-responsive-accessibility`
**Date:** 2026-05-25

## Must Haves

- Mobile responsive layout across all pages (breakpoints at 640px, 768px)
- Accessibility features: aria labels, roles, focus management, semantic HTML
- Fix Modal and ConfirmDialog for a11y (role="dialog", aria-modal, focus trapping)
- Add skip-to-content link
- Ensure all interactive elements have visible focus indicators
- Responsive Dock navigation overflow handling
- Touch-friendly tap targets on mobile

## Task 1: Global responsive + a11y improvements

**Files:**
- `src/index.css` — Add skip-to-content styles, focus-visible, responsive utilities
- `src/App.tsx` — Add skip-to-content link
- `src/layouts/DashboardLayout.tsx` — Responsive header, mobile nav improvements
- `src/layouts/AuthLayout.tsx` — Mobile padding adjustments

**Action:**
- Add `skip-to-content` link in App.tsx
- Add `focus-visible` ring styles in index.css for keyboard users
- Make DashboardLayout header responsive (stack camp switcher on mobile)
- Add responsive padding/margins for mobile in layouts
- Add `aria-current="page"` to active nav items
- Ensure proper heading hierarchy

**Verify:**
- Skip link visible on tab, hidden otherwise
- Focus-visible rings appear on all interactive elements
- Layout doesn't overflow horizontally on 375px viewport

## Task 2: Modal + ConfirmDialog accessibility

**Files:**
- `src/components/Modal.tsx`
- `src/components/ConfirmDialog.tsx`

**Action:**
- Add `role="dialog"`, `aria-modal="true"`, `aria-label` or `aria-labelledby`
- Add focus trapping (trap focus within modal when open)
- Restore focus to trigger element on close
- Add proper Escape key handling
- Add inert behavior to background content

**Verify:**
- Screen reader announces modal/dialog on open
- Focus is trapped within modal
- Tab order is correct
- Escape closes modal

## Task 3: Responsive tables and data lists

**Files:**
- `src/index.css` — Enhanced responsive table styles
- All feature files with tables: PopulationRoster, InventoryList, AdmissionList, TransferList, ExpeditionList, ResourcesPage, RationsPage, UsersPage, RolesPage, PermissionsPage, ProfessionsPage, CampManagement, InventoryAudit

**Action:**
- Improve responsive table styles (card-based layout on mobile)
- Add responsive breakpoints to grid layouts
- Ensure touch-friendly tap targets (min 44px)
- Add aria-labels to action buttons (edit, delete, transfer)
- Add `scope="col"` to table headers

**Verify:**
- Tables scroll horizontally on mobile without breaking layout
- Minimum tap target size 44x44px on mobile
- Screen readers identify table headers properly

## Task 4: Dock navigation mobile optimization

**Files:**
- `src/components/navigation/Dock.tsx`
- `src/components/navigation/Dock.css`

**Action:**
- Make dock horizontally scrollable on mobile when items overflow
- Ensure touch targets are large enough
- Add proper aria attributes

**Verify:**
- All nav items accessible on mobile without overflow
- Touch interaction works on mobile
