# Quick Task 260523-p11: ReactBits UI Refactor Pass 1+2 — Summary

**Status:** complete
**Date:** 2026-05-24

## What was done

### Pass 2a — Components installed
- `src/components/ClickSpark.tsx` — Canvas particle burst on primary button clicks
- `src/components/CountUp.tsx` — Animated number counter (IntersectionObserver + requestAnimationFrame)
- `src/components/DecryptedText.tsx` — Character scramble-reveal for status badges/IDs
- `src/components/StarBorder.tsx` + `.css` — Traveling glow particle orbiting button borders
- `src/components/GlareHover.tsx` + `.css` — Metallic light-sweep sheen on hover

### Pass 2a — Components integrated

| Component | Where | What |
|---|---|---|
| **BlurText** | `LoginPage.tsx` h1, `DashboardPage.tsx` h1 | Word/letter blur-in animation replacing static headings |
| **ClickSpark** | `TacticalButton.tsx` (primary variant) | Particle burst on all primary CTA button clicks |
| **CountUp** | `DashboardPage.tsx` (7 stat cards) | Animated count-up for campCount, resourceCount, autoDailyCount, peopleInCamp, inventoryItemCount, activeExplorationsInCamp |
| **DecryptedText** | `StatusBadge.tsx` | Scramble-reveal on all status badge labels across the app |
| **StarBorder** | `TacticalButton.tsx` (primary variant) | Traveling glow particle orbiting primary CTA buttons (wraps ClickSpark) |

### Pass 2b — Roundness refactor (design system modernization)

| Change | Files affected |
|---|---|
| Bumped `--gdf-radius-sm`: 2px → 4px | `tokens.css` |
| Bumped `--gdf-radius-md`: 6px → 10px | `tokens.css` |
| Bumped `--gdf-radius-lg`: 10px → 14px | `tokens.css` |
| Added `--gdf-radius-xl`: 20px, `--gdf-radius-2xl`: 26px | `tokens.css` |
| Migrated `rounded-sm` → `rounded-md` on ~70 form inputs | 14 feature page files |
| Migrated `rounded-sm` → `rounded-md` on UI primitives | `button.tsx`, `badge.tsx`, `card.tsx`, `checkbox.tsx`, `command.tsx`, `context-menu.tsx`, `dialog.tsx`, `dropdown-menu.tsx`, `menubar.tsx`, `resizable.tsx`, `select.tsx`, `sheet.tsx` |
| Rounded GF Logo square (sharp 90° → rounded-md) | `AppShell.tsx` |

### Pass 2a — Styling cleanup

| Change | File |
|---|---|
| Removed JetBrains Mono font import | `globals.css` |
| Changed `.font-mono-data`, `.font-mono`, `.font-mono-sm` font-family from JetBrains Mono → Inter | `globals.css` |
| Changed `--gdf-font-mono` from JetBrains Mono → Inter | `tokens.css` |
| Changed `fontFamily.mono` from JetBrains Mono → Inter | `tailwind.config.js` |
| Removed `.brutalist-border` class (zero uses) | `globals.css` |
| Removed legacy `.glass`, `.glass-heavy`, `.glass-interactive` classes (zero JSX uses) | `globals.css` |
| Removed `.gdf-glass-card` class (zero uses) | `globals.css` |
| Removed unused `useEffect`, `Tent`, `localCampId`, `handleCampChange`, `ROLE_LANDING`, `useCamps` imports | `AppShell.tsx` |

### Pre-existing warnings (not introduced)
- Aurora.tsx: useEffect missing deps (blend, colorStops)
- Dock.tsx: useMemo missing dep (dockHeight)
- FadeContent.tsx: useEffect missing deps

## Skipped (scope too large for single quick task)
- AnimatedContent integration (29 page sections across 17 files — GSAP/ScrollTrigger dependency)
- FadeContent integration (3 img tags)
- SplitText, TiltedCard, ScrollReveal integration
- GlareHover integration on Input/Textarea primitives (ref-forwarding complexity)
- 7 remaining bonus ReactBits components (PixelTransition, SplashCursor, AnimatedList, InfiniteScroll, MagnetLines, RotatingText, ShinyText, Magnet)
