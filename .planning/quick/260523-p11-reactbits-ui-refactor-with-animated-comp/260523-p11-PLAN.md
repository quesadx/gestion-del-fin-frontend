# Quick Task 260523-p11: ReactBits UI Refactor Pass 1+2

**Description:** Integrate ReactBits animated components (Pass 1 BlurText/AnimatedContent/FadeContent + Pass 2 ClickSpark/CountUp/DecryptedText), clean up legacy terminal-style fonts and brutalist borders.

## Status
- [x] Aurora background — installed ✓, integrated ✓
- [x] Dock navigation — installed ✓, integrated ✓
- [ ] BlurText — installed but **not integrated** (zero imports)
- [ ] AnimatedContent — installed but **not integrated** (zero imports)
- [ ] FadeContent — installed but **not integrated** (zero imports)
- [ ] ClickSpark — **not installed**
- [ ] CountUp — **not installed**
- [ ] DecryptedText — **not installed**
- [ ] Old styles cleanup — font-mono-data/font-mono/font-mono-sm, brutalist-border, neon undefined vars

## Tasks

### Task 1: Install Pass 2 ReactBits components
**Files:** `src/components/ClickSpark.tsx`, `src/components/CountUp.tsx`, `src/components/DecryptedText.tsx`
**Action:** Fetch component source from reactbits.dev and save as copy-paste files
**Verify:** Files exist, valid TypeScript/TSX, proper exports
**Done:** All 3 files created

### Task 2: Integrate BlurText on all h1/h2 headings
**Files:** All page files with `<h1>` or `<h2>` elements
**Action:** Replace every `<h1>` with `<BlurText>` and prominent `<h2>` with `<BlurText>`
**Verify:** grep for `<h1` returns zero plain tags
**Done:** All headings converted

### Task 3: Integrate AnimatedContent on page sections
**Files:** All page files
**Action:** Wrap top-level sections in `<AnimatedContent direction="vertical">` with staggered delays
**Verify:** Every page has AnimatedContent wrappers on major sections
**Done:** All sections wrapped

### Task 4: Integrate FadeContent on images/decorative elements
**Files:** Files with images, avatars, decorative graphics
**Action:** Wrap hero images, avatars, illustrations in `<FadeContent blur={true}>`
**Verify:** Images are wrapped in FadeContent
**Done:** All images wrapped

### Task 5: Integrate ClickSpark on primary CTA buttons
**Files:** Files with primary submit/action buttons
**Action:** Wrap primary buttons in `<ClickSpark>`
**Verify:** Primary buttons have ClickSpark wrapper
**Done:** CTAs wrapped

### Task 6: Integrate CountUp on stat/metric numbers
**Files:** DashboardPage, any stats/metrics displays
**Action:** Replace static numbers with `<CountUp to={value}>`
**Verify:** Stat numbers animate on view
**Done:** Stats converted

### Task 7: Integrate DecryptedText on status badges
**Files:** StatusBadge component, online status indicators
**Action:** Replace static badge text with `<DecryptedText>` for status/version labels
**Verify:** Badges scramble-reveal on view
**Done:** Badges converted

### Task 8: Clean up legacy terminal-style fonts and brutalist borders
**Files:** `globals.css`, `tailwind.config.js`, all page files using `font-mono-data`
**Action:**
- Remove `.brutalist-border` (zero uses)
- Remove `.font-mono-data` / `.font-mono` / `.font-mono-sm` CSS definitions
- Remove JetBrains Mono Google Font import
- Remove `.gdf-glass-card` (zero uses)
- Remove legacy `.glass` / `.glass-heavy` / `.glass-interactive` (zero uses in JSX)
- Replace `font-mono-data` usages with `font-sans text-xs`
- Replace `font-mono` usages with `font-sans text-sm`
**Verify:** No `font-mono-data` or `font-mono` class usages remain
**Done:** Legacy styles removed
