---
phase: 02-tactical-ui-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/alert-dialog.tsx
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - "Dialog modals render with glass translucent background and heavy blur"
    - "Sheet sidebar drawer renders with glass panel and gdf-styled close button"
    - "Alert dialogs render with glass overlay and gdf accent-colored action buttons"
    - "All three components use consistent glass overlay standard (heavy glass + 24px blur + cyan-tinted border)"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "Updated DialogContent, DialogOverlay with glass classes"
      contains: "gdf-glass-overlay"
    - path: "src/components/ui/sheet.tsx"
      provides: "Updated SheetContent, SheetOverlay with glass classes"
      contains: "gdf-glass-heavy"
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "Updated AlertDialogContent, AlertDialogOverlay with glass classes"
      contains: "gdf-glass-overlay"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "src/app/styles/globals.css"
      via: "css class"
      pattern: "gdf-glass-overlay"
    - from: "src/components/ui/sheet.tsx"
      to: "src/app/styles/globals.css"
      via: "css class"
      pattern: "gdf-glass-heavy"
</must_haves>
---

<objective>
Restyle Dialog, Sheet, and AlertDialog shadcn/ui primitives with the gdf glass overlay standard. These three components create floating panels — the most visible overlay elements in the application. Dialog is imported by 10 feature pages for create/edit forms; Sheet powers the mobile sidebar in AppShell; AlertDialog provides destructive action confirmations.

Purpose: These are the highest-impact overlay components (P1 priority from CONTEXT.md). They currently render with solid `bg-background` and generic borders, breaking the holographic glass aesthetic. After this plan, all modals and drawer overlays will render with heavy glass translucency, 24px backdrop blur, and cyan-tinted glass borders.

Output: dialog.tsx, sheet.tsx, alert-dialog.tsx updated with glass overlay standard classes.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md
@src/app/styles/tokens.css
@src/app/styles/globals.css
@tailwind.config.js
@src/components/ui/dialog.tsx
@src/components/ui/sheet.tsx
@src/components/ui/alert-dialog.tsx
@AGENTS.md

<interfaces>
<!-- Dialog.tsx already exported sub-components (all named exports) -->
Dialog, DialogPortal, DialogOverlay, DialogTrigger, DialogClose, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription

<!-- Sheet.tsx already exported sub-components -->

Sheet, SheetPortal, SheetOverlay, SheetTrigger, SheetClose, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription

<!-- AlertDialog.tsx already exported sub-components -->

AlertDialog, AlertDialogPortal, AlertDialogOverlay, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogFooter, AlertDialogTitle, AlertDialogDescription, AlertDialogAction, AlertDialogCancel

<!-- Glass Overlay Standard (from globals.css after Plan 02-01) -->

.gdf-glass-overlay — heavy glass bg + 24px blur + glass border + shadow + md radius
.gdf-glass-heavy — heavy glass bg + 24px blur + glass border (no shadow)
.backdrop-blur-glass-heavy — 24px blur utility from tailwind.config.js
.backdrop-blur-glass — 16px blur utility
.gdf-interactive-hover — interactive hover highlight
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Dialog component with glass overlay standard</name>
  <files>src/components/ui/dialog.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (full file, 104 lines)
    - src/app/styles/globals.css (lines 240-300 for gdf-glass-* utilities created in Plan 02-01)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 244-250 for D-07 Glass Dialog/Modal spec)
  </read_first>
  <action>
    In `src/components/ui/dialog.tsx`, modify the default className of two sub-components:

    **DialogOverlay** (line 23-24):
    Replace current className:
    ```
    'fixed inset-0 z-50 bg-black/80  data-[state=open]:animate-in ...'
    ```
    With:
    ```
    'fixed inset-0 z-50 bg-gdf-surface-root/60 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0'
    ```
    (Change `bg-black/80` to `bg-gdf-surface-root/60 backdrop-blur-sm`. Preserve all animation classes.)

    **DialogContent** (lines 40-42):
    Replace current className:
    ```
    'fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-6 shadow-lg duration-200 ... sm:rounded-sm'
    ```
    With:
    ```
    'fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 gdf-glass-overlay p-6 duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%] sm:max-w-md'
    ```
    (Replace `border bg-background shadow-lg sm:rounded-sm` with `gdf-glass-overlay` which bundles bg+border+blur+shadow+radius. Add `sm:max-w-md` for mobile safety per D-12.)

    **Close button** (lines 47-49):
    Replace `ring-offset-background` with `ring-offset-gdf-surface-root` and `focus:ring-ring` with `focus:ring-gdf-accent-primary` and `data-[state=open]:bg-accent data-[state=open]:text-muted-foreground` with `data-[state=open]:bg-gdf-accent-primary/10 data-[state=open]:text-gdf-accent-primary`:
    ```
    className="absolute right-4 top-4 rounded-sm opacity-70 ring-offset-gdf-surface-root transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-gdf-accent-primary focus:ring-offset-2 disabled:pointer-events-none data-[state=open]:bg-gdf-accent-primary/10 data-[state=open]:text-gdf-accent-primary"
    ```

    Do NOT change any animation classes, positioning, z-index, or layout classes. Do NOT change any exported names or component signatures.

  </action>
  <acceptance_criteria>
    - DialogOverlay className contains `bg-gdf-surface-root/60` (not `bg-black/80`)
    - DialogOverlay className contains `backdrop-blur-sm`
    - DialogContent className contains `gdf-glass-overlay` (not `bg-background border`)
    - DialogContent className contains `sm:max-w-md`
    - Close button className contains `ring-offset-gdf-surface-root` (not `ring-offset-background`)
    - Close button className contains `focus:ring-gdf-accent-primary` (not `focus:ring-ring`)
    - grep `bg-black` in dialog.tsx returns 0 matches
    - grep `bg-background` in dialog.tsx returns 0 matches
    - grep `ring-offset-background` in dialog.tsx returns 0 matches
    - No animation classes removed (data-[state=open]:animate-in etc. preserved)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/dialog.tsx && grep -c "bg-gdf-surface-root/60" src/components/ui/dialog.tsx && grep -c "sm:max-w-md" src/components/ui/dialog.tsx'</automated>
  </verify>
  <done>DialogContent uses gdf-glass-overlay (heavy glass + blur + border); DialogOverlay uses gdf-surface-root/60 + backdrop-blur-sm; close button uses gdf color tokens; all animation classes preserved</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Sheet component with glass drawer styling</name>
  <files>src/components/ui/sheet.tsx</files>
  <read_first>
    - src/components/ui/sheet.tsx (full file, 122 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (line 118-122 for D-10 responsive refinement spec)
  </read_first>
  <action>
    In `src/components/ui/sheet.tsx`, modify three sub-components:

    **SheetOverlay** (lines 22-25):
    Replace `bg-black/80` with `bg-gdf-surface-root/60 backdrop-blur-sm`:
    ```
    className={cn(
      'fixed inset-0 z-50 bg-gdf-surface-root/60 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0',
      className,
    )}
    ```

    **SheetContent** base variant (line 34 — the `cva` call):
    Replace `bg-background` with `gdf-glass-heavy` in the base variant string. Change from:
    ```
    'fixed z-50 gap-4 bg-background p-6 shadow-lg transition ease-in-out data-[state=closed]:duration-300 data-[state=open]:duration-500 data-[state=open]:animate-in data-[state=closed]:animate-out',
    ```
    To:
    ```
    'fixed z-50 gap-4 gdf-glass-heavy p-6 shadow-lg transition ease-in-out data-[state=closed]:duration-300 data-[state=open]:duration-500 data-[state=open]:animate-in data-[state=closed]:animate-out',
    ```
    `gdf-glass-heavy` provides bg + border + 24px blur. Remove `bg-background` since gdf-glass-heavy already sets background.

    **Close button** inside SheetContent (lines 64-66):
    Replace `ring-offset-background` with `ring-offset-gdf-surface-root` and `focus:ring-ring` with `focus:ring-gdf-accent-primary` and `data-[state=open]:bg-secondary` with `data-[state=open]:bg-gdf-accent-primary/10`:
    ```
    className="absolute right-4 top-4 rounded-sm opacity-70 ring-offset-gdf-surface-root transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-gdf-accent-primary focus:ring-offset-2 disabled:pointer-events-none data-[state=open]:bg-gdf-accent-primary/10"
    ```

    **SheetHeader** (lines 74-76):
    Add border-bottom separator for visual separation:
    ```
    <div className={cn('flex flex-col space-y-2 text-center sm:text-left border-b border-gdf-glass-border pb-4', className)} {...props} />
    ```

    Do NOT change any animation classes, variant definitions (top/bottom/left/right), or cva structure.

  </action>
  <acceptance_criteria>
    - SheetOverlay className contains `bg-gdf-surface-root/60` and `backdrop-blur-sm`
    - sheetVariants base class contains `gdf-glass-heavy` (not `bg-background`)
    - Close button uses `ring-offset-gdf-surface-root` and `focus:ring-gdf-accent-primary`
    - SheetHeader has `border-b border-gdf-glass-border pb-4`
    - grep `bg-background` in sheet.tsx returns 0 matches
    - grep `ring-offset-background` in sheet.tsx returns 0 matches
    - All side variants (top/bottom/left/right) and animation classes preserved
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-heavy" src/components/ui/sheet.tsx && grep -c "bg-gdf-surface-root/60" src/components/ui/sheet.tsx && grep -c "border-gdf-glass-border" src/components/ui/sheet.tsx'</automated>
  </verify>
  <done>SheetContent uses gdf-glass-heavy; SheetOverlay uses gdf-surface-root/60 + blur; close button uses gdf tokens; SheetHeader has glass border separator</done>
</task>

<task type="auto">
  <name>Task 3: Restyle AlertDialog component with glass overlay standard</name>
  <files>src/components/ui/alert-dialog.tsx</files>
  <read_first>
    - src/components/ui/alert-dialog.tsx (full file, 115 lines)
    - src/components/ui/dialog.tsx (the already-restyled dialog.tsx for pattern consistency)
  </read_first>
  <action>
    In `src/components/ui/alert-dialog.tsx`, apply the same glass overlay standard used in dialog.tsx:

    **AlertDialogOverlay** (lines 17-19):
    Replace `bg-black/80` with `bg-gdf-surface-root/60 backdrop-blur-sm`:
    ```
    className={cn(
      'fixed inset-0 z-50 bg-gdf-surface-root/60 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0',
      className,
    )}
    ```

    **AlertDialogContent** (lines 36-38):
    Replace `bg-background border shadow-lg sm:rounded-sm` with `gdf-glass-overlay`:
    ```
    className={cn(
      'fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 gdf-glass-overlay p-6 duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%]',
      className,
    )}
    ```
    Note: remove `sm:rounded-sm` since gdf-glass-overlay includes `border-radius: var(--gdf-radius-md)` via CSS.

    **AlertDialogTitle** (line 64):
    Add `text-gdf-text-primary` for explicit text color:
    ```
    className={cn('text-lg font-semibold text-gdf-text-primary', className)}
    ```

    AlertDialogAction and AlertDialogCancel use `buttonVariants()` from button.tsx (already wired to shadcn variables). No changes needed.

  </action>
  <acceptance_criteria>
    - AlertDialogOverlay className contains `bg-gdf-surface-root/60` and `backdrop-blur-sm`
    - AlertDialogContent className contains `gdf-glass-overlay`
    - AlertDialogTitle className contains `text-gdf-text-primary`
    - grep `bg-black` in alert-dialog.tsx returns 0 matches
    - grep `bg-background` in alert-dialog.tsx returns 0 matches
    - All animation classes preserved
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/alert-dialog.tsx && grep -c "bg-gdf-surface-root/60" src/components/ui/alert-dialog.tsx && grep -c "text-gdf-text-primary" src/components/ui/alert-dialog.tsx'</automated>
  </verify>
  <done>AlertDialogContent uses gdf-glass-overlay; AlertDialogOverlay uses gdf-surface-root/60 + blur; title uses explicit text-primary color</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                     |
| -------------------------------- | ------------------------------- |
| N/A — CSS/className changes only | No user data crosses boundaries |

## STRIDE Threat Register

| Threat ID  | Category | Component                               | Disposition | Mitigation Plan                                           |
| ---------- | -------- | --------------------------------------- | ----------- | --------------------------------------------------------- |
| T-02-02-01 | N/A      | dialog.tsx, sheet.tsx, alert-dialog.tsx | accept      | Visual styling changes only; no executable logic modified |

No package install task — no slopcheck gate needed.
</threat_model>

<verification>
Phase verification: `pnpm build` succeeds with updated shadcn/ui components. All three overlay components render with glass translucent backgrounds and 24px blur in dev server. Close buttons have visible focus rings.
</verification>

<success_criteria>

- [ ] Dialog renders with gdf-glass-overlay (heavy glass bg + 24px blur + cyan border + shadow)
- [ ] Dialog overlay renders with 60% surface-root opacity + subtle blur
- [ ] Sheet renders with gdf-glass-heavy for mobile sidebar drawer
- [ ] SheetHeader shows border separator
- [ ] AlertDialog renders with same glass overlay standard as Dialog
- [ ] All close buttons use gdf color tokens and have visible focus rings
- [ ] `pnpm build` passes
- [ ] No animation regressions (all open/close animations preserved)
      </success_criteria>

<output>
Create `.planning/phases/02-tactical-ui-consolidation/02-02-SUMMARY.md` when done
</output>
