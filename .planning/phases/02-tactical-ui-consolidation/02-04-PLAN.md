---
phase: 02-tactical-ui-consolidation
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/ui/command.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/hover-card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/toggle.tsx
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - "Command palette renders with glass overlay and scanner sweep accent line"
    - "Tooltips render with glass translucent content and gdf text colors"
    - "Hover cards render with glass overlay standard"
    - "Input fields render with glass-styled background and gdf accent focus ring"
    - "Textarea fields mirror input glass styling"
    - "Toggle buttons use gdf accent colors for active state"
  artifacts:
    - path: "src/components/ui/command.tsx"
      provides: "Updated Command, CommandInput, CommandItem with glass classes"
      contains: "gdf-glass-overlay"
    - path: "src/components/ui/tooltip.tsx"
      provides: "Updated TooltipContent with glass classes"
      contains: "gdf-glass-heavy"
    - path: "src/components/ui/hover-card.tsx"
      provides: "Updated HoverCardContent with glass classes"
      contains: "gdf-glass-overlay"
    - path: "src/components/ui/input.tsx"
      provides: "Updated Input with glass border and accent focus ring"
      contains: "border-gdf-border-subtle"
    - path: "src/components/ui/textarea.tsx"
      provides: "Updated Textarea with glass border and accent focus ring"
      contains: "border-gdf-border-subtle"
    - path: "src/components/ui/toggle.tsx"
      provides: "Updated Toggle with gdf accent active state"
      contains: "data-[state=on]:bg-gdf-accent-primary"
  key_links:
    - from: "src/components/ui/input.tsx"
      to: "src/app/styles/globals.css"
      via: "css class"
      pattern: "border-gdf-border-subtle"
</must_haves>
---

<objective>
Restyle Command, Tooltip, HoverCard, Input, Textarea, and Toggle shadcn/ui primitives with gdf glass tokens. These cover P1-P3 priority from CONTEXT.md: floating panels (command palette, tooltips, hover cards) get the glass overlay standard; form input elements get glass-styled borders and accent focus rings.

Purpose: The Command palette (cmdk) is the most visible complex overlay — currently renders with solid `bg-popover`. Tooltips and HoverCards are used in the sidebar and throughout. Input/Textarea are used in every form across every feature page. Toggle provides interactive toggle buttons.

Output: 6 shadcn/ui component files updated with gdf glass styling.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md
@src/app/styles/tokens.css
@src/app/styles/globals.css
@src/components/ui/command.tsx
@src/components/ui/tooltip.tsx
@src/components/ui/hover-card.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/ui/toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Command palette with glass overlay + scanner accent</name>
  <files>src/components/ui/command.tsx</files>
  <read_first>
    - src/components/ui/command.tsx (full file, 143 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 231-236 for D-05 Glass Command Palette spec)
  </read_first>
  <action>
    In `src/components/ui/command.tsx`, modify the following sub-components:

    **Command root** (lines 17-18):
    Replace `bg-popover text-popover-foreground` with glass styling:
    ```
    className={cn(
      'flex h-full w-full flex-col overflow-hidden rounded-md gdf-glass-overlay text-gdf-text-primary',
      className,
    )}
    ```

    **CommandDialog** (line 30):
    Update the Command wrapper className:
    ```
    <Command className="[&_[cmdk-group-heading]]:px-2 [&_[cmdk-group-heading]]:font-medium [&_[cmdk-group-heading]]:text-gdf-text-muted [&_[cmdk-group]:not([hidden])_~[cmdk-group]]:pt-0 [&_[cmdk-group]]:px-2 [&_[cmdk-input-wrapper]_svg]:h-5 [&_[cmdk-input-wrapper]_svg]:w-5 [&_[cmdk-input]]:h-12 [&_[cmdk-item]]:px-2 [&_[cmdk-item]]:py-3 [&_[cmdk-item]_svg]:h-5 [&_[cmdk-item]_svg]:w-5">
    ```
    Change `text-muted-foreground` to `text-gdf-text-muted` on cmdk-group-heading.

    **CommandInput wrapper** (line 42):
    Add glass border separator and input styling per D-05 (glass input with scanner accent):
    ```
    <div className="flex items-center border-b border-gdf-glass-border px-3" cmdk-input-wrapper="">
    ```
    Change from no explicit border class to `border-b border-gdf-glass-border`.

    **CommandInput** (lines 46-48):
    Add text color and placeholder:
    ```
    className={cn(
      'flex h-10 w-full rounded-md bg-transparent py-3 text-sm text-gdf-text-primary outline-none placeholder:text-gdf-text-muted disabled:cursor-not-allowed disabled:opacity-50',
      className,
    )}
    ```

    **CommandItem** (lines 113-114):
    Replace `data-[selected=true]:bg-accent data-[selected=true]:text-accent-foreground` with gdf accent:
    ```
    'relative flex cursor-default gap-2 select-none items-center rounded-sm px-2 py-1.5 text-sm text-gdf-text-secondary outline-none data-[disabled=true]:pointer-events-none data-[selected=true]:bg-gdf-accent-primary/15 data-[selected=true]:text-gdf-accent-primary data-[disabled=true]:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0',
    ```

    **CommandEmpty** (line 74):
    Add gdf text styling (per D-05: `text-gdf-text-muted with tactical icon`):
    ```
    <CommandPrimitive.Empty ref={ref} className="py-6 text-center text-sm text-gdf-text-muted" {...props} />
    ```

    **CommandGroup** (lines 85-87):
    Change heading text color:
    ```
    'overflow-hidden p-1 text-gdf-text-primary [&_[cmdk-group-heading]]:px-2 [&_[cmdk-group-heading]]:py-1.5 [&_[cmdk-group-heading]]:text-xs [&_[cmdk-group-heading]]:font-medium [&_[cmdk-group-heading]]:text-gdf-text-muted',
    ```

    **CommandSeparator** (line 101):
    Change `bg-border` to `bg-gdf-glass-border`:
    ```
    className={cn('-mx-1 h-px bg-gdf-glass-border', className)}
    ```

    **CommandShortcut** (line 126):
    Update:
    ```
    <span className={cn('ml-auto text-xs tracking-widest text-gdf-text-muted font-mono', className)} {...props} />
    ```

  </action>
  <acceptance_criteria>
    - Command root className contains `gdf-glass-overlay` (not `bg-popover`)
    - CommandInput wrapper has `border-b border-gdf-glass-border`
    - CommandItem uses `data-[selected=true]:bg-gdf-accent-primary/15` and `text-gdf-text-secondary`
    - CommandEmpty uses `text-gdf-text-muted`
    - CommandSeparator uses `bg-gdf-glass-border` (not `bg-border`)
    - CommandShortcut uses `text-gdf-text-muted font-mono`
    - grep `bg-popover` in command.tsx returns 0 matches
    - grep `bg-accent` in command.tsx returns 0 matches
    - grep `text-muted-foreground` in command.tsx returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/command.tsx && grep -c "border-gdf-glass-border" src/components/ui/command.tsx && grep -c "bg-gdf-accent-primary/15" src/components/ui/command.tsx && grep -c "text-gdf-text-muted" src/components/ui/command.tsx'</automated>
  </verify>
  <done>Command palette fully glass-styled: glass overlay root, glass border input separator, gdf accent selected items, gdf text colors throughout, glass border separator</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Tooltip and HoverCard with glass overlay standard</name>
  <files>src/components/ui/tooltip.tsx, src/components/ui/hover-card.tsx</files>
  <read_first>
    - src/components/ui/tooltip.tsx (full file, 32 lines)
    - src/components/ui/hover-card.tsx (full file, 27 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 257-261 for D-06 Glass Tooltip spec)
  </read_first>
  <action>
    **tooltip.tsx — TooltipContent** (lines 22-23):
    Replace `bg-primary px-3 py-1.5 text-xs text-primary-foreground`:
    ```
    className={cn(
      'z-50 overflow-hidden gdf-glass-overlay px-3 py-1.5 text-xs text-gdf-text-secondary animate-in fade-in-0 zoom-in-95 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-(--radix-tooltip-content-transform-origin)',
      className,
    )}
    ```
    Per D-06 spec: `bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy border border-gdf-glass-border rounded-md px-3 py-1.5 text-xs text-gdf-text-secondary`. `gdf-glass-overlay` provides bg+blur+border+radius; add `px-3 py-1.5 text-xs text-gdf-text-secondary`.

    **hover-card.tsx — HoverCardContent** (lines 18-19):
    Replace `bg-popover border shadow-md`:
    ```
    className={cn(
      'z-50 w-64 gdf-glass-overlay p-4 text-gdf-text-primary outline-none data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-(--radix-hover-card-content-transform-origin)',
      className,
    )}
    ```
    Replace `rounded-md border bg-popover p-4 text-popover-foreground shadow-md` with `gdf-glass-overlay p-4 text-gdf-text-primary`.

  </action>
  <acceptance_criteria>
    - TooltipContent className contains `gdf-glass-overlay` and `text-gdf-text-secondary`
    - HoverCardContent className contains `gdf-glass-overlay` and `text-gdf-text-primary`
    - grep `bg-primary` in tooltip.tsx returns 0 matches
    - grep `bg-popover` in hover-card.tsx returns 0 matches
    - All animation classes preserved in both files
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/tooltip.tsx && grep -c "gdf-glass-overlay" src/components/ui/hover-card.tsx'</automated>
  </verify>
  <done>Tooltip uses gdf-glass-overlay with text-gdf-text-secondary; HoverCard uses gdf-glass-overlay with text-gdf-text-primary</done>
</task>

<task type="auto">
  <name>Task 3: Restyle Input, Textarea, and Toggle with glass borders and accent focus rings</name>
  <files>src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/toggle.tsx</files>
  <read_first>
    - src/components/ui/input.tsx (full file, 22 lines)
    - src/components/ui/textarea.tsx (full file, 21 lines)
    - src/components/ui/toggle.tsx (full file, read for variant definitions)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 251-256 for D-06 Glass Inputs spec)
  </read_first>
  <action>
    **input.tsx — Input** (lines 10-12):
    Replace current className with glass input styling per D-06:
    ```
    className={cn(
      'flex h-9 w-full rounded-md border border-gdf-border-subtle bg-gdf-surface-base/50 px-3 py-1 text-sm text-gdf-text-primary shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium file:text-gdf-text-primary placeholder:text-gdf-text-muted focus-visible:outline-none focus-visible:border-gdf-accent-primary focus-visible:ring-1 focus-visible:ring-gdf-accent-primary/20 disabled:cursor-not-allowed disabled:opacity-50',
      className,
    )}
    ```
    Changes from current:
    - `rounded-sm` → `rounded-md` (consistency with gdf-radius-md)
    - `border border-input bg-transparent` → `border border-gdf-border-subtle bg-gdf-surface-base/50`
    - `text-base` → `text-sm text-gdf-text-primary` (explicit)
    - `placeholder:text-muted-foreground` → `placeholder:text-gdf-text-muted`
    - `focus-visible:ring-1 focus-visible:ring-ring` → `focus-visible:border-gdf-accent-primary focus-visible:ring-1 focus-visible:ring-gdf-accent-primary/20`
    - `file:text-foreground` → `file:text-gdf-text-primary`

    **textarea.tsx — Textarea** (lines 9-11):
    Apply same pattern as Input:
    ```
    className={cn(
      'flex min-h-[60px] w-full rounded-md border border-gdf-border-subtle bg-gdf-surface-base/50 px-3 py-2 text-sm text-gdf-text-primary shadow-sm placeholder:text-gdf-text-muted focus-visible:outline-none focus-visible:border-gdf-accent-primary focus-visible:ring-1 focus-visible:ring-gdf-accent-primary/20 disabled:cursor-not-allowed disabled:opacity-50',
      className,
    )}
    ```

    **toggle.tsx — Toggle** (lines for toggleVariants and Toggle component):
    Read the file first to locate the exact line numbers. Modify the toggle variants:
    - Default variant: `data-[state=on]:bg-accent data-[state=on]:text-accent-foreground` → `data-[state=on]:bg-gdf-accent-primary data-[state=on]:text-gdf-text-inverse`
    - Outline variant: `border border-input bg-transparent` → `border border-gdf-border-subtle bg-transparent`, `hover:bg-accent hover:text-accent-foreground` → `hover:bg-gdf-accent-primary/10 hover:text-gdf-accent-primary`
    - Hover state: `hover:bg-muted hover:text-muted-foreground` → `hover:bg-gdf-surface-hover hover:text-gdf-text-primary`
    - Focus ring: `focus-visible:ring-ring` → `focus-visible:ring-gdf-accent-primary`
    - Disabled: keep `disabled:opacity-50`

  </action>
  <acceptance_criteria>
    - Input className contains `border-gdf-border-subtle` (not `border-input`)
    - Input className contains `bg-gdf-surface-base/50` (not `bg-transparent`)
    - Input className contains `focus-visible:border-gdf-accent-primary` (not `focus-visible:ring-ring`)
    - Input className contains `placeholder:text-gdf-text-muted`
    - Textarea matches Input pattern for border, background, focus ring, placeholder
    - Toggle active state uses `bg-gdf-accent-primary` (not `bg-accent`)
    - grep `border-input` in input.tsx returns 0 matches
    - grep `border-input` in textarea.tsx returns 0 matches
    - grep `bg-accent` in toggle.tsx returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "border-gdf-border-subtle" src/components/ui/input.tsx && grep -c "border-gdf-border-subtle" src/components/ui/textarea.tsx && grep -c "bg-gdf-surface-base/50" src/components/ui/input.tsx && grep -c "focus-visible:border-gdf-accent-primary" src/components/ui/input.tsx'</automated>
  </verify>
  <done>Input and Textarea use glass-styled borders with gdf accent focus rings; Toggle uses gdf-accent-primary for active/on state and gdf-border-subtle for outline variant</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                     |
| -------------------------------- | ------------------------------- |
| N/A — CSS/className changes only | No user data crosses boundaries |

## STRIDE Threat Register

| Threat ID  | Category | Component                       | Disposition | Mitigation Plan                                           |
| ---------- | -------- | ------------------------------- | ----------- | --------------------------------------------------------- |
| T-02-04-01 | N/A      | All 6 shadcn/ui component files | accept      | Visual styling changes only; no executable logic modified |

No package install task — no slopcheck gate needed.
</threat_model>

<verification>
Phase verification: `pnpm build` succeeds. In dev server, verify Command palette renders with glass background; Tooltip shows translucent glass content; Input fields show subtle glass border with blue accent ring on focus; Toggle buttons show gdf-accent-primary blue when active.
</verification>

<success_criteria>

- [ ] Command palette renders with gdf-glass-overlay background
- [ ] Command input has glass border separator
- [ ] Command items selected state uses gdf-accent-primary/15 highlight
- [ ] Tooltip content renders glass translucent with gdf-text-secondary
- [ ] HoverCard content renders with gdf-glass-overlay
- [ ] Input fields have gdf-border-subtle default border and gdf-accent-primary focus ring
- [ ] Textarea matches Input glass styling
- [ ] Toggle active state uses gdf-accent-primary
- [ ] `pnpm build` passes
      </success_criteria>

<output>
Create `.planning/phases/02-tactical-ui-consolidation/02-04-SUMMARY.md` when done
</output>
