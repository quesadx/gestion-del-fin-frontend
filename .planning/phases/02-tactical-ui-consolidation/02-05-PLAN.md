---
phase: 02-tactical-ui-consolidation
plan: 05
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - src/components/ui/checkbox.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/calendar.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/breadcrumb.tsx
  - src/components/ui/context-menu.tsx
  - src/components/ui/menubar.tsx
  - src/components/ui/navigation-menu.tsx
  - src/components/ui/drawer.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/toggle-group.tsx
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - "Checkbox uses gdf-accent-primary for checked state"
    - "Slider track and thumb use gdf accent colors"
    - "Calendar uses gdf color tokens for selected/nav/today states"
    - "Tabs and Accordion use gdf text and border tokens"
    - "ContextMenu, Menubar, NavigationMenu use glass overlay content"
    - "All P3-P6 priority components have gdf token compatibility"
  artifacts:
    - path: "src/components/ui/checkbox.tsx"
      provides: "Updated Checkbox with gdf accent checked state"
      contains: "data-[state=checked]:bg-gdf-accent-primary"
    - path: "src/components/ui/calendar.tsx"
      provides: "Updated Calendar with gdf color tokens"
      contains: "bg-gdf-accent-primary"
    - path: "src/components/ui/tabs.tsx"
      provides: "Updated Tabs with gdf token colors"
      contains: "data-[state=active]:bg-gdf-surface-raised"
  key_links:
    - from: "src/components/ui/checkbox.tsx"
      to: "src/app/styles/tokens.css"
      via: "Tailwind"
      pattern: "bg-gdf-accent-primary"
</must_haves>
---

<objective>
Restyle the remaining P3-P5 shadcn/ui primitives (toggle, checkbox, slider, calendar, tabs, accordion, avatar, breadcrumb, context-menu, menubar, navigation-menu, drawer, badge, alert, toggle-group) with gdf token compatibility. These are lower-priority components but are collectively visible throughout the application.

Purpose: Complete the visual unification of all shadcn/ui primitives under the gdf-\* token system. After this plan, every shadcn/ui component renders with gdf colors instead of Tailwind defaults. Many of these components have minimal changes needed — just swapping `bg-accent`, `bg-primary`, `text-muted-foreground`, etc. for their gdf equivalents.

Output: 15 shadcn/ui component files updated with gdf token compatibility.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md
@src/app/styles/tokens.css
@src/app/styles/globals.css
@tailwind.config.js
@src/components/ui/toggle.tsx
@src/components/ui/checkbox.tsx
@src/components/ui/tabs.tsx
@src/components/ui/accordion.tsx
@src/components/ui/calendar.tsx
@src/components/ui/slider.tsx
@src/components/ui/avatar.tsx
@src/components/ui/breadcrumb.tsx
@src/components/ui/context-menu.tsx
@src/components/ui/menubar.tsx
@src/components/ui/navigation-menu.tsx
@src/components/ui/drawer.tsx
@src/components/ui/badge.tsx
@src/components/ui/alert.tsx
@src/components/ui/toggle-group.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle checkbox, slider, tabs, accordion with gdf accent tokens</name>
  <files>
    src/components/ui/checkbox.tsx,
    src/components/ui/slider.tsx,
    src/components/ui/tabs.tsx,
    src/components/ui/accordion.tsx
  </files>
  <read_first>
    - src/components/ui/checkbox.tsx (26 lines)
    - src/components/ui/slider.tsx (full file)
    - src/components/ui/tabs.tsx (53 lines)
    - src/components/ui/accordion.tsx (full file)
  </read_first>
  <action>
    Replace shadcn default color tokens with gdf equivalents in all 4 files.
    NOTE: toggle.tsx is already handled in Plan 02-04 (Wave 2) — do NOT modify it here.

    **checkbox.tsx** (line 14):
    - `border-primary` → `border-gdf-border-default`
    - `focus-visible:ring-ring` → `focus-visible:ring-gdf-accent-primary`
    - `data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground` → `data-[state=checked]:bg-gdf-accent-primary data-[state=checked]:text-gdf-text-inverse`

    **slider.tsx**: Read file for exact selectors. Apply:
    - Track: `bg-primary/20` → `bg-gdf-accent-primary/20` or similar shadcn defaults
    - Range: `bg-primary` → `bg-gdf-accent-primary`
    - Thumb: `border-primary bg-background` → `border-gdf-accent-primary bg-gdf-surface-raised`
    - Thumb: `focus-visible:ring-ring` → `focus-visible:ring-gdf-accent-primary`

    **tabs.tsx** (53 lines):
    - TabsList (line 15): `bg-muted text-muted-foreground` → `bg-gdf-surface-overlay text-gdf-text-secondary`
    - TabsTrigger (line 30): `ring-offset-background` → `ring-offset-gdf-surface-root`
    - TabsTrigger (line 30): `focus-visible:ring-ring` → `focus-visible:ring-gdf-accent-primary`
    - TabsTrigger (line 30): `data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow` → `data-[state=active]:bg-gdf-surface-raised data-[state=active]:text-gdf-text-primary data-[state=active]:shadow-sm`
    - TabsContent (line 45): `ring-offset-background focus-visible:ring-ring` → `ring-offset-gdf-surface-root focus-visible:ring-gdf-accent-primary`

    **accordion.tsx**: Read file for exact selectors. Apply:
    - Trigger: `hover:underline` + add `text-gdf-text-primary`
    - Trigger chevron: add `text-gdf-text-muted`
    - Content: add `text-gdf-text-secondary`
    - Any focus rings: `ring-ring` → `ring-gdf-accent-primary`

  </action>
  <acceptance_criteria>
    - checkbox.tsx: `data-[state=checked]:bg-gdf-accent-primary` present
    - slider.tsx: thumb uses `border-gdf-accent-primary`
    - tabs.tsx: `data-[state=active]:bg-gdf-surface-raised` present
    - accordion.tsx: uses gdf text tokens
    - grep `bg-primary` in checkbox.tsx returns 0 matches
    - grep `bg-background` in tabs.tsx returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'echo "checkbox:" && grep "gdf-" src/components/ui/checkbox.tsx | wc -l && echo "tabs:" && grep "gdf-" src/components/ui/tabs.tsx | wc -l && echo "slider:" && grep -c "gdf-" src/components/ui/slider.tsx && echo "accordion:" && grep -c "gdf-" src/components/ui/accordion.tsx'</automated>
  </verify>
  <done>Checkbox uses gdf-accent-primary for checked; Slider uses gdf accent track/thumb; Tabs use gdf surface colors; Accordion uses gdf text tokens. toggle.tsx was handled in Plan 02-04</done>
</task>

<task type="auto">
  <name>Task 2: Restyle calendar, avatar, breadcrumb, badge, alert with gdf tokens</name>
  <files>
    src/components/ui/calendar.tsx,
    src/components/ui/avatar.tsx,
    src/components/ui/breadcrumb.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/alert.tsx
  </files>
  <read_first>
    - src/components/ui/calendar.tsx (full file, check for classNames API usage)
    - src/components/ui/avatar.tsx (full file)
    - src/components/ui/breadcrumb.tsx (full file)
    - src/components/ui/badge.tsx (full file)
    - src/components/ui/alert.tsx (full file)
  </read_first>
  <action>
    **calendar.tsx**: The calendar uses a `classNames` API (lines 38-105+) which overrides react-day-picker's day picker. Replace shadcn defaults with gdf equivalents:
    - `day_selected: "bg-primary text-primary-foreground"` → `day_selected: "bg-gdf-accent-primary text-gdf-text-inverse"`
    - `day_today: "bg-accent text-accent-foreground"` → `day_today: "bg-gdf-accent-primary/15 text-gdf-accent-primary"`
    - `day_outside: "text-muted-foreground"` → `day_outside: "text-gdf-text-muted opacity-50"`
    - `nav_button: "hover:bg-accent hover:text-accent-foreground"` → `nav_button: "hover:bg-gdf-accent-primary/10 hover:text-gdf-accent-primary"`
    - `head_cell: "text-muted-foreground"` → `head_cell: "text-gdf-text-muted"`
    - `caption_label`: add `text-gdf-text-primary`

    **avatar.tsx**: Minimal changes — Avatar is mostly structural:
    - Image fallback: `bg-muted` → `bg-gdf-surface-overlay text-gdf-text-muted`

    **breadcrumb.tsx**: Minimal — text and separator colors:
    - Separator: add `text-gdf-text-muted`
    - Link: add `hover:text-gdf-text-primary`
    - Current page: ensure `text-gdf-text-primary`

    **badge.tsx**: Variants use shadcn defaults:
    - Default variant: `bg-primary text-primary-foreground` → `bg-gdf-accent-primary text-gdf-text-inverse`
    - Secondary variant: `bg-secondary text-secondary-foreground` → `bg-gdf-surface-overlay text-gdf-text-secondary`
    - Destructive variant: `bg-destructive text-destructive-foreground` → `bg-gdf-status-danger text-gdf-text-inverse`
    - Outline variant: `text-foreground` → `text-gdf-text-primary`

    **alert.tsx**: Alert variants:
    - Root: `[&>svg]:text-foreground` → `[&>svg]:text-gdf-text-primary`
    - Description: `text-muted-foreground` → `text-gdf-text-secondary`

    For each file, preserve all animation, layout, z-index, and structural classes. Only replace color/background/text tokens with gdf equivalents.

  </action>
  <acceptance_criteria>
    - calendar.tsx day_selected classNames uses `bg-gdf-accent-primary`
    - calendar.tsx day_today uses `bg-gdf-accent-primary/15`
    - badge.tsx default uses `bg-gdf-accent-primary`
    - avatar.tsx fallback uses `bg-gdf-surface-overlay`
    - breadcrumb.tsx link uses `text-gdf-text-muted`
    - alert.tsx description uses `text-gdf-text-secondary`
    - grep `bg-primary` in calendar.tsx returns 0 (in classNames)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'echo "calendar gdf:" && grep -c "gdf-" src/components/ui/calendar.tsx && echo "badge gdf:" && grep -c "gdf-" src/components/ui/badge.tsx && echo "avatar gdf:" && grep -c "gdf-" src/components/ui/avatar.tsx && echo "alert gdf:" && grep -c "gdf-" src/components/ui/alert.tsx'</automated>
  </verify>
  <done>Calendar uses gdf accent tokens for selected/today/nav states; Badge variants use gdf tokens; Avatar/Breadcrumb/Alert use gdf text and bg colors</done>
</task>

<task type="auto">
  <name>Task 3: Restyle context-menu, menubar, navigation-menu, drawer, toggle-group with glass/glow tokens</name>
  <files>
    src/components/ui/context-menu.tsx,
    src/components/ui/menubar.tsx,
    src/components/ui/navigation-menu.tsx,
    src/components/ui/drawer.tsx,
    src/components/ui/toggle-group.tsx
  </files>
  <read_first>
    - src/components/ui/context-menu.tsx (full file — likely large, similar pattern to dropdown-menu)
    - src/components/ui/menubar.tsx (full file)
    - src/components/ui/navigation-menu.tsx (full file)
    - src/components/ui/drawer.tsx (full file)
    - src/components/ui/toggle-group.tsx (full file)
  </read_first>
  <action>
    For each file, apply the gdf token replacement pattern used in earlier plans:

    **context-menu.tsx**: Same glass overlay standard as dropdown-menu.tsx (P5 priority):
    - Content: `bg-popover border` → `gdf-glass-overlay`
    - Items: `focus:bg-accent focus:text-accent-foreground` → `focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary`
    - Separator: `bg-muted` → `bg-gdf-glass-border`
    - Shortcut: `opacity-60` → `text-gdf-text-muted font-mono`

    **menubar.tsx** (P5 priority):
    - Root: `bg-background border` → `bg-gdf-glass-bg border border-gdf-glass-border backdrop-blur-glass`
    - Trigger: `data-[state=open]:bg-accent` → `data-[state=open]:bg-gdf-accent-primary/10`
    - Content: `bg-popover border` → `gdf-glass-overlay`
    - Items: same as context-menu item pattern

    **navigation-menu.tsx** (P5 priority):
    - Root: ensure `bg-transparent` (already likely)
    - Trigger: `hover:bg-accent hover:text-accent-foreground` → `hover:bg-gdf-accent-primary/10 hover:text-gdf-accent-primary`
    - Content/Viewport: `bg-popover border` → `gdf-glass-overlay`
    - Indicators/chevrons: add `text-gdf-text-muted`

    **drawer.tsx** (P6 priority — vaul-based drawer):
    - Overlay: `bg-black/80` → `bg-gdf-surface-root/60 backdrop-blur-sm`
    - Content: `bg-background` → `gdf-glass-heavy`
    - Handle: visible with `bg-gdf-border-default`

    **toggle-group.tsx** (P5 priority):
    - Root: `bg-muted` → `bg-gdf-surface-overlay border border-gdf-border-subtle rounded-md`
    - Items: reuse `toggleVariants` from toggle.tsx (already updated in Task 1)

    Preserve all animation classes, layout, z-index, and structural properties. Only change color/background/border tokens.

  </action>
  <acceptance_criteria>
    - context-menu.tsx Content uses `gdf-glass-overlay`
    - menubar.tsx Root uses `backdrop-blur-glass`
    - navigation-menu.tsx Content uses `gdf-glass-overlay`
    - drawer.tsx Content uses `gdf-glass-heavy`
    - toggle-group.tsx Root uses `bg-gdf-surface-overlay`
    - All 5 files: grep `bg-popover` returns 0 matches
    - All 5 files: grep `bg-accent` returns 0 matches
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'for f in context-menu menubar navigation-menu drawer toggle-group; do echo "$f gdf counts:" && grep -c "gdf-" src/components/ui/$f.tsx 2>/dev/null; done'</automated>
  </verify>
  <done>ContextMenu, Menubar, NavigationMenu use glass overlay content; Drawer uses glass-heavy; ToggleGroup uses gdf surface tokens; all P5-P6 components gdf-compatible</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                     |
| -------------------------------- | ------------------------------- |
| N/A — CSS/className changes only | No user data crosses boundaries |

## STRIDE Threat Register

| Threat ID  | Category | Component                        | Disposition | Mitigation Plan                                           |
| ---------- | -------- | -------------------------------- | ----------- | --------------------------------------------------------- |
| T-02-05-01 | N/A      | All 15 shadcn/ui component files | accept      | Visual styling changes only; no executable logic modified |

No package install task — no slopcheck gate needed.
</threat_model>

<verification>
Phase verification: `pnpm build` succeeds. Spot-check a few components in dev server — Toggle button active state, Checkbox checked state, Calendar day selection — all use gdf-accent-primary blue.
</verification>

<success_criteria>

- [ ] All P3-P5 components use gdf-\* color tokens (no shadcn default `bg-accent`, `bg-primary`, `text-muted-foreground` without gdf equivalent)
- [ ] ContextMenu, Menubar, NavigationMenu content panels use glass overlay standard
- [ ] Drawer uses glass-heavy background with gdf overlay
- [ ] Toggle-group uses gdf surface colors
- [ ] `pnpm build` passes
- [ ] No component lost animation or layout functionality
      </success_criteria>

<output>
Create `.planning/phases/02-tactical-ui-consolidation/02-05-SUMMARY.md` when done
</output>
