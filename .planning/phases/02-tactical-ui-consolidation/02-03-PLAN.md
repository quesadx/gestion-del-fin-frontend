---
phase: 02-tactical-ui-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/select.tsx
  - src/components/ui/popover.tsx
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - "Dropdown menus render with glass translucent content and gdf-accent colored item hover states"
    - "Select dropdowns render with glass trigger styling and glass content panel"
    - "Popovers render with glass overlay standard and glass border"
    - "All three components share consistent glass overlay standard"
  artifacts:
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "Updated DropdownMenuContent, DropdownMenuItem with glass classes"
      contains: "gdf-glass-overlay"
    - path: "src/components/ui/select.tsx"
      provides: "Updated SelectTrigger, SelectContent with glass classes"
      contains: "gdf-glass-bg"
    - path: "src/components/ui/popover.tsx"
      provides: "Updated PopoverContent with glass classes"
      contains: "gdf-glass-overlay"
  key_links:
    - from: "src/components/ui/dropdown-menu.tsx"
      to: "src/app/styles/globals.css"
      via: "css class"
      pattern: "gdf-glass-overlay"
    - from: "src/components/ui/select.tsx"
      to: "src/app/styles/globals.css"
      via: "css class"
      pattern: "backdrop-blur-glass"
</must_haves>
---

<objective>
Restyle DropdownMenu, Select, and Popover shadcn/ui primitives with the gdf glass overlay standard. These are the highest-priority components (P0 from CONTEXT.md D-01) because they currently render with solid opaque `bg-popover` backgrounds, completely breaking the holographic glass aesthetic.

Purpose: DropdownMenu is used in the sidebar and context menus throughout the app. Select is used in the camp selector (AppShell) and form fields across all feature pages. Popover powers hover-triggered panels. All three currently render with solid dark backgrounds and no translucency — the most immersion-breaking UI elements in the app.

Output: dropdown-menu.tsx, select.tsx, popover.tsx updated with glass overlay standard classes.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md
@src/app/styles/tokens.css
@src/app/styles/globals.css
@tailwind.config.js
@src/components/ui/dropdown-menu.tsx
@src/components/ui/select.tsx
@src/components/ui/popover.tsx

<interfaces>
<!-- After Plan 02-01, the following are available in globals.css: -->
.gdf-glass-overlay — heavy glass bg + 24px blur + glass border + shadow + md radius
.gdf-glass-heavy — heavy glass bg + 24px blur + glass border
.backdrop-blur-glass — 16px blur (Tailwind utility)
.backdrop-blur-glass-heavy — 24px blur (Tailwind utility)
.bg-gdf-glass-bg — 65% opacity glass via Tailwind color: gdf.glass.bg
.border-gdf-glass-border — cyan-tinted glass border via Tailwind color: gdf.glass.border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle DropdownMenu with glass overlay standard</name>
  <files>src/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - src/components/ui/dropdown-menu.tsx (full file, 188 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 224-229 for D-05 Glass Dropdown Menu spec)
  </read_first>
  <action>
    In `src/components/ui/dropdown-menu.tsx`, modify the following sub-components:

    **DropdownMenuContent** (lines 65-68):
    Replace current className which uses `bg-popover border shadow-md`:
    ```
    'z-50 max-h-[var(--radix-dropdown-menu-content-available-height)] min-w-[8rem] overflow-y-auto overflow-x-hidden rounded-md border gdf-glass-overlay p-1',
    ```
    Change to:
    ```
    'z-50 max-h-[var(--radix-dropdown-menu-content-available-height)] min-w-[8rem] overflow-y-auto overflow-x-hidden gdf-glass-overlay p-1',
    ```
    Remove `rounded-md border` since `gdf-glass-overlay` already provides border and radius.

    **DropdownMenuSubContent** (lines 48-49):
    Same change — replace `bg-popover border shadow-lg` with `gdf-glass-overlay`:
    ```
    'z-50 min-w-[8rem] overflow-hidden gdf-glass-overlay p-1 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2',
    ```

    **DropdownMenuItem** (lines 84-85):
    Update hover/focus states to use gdf accent tokens per D-05 (items: hover→`bg-gdf-accent-primary/10 text-gdf-accent-primary`):
    ```
    'relative flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm text-gdf-text-secondary outline-none transition-colors focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary data-[disabled]:pointer-events-none data-[disabled]:opacity-50 [&>svg]:size-4 [&>svg]:shrink-0',
    ```
    Changes: add `text-gdf-text-secondary`, change `focus:bg-accent focus:text-accent-foreground` to `focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary`.

    **DropdownMenuSubTrigger** (lines 30-31):
    Update focus/open state:
    ```
    'flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm text-gdf-text-secondary outline-none focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary data-[state=open]:bg-gdf-accent-primary/10 data-[state=open]:text-gdf-accent-primary [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0',
    ```

    **DropdownMenuCheckboxItem + DropdownMenuRadioItem** (lines 100-101 and 123-124):
    Change `focus:bg-accent focus:text-accent-foreground` to `focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary`.

    **DropdownMenuSeparator** (line 159):
    Change `bg-muted` to `bg-gdf-glass-border`:
    ```
    className={cn('-mx-1 my-1 h-px bg-gdf-glass-border', className)}
    ```

    **DropdownMenuShortcut** (line 167):
    Change `opacity-60` to `text-gdf-text-muted`:
    ```
    <span className={cn('ml-auto text-xs tracking-widest text-gdf-text-muted font-mono', className)} {...props} />
    ```

    **DropdownMenuLabel** (line 147):
    Add explicit text color:
    ```
    className={cn('px-2 py-1.5 text-sm font-semibold text-gdf-text-primary', inset && 'pl-8', className)}
    ```

    Do NOT change any animation classes, z-index, layout, or data-[state] attributes.

  </action>
  <acceptance_criteria>
    - DropdownMenuContent className contains `gdf-glass-overlay` (not `bg-popover border`)
    - DropdownMenuSubContent className contains `gdf-glass-overlay`
    - DropdownMenuItem className contains `text-gdf-text-secondary` and `focus:bg-gdf-accent-primary/10`
    - DropdownMenuSeparator className contains `bg-gdf-glass-border` (not `bg-muted`)
    - DropdownMenuShortcut className contains `text-gdf-text-muted font-mono` (not `opacity-60`)
    - DropdownMenuLabel className contains `text-gdf-text-primary`
    - grep `bg-popover` in dropdown-menu.tsx returns 0 matches
    - grep `bg-accent` in dropdown-menu.tsx returns 0 matches
    - grep `bg-muted` in dropdown-menu.tsx returns 0 matches
    - All animation classes preserved
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/dropdown-menu.tsx && grep -c "text-gdf-text-secondary" src/components/ui/dropdown-menu.tsx && grep -c "bg-gdf-glass-border" src/components/ui/dropdown-menu.tsx && grep -c "bg-gdf-accent-primary/10" src/components/ui/dropdown-menu.tsx'</automated>
  </verify>
  <done>DropdownMenu component fully glass-styled: glass overlay content, gdf accent item states, glass border separators, gdf text colors for label/shortcut/items</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Select with glass trigger and glass content panel</name>
  <files>src/components/ui/select.tsx</files>
  <read_first>
    - src/components/ui/select.tsx (full file, 152 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 216-222 for D-04 Glass Select spec)
  </read_first>
  <action>
    In `src/components/ui/select.tsx`, modify three sub-components:

    **SelectTrigger** (lines 21-23):
    Replace current className. Per D-04: trigger gets `bg-gdf-glass-bg backdrop-blur-glass border border-gdf-glass-border`. Change from:
    ```
    'flex h-9 w-full items-center justify-between whitespace-nowrap rounded-md border border-input bg-transparent px-3 py-2 text-sm shadow-sm ring-offset-background data-[placeholder]:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-ring disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1'
    ```
    To:
    ```
    'flex h-9 w-full items-center justify-between whitespace-nowrap rounded-md border border-gdf-glass-border bg-gdf-glass-bg backdrop-blur-glass px-3 py-2 text-sm text-gdf-text-primary shadow-sm ring-offset-gdf-surface-root data-[placeholder]:text-gdf-text-muted focus:outline-none focus:ring-1 focus:ring-gdf-accent-primary focus:border-gdf-accent-primary disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1'
    ```
    Changes: `border-input bg-transparent` → `border-gdf-glass-border bg-gdf-glass-bg backdrop-blur-glass`, `text-gdf-text-primary`, `ring-offset-background` → `ring-offset-gdf-surface-root`, `placeholder:text-muted-foreground` → `data-[placeholder]:text-gdf-text-muted`, `focus:ring-ring` → `focus:ring-gdf-accent-primary`, add `focus:border-gdf-accent-primary` for border glow on open.

    **SelectContent** (lines 70-71):
    Replace `bg-popover border shadow-md` with `gdf-glass-overlay`:
    ```
    'relative z-50 max-h-(--radix-select-content-available-height) min-w-[8rem] overflow-y-auto overflow-x-hidden gdf-glass-overlay data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-(--radix-select-content-transform-origin)',
    ```
    Remove `rounded-md border` since gdf-glass-overlay provides them.

    **SelectItem** (lines 113-114):
    Update focus state to use gdf accent:
    ```
    'relative flex w-full cursor-default select-none items-center rounded-sm py-1.5 pl-2 pr-8 text-sm text-gdf-text-secondary outline-none focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
    ```
    Change: `focus:bg-accent focus:text-accent-foreground` → `focus:bg-gdf-accent-primary/10 focus:text-gdf-accent-primary`, add `text-gdf-text-secondary`.

    **SelectSeparator** (line 134):
    Change `bg-muted` to `bg-gdf-glass-border`.

    **SelectLabel** (line 100):
    Add text color:
    ```
    className={cn('px-2 py-1.5 text-sm font-semibold text-gdf-text-primary', className)}
    ```

    Do NOT change SelectScrollUpButton, SelectScrollDownButton animation, or position-related classes.

  </action>
  <acceptance_criteria>
    - SelectTrigger className contains `bg-gdf-glass-bg` and `backdrop-blur-glass`
    - SelectTrigger className contains `border-gdf-glass-border` and `focus:border-gdf-accent-primary`
    - SelectContent className contains `gdf-glass-overlay`
    - SelectItem className contains `focus:bg-gdf-accent-primary/10` and `text-gdf-text-secondary`
    - SelectSeparator className contains `bg-gdf-glass-border`
    - grep `bg-popover` in select.tsx returns 0 matches
    - grep `bg-accent` in select.tsx returns 0 matches
    - grep `bg-muted` in select.tsx returns 0 matches
    - grep `border-input` in select.tsx returns 0 matches
    - All animation classes preserved
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/select.tsx && grep -c "backdrop-blur-glass" src/components/ui/select.tsx && grep -c "bg-gdf-glass-bg" src/components/ui/select.tsx && grep -c "bg-gdf-accent-primary/10" src/components/ui/select.tsx'</automated>
  </verify>
  <done>Select trigger uses glass bg + 16px blur + glass border with accent focus border; SelectContent uses glass overlay; items use gdf accent hover states</done>
</task>

<task type="auto">
  <name>Task 3: Restyle Popover with glass overlay standard</name>
  <files>src/components/ui/popover.tsx</files>
  <read_first>
    - src/components/ui/popover.tsx (full file, 31 lines)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 238-242 for D-06 Glass Popover spec)
  </read_first>
  <action>
    In `src/components/ui/popover.tsx`, modify PopoverContent (lines 21-23):

    Replace current className:
    ```
    'z-50 w-72 rounded-md border bg-popover p-4 text-popover-foreground shadow-md outline-none data-[state=open]:animate-in ...'
    ```
    With:
    ```
    'z-50 w-72 gdf-glass-overlay p-4 text-gdf-text-primary outline-none data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 origin-(--radix-popover-content-transform-origin)',
    ```

    Changes:
    - Replace `rounded-md border bg-popover text-popover-foreground shadow-md` with `gdf-glass-overlay text-gdf-text-primary`
    - `gdf-glass-overlay` bundles bg+border+shadow+blur+radius
    - `text-gdf-text-primary` replaces `text-popover-foreground`
    - All animation classes preserved

    Do NOT change `align`, `sideOffset`, or any component signature.

  </action>
  <acceptance_criteria>
    - PopoverContent className contains `gdf-glass-overlay`
    - PopoverContent className contains `text-gdf-text-primary`
    - grep `bg-popover` in popover.tsx returns 0 matches
    - grep `text-popover-foreground` in popover.tsx returns 0 matches
    - grep `shadow-md` in popover.tsx returns 0 matches (gdf-glass-overlay provides shadow-lg)
    - All animation and origin classes preserved
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "gdf-glass-overlay" src/components/ui/popover.tsx && grep -c "text-gdf-text-primary" src/components/ui/popover.tsx'</automated>
  </verify>
  <done>PopoverContent uses gdf-glass-overlay with glass bg + blur + border + shadow; text uses gdf-text-primary</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                     |
| -------------------------------- | ------------------------------- |
| N/A — CSS/className changes only | No user data crosses boundaries |

## STRIDE Threat Register

| Threat ID  | Category | Component                                  | Disposition | Mitigation Plan                                           |
| ---------- | -------- | ------------------------------------------ | ----------- | --------------------------------------------------------- |
| T-02-03-01 | N/A      | dropdown-menu.tsx, select.tsx, popover.tsx | accept      | Visual styling changes only; no executable logic modified |

No package install task — no slopcheck gate needed.
</threat_model>

<verification>
Phase verification: `pnpm build` succeeds. In dev server, open a Select dropdown — verify glass translucency visible through to background. Open a DropdownMenu — verify glass overlay with 24px blur. Verify item hover states show gdf-accent-primary blue highlight.
</verification>

<success_criteria>

- [ ] DropdownMenu content renders glass translucent with 24px blur
- [ ] DropdownMenu items show blue accent highlight on hover/focus
- [ ] DropdownMenu separators use glass border color
- [ ] Select trigger renders with glass background and blur
- [ ] Select trigger border shifts to accent-primary on focus/open
- [ ] Select content renders with glass overlay standard
- [ ] Select items show accent highlight on focus
- [ ] Popover content renders with glass overlay standard
- [ ] `pnpm build` passes
- [ ] No animation regressions
      </success_criteria>

<output>
Create `.planning/phases/02-tactical-ui-consolidation/02-03-SUMMARY.md` when done
</output>
