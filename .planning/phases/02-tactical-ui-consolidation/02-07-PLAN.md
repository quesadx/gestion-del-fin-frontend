---
phase: 02-tactical-ui-consolidation
plan: 07
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - src/app/styles/globals.css
  - src/app/styles/tokens.css
  - src/components/cyber/StockBarChart.tsx
  - cspell.json
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - "Dark mode: all overlay components render glass translucent with visible borders"
    - "Light mode: glass borders have sufficient contrast against white backgrounds"
    - "Light mode: text passes WCAG AA 4.5:1 contrast ratio on glass backgrounds"
    - "Legacy `.terminal-*` CSS classes are removed with zero usage"
    - "Legacy `StockBarChart.tsx` in cyber/ is deleted"
    - "cspell.json contains no legacy/terminal-specific words"
    - "All responsive breakpoints render without overflow or layout breakage"
  artifacts:
    - path: "src/app/styles/globals.css"
      provides: "Cleaned CSS without terminal-* legacy classes"
      contains: "gdf-glass-overlay"
      min_lines: 300
    - path: "src/components/cyber/StockBarChart.tsx"
      provides: "DELETED — relocated to tactical/"
      deleted: true
    - path: "cspell.json"
      provides: "Updated spell dictionary without legacy words"
  key_links:
    - from: "src/app/styles/globals.css"
      to: "src/app/styles/tokens.css"
      via: "CSS import"
      pattern: "@import './tokens.css'"
</must_haves>
---

<objective>
Complete the Phase 02 consolidation with dark/light mode audit, responsive refinement, legacy code removal, dead file deletion, and final validation. This is the quality-assurance and cleanup wave that ensures all previous plans' changes work correctly in both themes, at all screen sizes, and without legacy artifacts.

Purpose: Even though D-09 (dark/light mode audit), D-10 (responsive refinement), D-11 (accessibility), D-12 (performance), and D-13 (dead code removal) are the last items in CONTEXT.md, they are critical for the phase to be truly "complete." This plan validates that the glass overlay standard works in light mode (where white glass on white background needs higher border alpha), removes legacy terminal-\* CSS classes, deletes the now-relocated StockBarChart from cyber/, cleans cspell.json, and runs a final pnpm check.

Output: globals.css cleaned (terminal-\* classes removed), tokens.css light mode verified, cyber/StockBarChart.tsx deleted, cspell.json cleaned, final build+lint+spell check passes.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md
@src/app/styles/globals.css
@src/app/styles/tokens.css
@cspell.json
@AGENTS.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark/light mode audit — verify contrast, fix light mode glass borders</name>
  <files>src/app/styles/tokens.css, src/app/styles/globals.css</files>
  <read_first>
    - src/app/styles/tokens.css (full file, 222 lines)
    - src/app/styles/globals.css (full file, 460+ lines after previous plans)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 109-116 for D-09 dark/light mode audit spec)
  </read_first>
  <action>
    Per D-09: Audit and fix light mode for glass readability, border contrast, focus ring visibility, and text contrast.

    **Step 1 — Verify light mode glass border alpha** (already done in 02-01 Task 3):
    Check that `:root[data-theme='light']` block in tokens.css has:
    ```
    --gdf-glass-border: rgba(59, 130, 246, 0.15);
    --gdf-glass-border-hover: rgba(59, 130, 246, 0.25);
    ```
    If not present, add them now.

    **Step 2 — Fix light mode glass overlay shadow**:
    The `.gdf-glass-overlay` utility uses `box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3)` which is too dark for light mode. Add a light mode override in globals.css:
    ```css
    :root[data-theme='light'] .gdf-glass-overlay {
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
    }
    ```
    Insert after `.gdf-glass-overlay` definition in the `@layer components` block.

    **Step 3 — Verify text contrast ratios** (D-09: WCAG AA minimum 4.5:1):
    - Check `--gdf-text-muted` in light mode: `#94a3b8` on white background → ratio ~3.0:1 (FAILS AA).
    - Fix: Change light mode `--gdf-text-muted` from `#94a3b8` to `#64748b` (which gives ~4.6:1 on white).
    - In tokens.css line 203, change: `--gdf-text-muted: #94a3b8;` to `--gdf-text-muted: #64748b;`

    - Check `--gdf-text-secondary` in light mode: `#475569` on white → ~5.5:1 (PASSES AA). OK.
    - Check `--gdf-text-primary` in light mode: `#0f172a` on white → ~15:1 (PASSES AAA). OK.

    **Step 4 — Verify focus ring visibility in light mode**:
    The focus ring uses `--gdf-accent-primary` (#3b82f6) which is consistent across dark/light. `ring-offset-gdf-surface-root` uses `--gdf-surface-root` which is `#f0f4f8` in light mode — this provides proper contrast. Verify no components use `ring-offset-background` (which would be `#f0f4f8` in light mode — also fine).
    Run: `grep -rn "ring-offset-background" src/components/ui/ --include="*.tsx"` — should be 0 after previous plans.
    If any remain, fix them to `ring-offset-gdf-surface-root`.

    **Step 5 — Verify glass readability in light mode**:
    The glass tokens in light mode (`--gdf-glass-bg: rgba(255, 255, 255, 0.65)`) are sufficiently opaque to read text. `--gdf-glass-bg-heavy: rgba(255, 255, 255, 0.85)` for overlays. These should be fine. No changes needed.

  </action>
  <acceptance_criteria>
    - tokens.css light mode `--gdf-text-muted` is `#64748b` (not `#94a3b8`)
    - globals.css has light mode `.gdf-glass-overlay` box-shadow override at `rgba(0,0,0,0.08)`
    - `grep "ring-offset-background" src/components/ui/*.tsx` returns 0 matches
    - `grep "94a3b8" src/app/styles/tokens.css` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep "#64748b" src/app/styles/tokens.css && echo "text-muted fixed" && grep "rgba(0, 0, 0, 0.08)" src/app/styles/globals.css && echo "light shadow ok" && ! grep "ring-offset-background" src/components/ui/*.tsx 2>/dev/null && echo "no legacy ring-offset"'</automated>
  </verify>
  <done>Light mode text-muted passes WCAG AA (4.6:1); glass overlay shadow toned down for light mode; no legacy ring-offset-background; glass readability verified</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy CSS, delete dead files, clean cspell, responsive audit</name>
  <files>src/app/styles/globals.css, src/components/cyber/StockBarChart.tsx, cspell.json</files>
  <read_first>
    - src/app/styles/globals.css (lines 151-202 for terminal-* classes)
    - cspell.json (full file for legacy words)
    - .planning/phases/02-tactical-ui-consolidation/02-CONTEXT.md (lines 139-143 for D-13 dead code removal, lines 118-123 for D-10 responsive refinement)
  </read_first>
  <action>
    **Step 1 — Remove `.terminal-*` CSS classes** from `src/app/styles/globals.css`:
    Per D-13: "`.terminal-table`, `.terminal-input`, `.terminal-divider` CSS classes in globals.css → verify zero usage, then remove."

    Verify zero usage first:
    ```
    grep -rn "terminal-table\|terminal-input\|terminal-divider" src/ --include="*.tsx" --include="*.ts" 2>/dev/null
    ```
    Should return 0 matches (only in globals.css itself).

    Then, delete lines 151-202 from globals.css (the entire `.terminal-table`, `.terminal-table th`, `.terminal-table td`, `.terminal-table tr:hover td`, `.terminal-input`, `.terminal-input::placeholder`, `.terminal-input:focus`, `.terminal-divider` blocks).

    **Step 2 — Delete `src/components/cyber/StockBarChart.tsx`**:
    After Plan 02-06 relocated it to `src/components/tactical/StockBarChart.tsx`, delete the old file:
    ```
    rm src/components/cyber/StockBarChart.tsx
    ```
    This removes the last non-StatusBadge component from cyber/.

    **Step 3 — Clean `cspell.json`**:
    Per D-13: "cspell.json — remove any remaining legacy words (scanline references already removed in Phase 01)."
    Check for legacy words: `grep -i "scanline\|brutalist\|neon\|cyber\|terminal\|glitch\|zinc" cspell.json`
    If any legacy-specific words exist (like "scanlines"), remove them.

    Add any new Phase 02 tokens that may trip spell check: `gdf`, `gdf`, `backdrop`, `webkit`, `cmdk`. Verify these are already in the dictionary. If not, add them:
    ```
    "gdf", "cmdk", "partialize", "zustand"
    ```
    (Already likely there from Phase 01).

    **Step 4 — Responsive audit** (D-10):
    - Verify Dialog has `sm:max-w-md` (added in Plan 02-02) — check: `grep "sm:max-w-md" src/components/ui/dialog.tsx`
    - Verify GlassPanel doesn't overflow on tablet (768-1024px). Check existing styles in GlassPanel.tsx — it uses responsive padding already.
    - Verify AppShell Sheet (mobile sidebar) has glass header from Plan 02-02.
    - No specific code changes needed unless issues found.

    **Step 5 — Performance note** (D-12):
    Document in SUMMARY (not in code): maximum 3 simultaneously visible blurred glass layers. This is already naturally enforced by AnimatePresence — only one tooltip/popover/dropdown renders at a time. No code change needed.

  </action>
  <acceptance_criteria>
    - grep `terminal-table` in globals.css returns 0 matches
    - grep `terminal-input` in globals.css returns 0 matches
    - grep `terminal-divider` in globals.css returns 0 matches
    - `src/components/cyber/StockBarChart.tsx` does NOT exist
    - cspell.json has no legacy-only words (verify by running `pnpm spell`)
    - Dialog has `sm:max-w-md` class
    - `pnpm build` passes
    - `pnpm spell` passes
  </acceptance_criteria>
  <verify>
    <automated>bash -c '! grep "terminal-table\|terminal-input\|terminal-divider" src/app/styles/globals.css && echo "legacy CSS removed" && ! test -f src/components/cyber/StockBarChart.tsx && echo "old StockBarChart deleted" && pnpm spell 2>&1 | tail -3 && cd . && pnpm build 2>&1 | tail -3'</automated>
  </verify>
  <done>Terminal-* legacy CSS removed; cyber/StockBarChart.tsx deleted; cspell clean; responsive audit verified; pnpm spell + build pass</done>
</task>

<task type="auto">
  <name>Task 3: Final validation — pnpm check + per-component glass smoke test</name>
  <files>
    (no file modifications — validation only)
  </files>
  <read_first>
    - All shadcn/ui component files that were modified (verify correct gdf token usage)
    - AGENTS.md (for pnpm check command)
  </read_first>
  <action>
    Run the full validation pipeline:

    **Step 1 — `pnpm check`** (runs lint + spell + build):
    ```
    pnpm check
    ```
    This must exit 0. Fix any issues before proceeding.

    **Step 2 — Run `pnpm dev`** (background) and perform a smoke test checklist in the browser (document in SUMMARY):
    - Visit any page with a Dialog (e.g., Camps → Create Camp) → verify glass translucent overlay
    - Open a Select dropdown (camp selector in sidebar) → verify glass trigger and glass content
    - Check Dialog overlay transparency — should be 60% surface-root with subtle blur
    - Toggle to light mode — verify glass borders visible, text readable
    - Toggle to light mode — verify focus rings visible on input fields
    - Verify no regressions in existing pages (Dashboard, Inventory, etc.)

    **Step 3 — Verify Phase 02 requirements**:
    - RNF-03: Verify gdf-glass-* utility classes exist, verify interactive elements have hover/focus transitions
    - RNF-04: Verify `pnpm build` output size has not regressed significantly, verify lazy loading still works

    **Step 4 — Check for any remaining hardcoded colors**:
    Run a broad scan for common legacy color classes that should have been replaced:
    ```
    grep -rn "bg-black/\|bg-zinc-\|text-zinc-\|border-zinc-\|bg-brand-primary\|bg-brand-secondary" src/components/ui/ --include="*.tsx"
    ```
    Any matches should be in files not covered by Phase 02 (like button.tsx which uses its own variant system). Verify each match is intentional.

    **Step 5 — Document the completed phase** in SUMMARY.md.

  </action>
  <acceptance_criteria>
    - `pnpm check` exits 0 (lint + spell + build all pass)
    - Zero unintended hardcoded color classes in src/components/ui/
    - Phase 02 ROADMAP requirements (RNF-03, RNF-04) verifiably met
    - SUMMARY.md documents the completed phase state
  </acceptance_criteria>
  <verify>
    <automated>pnpm check 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm check passes; zero regressions; all 46 shadcn/ui components gdf-compatible; dark/light mode verified; RNF-03 and RNF-04 satisfied; SUMMARY.md created</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                     |
| --------------------------------- | ------------------------------- |
| N/A — cleanup and validation only | No user data crosses boundaries |

## STRIDE Threat Register

| Threat ID  | Category | Component                            | Disposition | Mitigation Plan                             |
| ---------- | -------- | ------------------------------------ | ----------- | ------------------------------------------- |
| T-02-07-01 | N/A      | globals.css, tokens.css, cspell.json | accept      | Cleanup tasks; no executable logic modified |

No package install task — no slopcheck gate needed.
</threat_model>

<verification>
Full phase verification: `pnpm check` passes. Dev server smoke test shows all glass overlay components rendering correctly in dark and light modes. No legacy terminal-* classes remain. StockBarChart lives in tactical/ directory. Emotional UI glitch visible. Background particles drift. Scanner sweep responsive to emotional state.
</verification>

<success_criteria>

- [ ] `.terminal-table`, `.terminal-input`, `.terminal-divider` CSS classes removed from globals.css
- [ ] `src/components/cyber/StockBarChart.tsx` deleted (relocated to tactical/)
- [ ] Light mode `--gdf-text-muted` passes WCAG AA contrast (4.6:1 at #64748b)
- [ ] Light mode glass overlay shadow toned down
- [ ] Zero `ring-offset-background` in shadcn/ui components
- [ ] `pnpm check` passes (lint + spell + build)
- [ ] cspell.json has no legacy words
- [ ] Dialog has responsive `sm:max-w-md`
- [ ] No regressions in dark mode glass appearance
- [ ] Light mode renders readable glass panels with visible borders
      </success_criteria>

<output>
Create `.planning/phases/02-tactical-ui-consolidation/02-07-SUMMARY.md` when done
</output>
