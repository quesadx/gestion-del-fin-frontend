---

phase: 04
plan: 02
type: execute
wave: 2
depends_on:

- 04-01
  files_modified:
  - src/components/tactical/HolographicRadar.tsx
  - src/components/tactical/TacticalBackground.tsx
    autonomous: true
    requirements:
- RNF-03
- RNF-04
  must_haves:
  truths: - "A subtle circular holographic radar is visible in the background, concentric rings centered on viewport" - "Radar uses pure CSS animations — no JavaScript animation loop" - "Radar respects prefers-reduced-motion" - "Radar does not interfere with cursor events or content readability" - "Radar is layered correctly between grid background and content" - "Radar sweep line rotates continuously at 12s per revolution"
  artifacts: - path: "src/components/tactical/HolographicRadar.tsx"
  provides: "Circular radar background layer with concentric rings, sweep arc, and scattered dots"
  contains: "export function HolographicRadar" - path: "src/components/tactical/TacticalBackground.tsx"
  provides: "Extended background with HolographicRadar integration"
  contains: "HolographicRadar"
  key_links: - from: "TacticalBackground.tsx"
  to: "HolographicRadar.tsx"
  via: "import + JSX render"
  pattern: "import.\*HolographicRadar"
  </objective>

<context>
@src/components/tactical/TacticalBackground.tsx
@src/layouts/AppShell.tsx
@src/app/styles/globals.css
@tailwind.config.js
@.planning/phases/04-undefined/04-RESEARCH.md

<interfaces>
From TacticalBackground.tsx — the wrapper div pattern:
```tsx
<div className="pointer-events-none fixed inset-0 z-[-10] overflow-hidden" aria-hidden="true">
  {/* child layers */}
</div>
```
The radar should be a child of this div, inheriting its pointer-events-none, fixed, inset-0, and aria-hidden.

From RESEARCH.md — radar implementation pattern (lines 146-175):

```tsx
// Concentric rings at w-[40vw], w-[60vw], w-[85vw] — opacity 0.03
// Rotating sweep arc using conic-gradient, animation: radar-sweep-rotate 12s linear infinite
// Scattered dots: 8-12 static, 2-3 blinking — at grid/ring intersections
```

Key performance note: NO will-change on radar. CSS transform: rotate() already triggers GPU compositing.

From globals.css — existing grid mask pattern (line 554):

```css
mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
```

Same mask pattern should be applied to radar rings container for edge fade.

From AppShell.tsx — where TacticalBackground is used (lines 211-212):

```tsx
<div className="gdf-critical-overlay" />
<main className="flex-1 overflow-y-auto p-4 md:p-6 lg:p-8">
```

The HolographicRadar should render inside TacticalBackground, NOT in AppShell directly. AppShell doesn't need changes if radar is a child of TacticalBackground.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HolographicRadar component</name>
  <files>src/components/tactical/HolographicRadar.tsx</files>
  <read_first>
- src/components/tactical/TacticalBackground.tsx (all 88 lines — establishes the fixed z-index layer pattern with pointer-events-none, aria-hidden, uses JSX divs with Tailwind classes and inline styles for particles)
- tailwind.config.js (post-04-01 state — confirm radar-sweep-rotate keyframe/animation exist)
- src/app/styles/globals.css (post-04-01 state — confirm .radar-sweep class exists for sweep arc styling)
- .planning/phases/04-undefined/04-RESEARCH.md (lines 138-174 — Pure-CSS Circular Holographic Radar pattern)
  </read_first>
  <action>
Create a new file at src/components/tactical/HolographicRadar.tsx. This is a zero-prop component that renders a subtle circular holographic radar layer using only CSS.

Export: `export function HolographicRadar()` — named export per AGENTS.md convention.

Structure:

1. Outer wrapper: `<div className="pointer-events-none absolute inset-0" aria-hidden="true">` — inherits fixed positioning and z-index from TacticalBackground parent.

2. Inner container for all radar elements: `<div className="absolute inset-0 flex items-center justify-center">`
   - Apply the same mask pattern as gdf-bg-grid: `mask-image: radial-gradient(ellipse at center, black 40%, transparent 80%)` via style prop. This fades the rings at viewport edges.

3. Three concentric rings (opacity 0.03, border 1px, rounded-full):
   - Ring 1: width/height 40vw, border-color var(--gdf-accent-secondary)
   - Ring 2: width/height 60vw, border-color var(--gdf-accent-secondary)
   - Ring 3: width/height 85vw, border-color var(--gdf-accent-secondary)

4. Rotating sweep arc:
   - Wrapper div with className="absolute inset-0 flex items-center justify-center" and style: `{ animation: 'radar-sweep-rotate 12s linear infinite' }`
   - Inner div: width/height 40vw, rounded-full, with style: `{ background: 'conic-gradient(from 0deg, transparent 0deg, rgba(6,182,212,0.06) 60deg, transparent 90deg, transparent 360deg)' }`
   - The opacity should read from CSS variable: style `{ opacity: 'var(--gdf-radar-opacity, 0.03)' }`

5. Eight static scattered tactical dots:
   - Positioned at calculated XY pairs where grid lines and ring intersections would occur visually: (20%,30%), (70%,20%), (50%,50%), (30%,70%), (75%,45%), (25%,55%), (60%,75%), (40%,40%)
   - Each dot: absolute positioned, 2px × 2px rounded-full, bg-gdf-accent-secondary at opacity 0.15
   - Font size: 2px to make them tiny dots

6. Two blinking dots:
   - Positioned at (45%,60%) and (65%,35%)
   - Use className="animate-blink" (already defined in tailwind keyframes)
   - Same styling as static dots plus animation-duration of 3s via style prop

7. The sweep arc wrapper must be wrapped in:

   ```
   @media (prefers-reduced-motion: no-preference) { ... }
   ```

   Achieve this via a CSS custom property gate: use style `{ animation: 'var(--gdf-reduced-motion, radar-sweep-rotate 12s linear infinite)' }`. When --gdf-reduced-motion is '1' (set by TacticalBackground.tsx line 39), the animation string becomes invalid CSS and pauses. This approach avoids needing a JS media query.

Constraints:

- ZERO JavaScript animation (no requestAnimationFrame, no setInterval, no useEffect for radar — CSS only).
- NO will-change property anywhere (per RESEARCH.md Pitfall 2).
- NO backdrop-filter on radar elements (per performance budget — radar uses transform/opacity only).
- ALL colors use CSS variables (var(--gdf-accent-secondary)) or Tailwind gdf-\* classes — never hardcoded hex/rgba.
- The component is self-contained — it renders a visual layer, exports nothing else.
- Use aria-hidden="true" on all radar divs (purely decorative).
  </action>
  <verify>
  <automated>grep -c 'export function HolographicRadar' src/components/tactical/HolographicRadar.tsx && echo "component exported"</automated>
  <automated>grep -c 'radar-sweep-rotate' src/components/tactical/HolographicRadar.tsx && echo "uses radar-sweep-rotate animation"</automated>
  <automated>grep -c 'conic-gradient' src/components/tactical/HolographicRadar.tsx && echo "uses conic-gradient for sweep"</automated>
  <automated>grep -c 'will-change' src/components/tactical/HolographicRadar.tsx | tail -1 | xargs -I{} sh -c 'test {} -eq 0' && echo "no will-change (correct)"</automated>
  <automated>grep -c 'requestAnimationFrame\|setInterval\|useEffect' src/components/tactical/HolographicRadar.tsx | tail -1 | xargs -I{} sh -c 'test {} -eq 0' && echo "no JS animation loop (correct)"</automated>
  </verify>
  <acceptance*criteria> - New file at src/components/tactical/HolographicRadar.tsx exists with named export `HolographicRadar` - Component has 3 concentric ring divs at 40vw, 60vw, 85vw with border 1px and opacity 0.03 - Component has 1 rotating sweep arc using conic-gradient and radar-sweep-rotate animation - Component has 10 tactical dots (8 static, 2 with animate-blink) - Outer container uses mask-image: radial-gradient for edge fade - All divs have aria-hidden="true" - No JS animation code (zero requestAnimationFrame/setInterval/useEffect) - No will-change CSS property - No hardcoded hex/rgba colors — all use var(--gdf-*) or Tailwind gdf-\_ tokens - Reduced motion respected via --gdf-reduced-motion variable gate on sweep animation
  </acceptance_criteria>
  <done>HolographicRadar component created with 3 concentric rings, 1 rotating sweep arc, 10 tactical dots, CSS-only animations, no JS loops, and reduced-motion support.</done>
  </task>

<task type="auto">
  <name>Task 2: Integrate HolographicRadar into TacticalBackground</name>
  <files>src/components/tactical/TacticalBackground.tsx</files>
  <read_first>
- src/components/tactical/TacticalBackground.tsx (all 88 lines — current structure: grid div, 3 nebula blobs, scanner line, 60 particles in a single wrapper div)
- src/components/tactical/HolographicRadar.tsx (post-Task-1 state — new component to import)
  </read_first>
  <action>
Extend TacticalBackground.tsx to render the HolographicRadar component as a child layer.

1. Add import at top (after last import, before createParticles):
   `import { HolographicRadar } from './HolographicRadar';`

2. Insert `<HolographicRadar />` inside the main wrapper div (className="pointer-events-none fixed inset-0 z-[-10] overflow-hidden"):
   - Place it AFTER the grid div (`<div className="gdf-bg-grid animate-grid-drift" />`) but BEFORE the nebula blobs.
   - This places radar at the same z-[-10] layer, rendered below nebula blobs (which have position: absolute and higher opacity/blur, effectively on top).

3. The final insertion point (between grid and first nebula):
   ```tsx
   <div className="gdf-bg-grid animate-grid-drift" />
   <HolographicRadar />
   <div className="absolute -top-[20%] -left-[10%] ...">
   ```

Constraints:

- Do NOT modify the cursor mousemove handler (handleMove, useEffect).
- Do NOT modify particle generation (createParticles).
- Do NOT modify scanner sweep or nebula blob position/size/animation.
- Do NOT change the wrapper div's z-index or className.
- The HolographicRadar component is self-contained (position: absolute, inset: 0) and will correctly fill the fixed wrapper div.
  </action>
  <verify>
  <automated>grep -c 'HolographicRadar' src/components/tactical/TacticalBackground.tsx && echo "HolographicRadar imported and rendered"</automated>
  <automated>grep -c 'import.\*HolographicRadar' src/components/tactical/TacticalBackground.tsx | tail -1 | xargs -I{} sh -c 'test {} -ge 1' && echo "import statement present"</automated>
  <automated>grep -c '<HolographicRadar' src/components/tactical/TacticalBackground.tsx | tail -1 | xargs -I{} sh -c 'test {} -ge 1' && echo "JSX render present"</automated>
  </verify>
  <acceptance_criteria> - TacticalBackground.tsx imports HolographicRadar from './HolographicRadar' - TacticalBackground.tsx renders <HolographicRadar /> between the grid div and first nebula blob div - Cursor mousemove handler still operates correctly (no code removed from handleMove/useEffect) - Scanner sweep, nebula blobs, and 60 particles unchanged - Build compiles without errors (TypeScript strict, named exports)
  </acceptance_criteria>
  <done>HolographicRadar renders inside TacticalBackground wrapper, between grid layer and nebula blobs, inheriting z-[-10] fixed positioning.</done>
  </task>

</tasks>

<verification>
pnpm check passes (covers TypeScript strict + lint + build). Visual verification: pnpm dev → radar concentric rings, rotating sweep arc, and tactical dots visible as subtle background layer behind nebula blobs and content.
</verification>

<success_criteria>

- HolographicRadar.tsx exists with named export, 3 concentric rings, rotating sweep arc, 10 tactical dots
- TacticalBackground.tsx imports and renders HolographicRadar
- Zero JavaScript animation loops (pure CSS keyframes only)
- No will-change usage
- All colors consuming var(--gdf-\*) tokens
- pnpm check exits 0
  </success_criteria>

<output>
Create `.planning/phases/04-undefined/04-02-SUMMARY.md` when done
</output>
