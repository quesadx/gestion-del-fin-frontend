---

phase: 01-ui-redesign
plan: 07
type: execute
wave: 5
depends_on: [01-06]
files_modified:

- src/shared/lib/toast.tsx
- src/app/styles/globals.css
- tailwind.config.js
- cspell.json
  autonomous: true
  requirements: [RNF-03]

must_haves:
truths: - "Toast notifications display with glass styling and gdf-\* status colors" - "8 dead cyber/ components are deleted" - "Legacy navigation/ directory is deleted" - "temp/ directory is deleted" - "Old CSS utility classes are removed from globals.css" - "Old Tailwind backward-compat color shims are removed from config" - "pnpm check passes with zero errors"
artifacts: - path: "src/shared/lib/toast.tsx"
provides: "Glass-styled toast notification system"
contains: "gdf-glass-bg" - path: "src/components/cyber/"
provides: "DELETED — only StatusBadge.tsx and StockBarChart.tsx remain"
key_links: - from: "globals.css cleanup"
to: "tailwind.config.js cleanup"
via: "paired removal of old utility classes and their Tailwind definitions"
pattern: "simultaneous removal"
</objective>

<objective>
Final cleanup phase: delete all dead code (8 unused cyber/ components, legacy navigation/ directory, temp/ directory), restyle the toast notification system to use gdf-* glass tokens, remove old CSS utility classes and backward-compat Tailwind color shims (now that all pages are migrated), and run final validation. This phase transitions from "dual design system" to "single design system" — the old brutalist/cyberpunk layer is permanently removed.

Purpose: Clean codebase, remove all traces of old design system, ensure no dead imports break the build.
Output: Deleted 8 dead cyber/ components, deleted navigation/ + temp/, restyled toast.tsx, cleaned globals.css + tailwind.config.js, final pnpm check
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md §1.1 (8 dead cyber/ components: WaveBackground, CyberGrid, RingMeter, StatCard, DataChart, SkeletonTable, SkeletonCard, FileInput), §7.4 (kill criteria for old components), §9.1 (legacy navigation deletion), §9.7 (temp/ directory deletion)
@.planning/codebase/CONCERNS.md (legacy navigation components, temp/ directory)
@.planning/codebase/STRUCTURE.md (legacy navigation directory location: src/components/navigation/)
@AGENTS.md

<interfaces>
<!-- 8 dead cyber/ components to DELETE (zero runtime imports confirmed in RESEARCH.md §1.1) -->
Files: src/components/cyber/WaveBackground.tsx, src/components/cyber/CyberGrid.tsx, src/components/cyber/RingMeter.tsx, src/components/cyber/StatCard.tsx, src/components/cyber/DataChart.tsx, src/components/cyber/SkeletonTable.tsx, src/components/cyber/SkeletonCard.tsx, src/components/cyber/FileInput.tsx

<!-- 6 ACTIVE cyber/ components to KEEP (still imported) -->

Files: StatusBadge.tsx (restyled in-place), StockBarChart.tsx (recolored in plan 01-06), ScreenLoader.tsx (still imported by DashboardPage? No — migrated to HoloLoader). Actually let me recheck...

After plan 01-05 and 01-06, the remaining cyber/ imports should be:

- StatusBadge.tsx: imported by feature pages (stays — restyled in-place)
- StockBarChart.tsx: imported by InventoryPage (stays — recolored in plan 01-06)
- Panel.tsx: should have 0 imports (all migrated to GlassPanel)
- GlitchButton.tsx: should have 0 imports (all migrated to TacticalButton)
- ScreenLoader.tsx: should have 0 imports (all migrated to HoloLoader)
- TerminalLine.tsx: should have 0 imports (removed from DashboardPage)

Verify at runtime: `grep -r "from '@/components/cyber/" src/ | grep -v "StatusBadge\|StockBarChart"` should return 0 lines.

If any residual import of Panel/GlitchButton/ScreenLoader/TerminalLine remains, remove it.

<!-- Legacy navigation/ to DELETE -->

Directory: src/components/navigation/ — Sidebar.tsx, Navbar.tsx, DockBar.tsx (all superseded by AppShell)

<!-- temp/ directory to DELETE -->

Directory: temp/ — neon-nova-dashboard scaffolding (~2,100 lines of duplicate shadcn/ui components)

<!-- Toast system current state (93 lines, uses --neon-fuchsia/cyan/violet CSS vars and oklch() backgrounds) -->

From src/shared/lib/toast.tsx:

```typescript
typeStyles = {
  error: {
    border: 'border-[var(--neon-fuchsia)]/30',
    bg: 'bg-[oklch(0.12_0.05_340_/_0.95)]',
    text: 'text-[var(--neon-fuchsia)]',
  },
  success: {
    border: 'border-[var(--neon-cyan)]/30',
    bg: 'bg-[oklch(0.12_0.05_210_/_0.95)]',
    text: 'text-[var(--neon-cyan)]',
  },
  info: {
    border: 'border-[var(--neon-violet)]/30',
    bg: 'bg-[oklch(0.12_0.05_280_/_0.95)]',
    text: 'text-[var(--neon-violet)]',
  },
};
```

<!-- Old CSS classes to remove from globals.css (now that all pages are migrated):
  .brutalist-border → KEEP for LockScreen until LockScreen is migrated
  .neon-glow-red → DELETE (never used)
  .glass, .glass-heavy, .glass-light, .glass-interactive → REWRITTEN to use gdf-* tokens in plan 01-01 (already done)
  .terminal-table → KEEP (still used by feature pages)
  .terminal-input → KEEP (still used by forms)
  .terminal-divider → KEEP (still used by feature pages)
  .font-tech → DELETE (never used, dead RingMeter)
  .text-glow-fuchsia, .text-glow-cyan → REWRITTEN in plan 01-01
  .text-glow-yellow → REWRITTEN in plan 01-01
  .text-glow → REWRITTEN in plan 01-01

  WAIT — .glass/.glass-heavy were already rewritten to use gdf-* tokens in plan 01-01 Task 3. They're kept because unmigrated pages still use them (e.g., LockScreen, DashboardPage was still using them until plan 01-06). Now that all pages are migrated, these can be simplified but should stay as compatibility aliases.

  Actually, the old .glass classes are now running through gdf-* tokens (rewritten in plan 01-01). They should stay in globals.css as utility classes. Only .font-tech and .neon-glow-red need to be removed (already done in plan 01-01).

  Let me reconsider: the Task 3 in this plan should verify that the cleanups from plan 01-01 are complete, and remove any remaining dead code. The backwards-compat shims in tailwind.config.js (surface.*, accent.*, brand.*, status.*) should be REMOVED now since all pages use gdf.* classes.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code — 8 unused cyber/ components, legacy navigation/, temp/</name>
  <files>(deletions only — no new files)</files>
  <read_first>
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §1.1 (lines 74-91 — catalog of 14 cyber/ components with import counts; the 8 dead ones have 0 imports)
    - .planning/codebase/STRUCTURE.md (navigation/ directory location, temp/ directory location)
    - .planning/codebase/CONCERNS.md §9.1 (legacy navigation), §9.7 (temp/ directory)
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §7.4 (kill criteria: zero imports, pnpm check passes)
  </read_first>
  <action>
    **Step 1: Verify zero imports before deletion**

    Run verification for each of the 8 dead components:
    ```bash
    grep -r "WaveBackground\|CyberGrid\|RingMeter\|StatCard\|DataChart\|SkeletonTable\|SkeletonCard\|FileInput" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v "\.planning"
    ```

    For navigation/:
    ```bash
    grep -r "from.*navigation/" src/ --include="*.tsx" --include="*.ts"
    ```

    For temp/:
    ```bash
    grep -r "from.*temp/" src/ --include="*.tsx" --include="*.ts"
    ```

    All three should return zero results before proceeding.

    **Step 2: Delete the 8 dead cyber/ components**
    ```bash
    rm src/components/cyber/WaveBackground.tsx
    rm src/components/cyber/CyberGrid.tsx
    rm src/components/cyber/RingMeter.tsx
    rm src/components/cyber/StatCard.tsx
    rm src/components/cyber/DataChart.tsx
    rm src/components/cyber/SkeletonTable.tsx
    rm src/components/cyber/SkeletonCard.tsx
    rm src/components/cyber/FileInput.tsx
    ```

    **Step 3: Delete 4 MORE cyber/ components that are now fully migrated**
    After plan 01-05 and 01-06, Panel.tsx, GlitchButton.tsx, ScreenLoader.tsx, and TerminalLine.tsx should also have zero imports. Verify:
    ```bash
    grep -r "from '@/components/cyber/Panel'" src/
    grep -r "from '@/components/cyber/GlitchButton'" src/
    grep -r "from '@/components/cyber/ScreenLoader'" src/
    grep -r "from '@/components/cyber/TerminalLine'" src/
    ```

    If ALL return zero results, delete:
    ```bash
    rm src/components/cyber/Panel.tsx
    rm src/components/cyber/GlitchButton.tsx
    rm src/components/cyber/ScreenLoader.tsx
    rm src/components/cyber/TerminalLine.tsx
    ```

    **After deletion, only 2 files should remain in src/components/cyber/:**
    - StatusBadge.tsx (restyled in-place, actively imported by feature pages)
    - StockBarChart.tsx (recolored, actively imported by InventoryPage)

    **Step 4: Delete legacy navigation/ directory**
    ```bash
    rm -rf src/components/navigation/
    ```

    **Step 5: Delete temp/ directory**
    ```bash
    rm -rf temp/
    ```

    **Step 6: Update cspell.json**
    Remove any custom words that only appeared in deleted files: WaveBackground, CyberGrid, RingMeter, StatCard, DataChart, SkeletonTable, SkeletonCard, FileInput, Navbar, DockBar. Check if these are still needed by other files before removing. Also remove "scanlines", "CRT" references if present (no longer relevant).

    **Step 7: Verify build passes**
    ```bash
    pnpm run build
    ```

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    **Before deletion:**
    - `grep -r "WaveBackground\|CyberGrid\|RingMeter\|StatCard\|DataChart\|SkeletonTable\|SkeletonCard\|FileInput" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v .planning` returns 0 lines
    - `grep -r "from.*navigation/" src/ --include="*.tsx" --include="*.ts"` returns 0 lines
    - `grep -r "from.*temp/" src/ --include="*.tsx" --include="*.ts"` returns 0 lines

    **After deletion:**
    - `src/components/cyber/WaveBackground.tsx` does not exist
    - `src/components/cyber/CyberGrid.tsx` does not exist
    - `src/components/cyber/RingMeter.tsx` does not exist
    - `src/components/cyber/StatCard.tsx` does not exist
    - `src/components/cyber/DataChart.tsx` does not exist
    - `src/components/cyber/SkeletonTable.tsx` does not exist
    - `src/components/cyber/SkeletonCard.tsx` does not exist
    - `src/components/cyber/FileInput.tsx` does not exist
    - `src/components/cyber/Panel.tsx` does not exist (if 0 imports verified)
    - `src/components/cyber/GlitchButton.tsx` does not exist (if 0 imports verified)
    - `src/components/cyber/ScreenLoader.tsx` does not exist (if 0 imports verified)
    - `src/components/cyber/TerminalLine.tsx` does not exist (if 0 imports verified)
    - `src/components/navigation/` directory does not exist
    - `temp/` directory does not exist
    - `pnpm run build` exits 0

</acceptance_criteria>
<done>All dead cyber/ components, legacy navigation/, and temp/ deleted; only StatusBadge + StockBarChart remain in cyber/</done>
</task>

<task type="auto">
  <name>Task 2: Restyle toast system and remove old CSS/Tailwind backward-compat shims</name>
  <files>src/shared/lib/toast.tsx, src/app/styles/globals.css, tailwind.config.js</files>
  <read_first>
    - src/shared/lib/toast.tsx (full 93 lines — typeStyles map, ToastContainer component)
    - src/app/styles/globals.css (full file — verify which old utility classes can be removed)
    - tailwind.config.js (backward-compat shims to remove: the surface/accent/text/border/brand/status color aliases)
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §1.2 (CSS variable audit — which vars are dead)
  </read_first>
  <action>
    **PART A: Restyle toast.tsx**

    Replace the `typeStyles` map with gdf-* glass styling:

    ```typescript
    const typeStyles: Record<ToastType, { border: string; bg: string; text: string; icon: typeof AlertTriangle }> = {
      error: {
        border: 'border-gdf-status-danger/30',
        bg: 'bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy',
        text: 'text-gdf-status-danger',
        icon: AlertTriangle,
      },
      success: {
        border: 'border-gdf-accent-secondary/30',
        bg: 'bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy',
        text: 'text-gdf-accent-secondary',
        icon: CheckCircle,
      },
      info: {
        border: 'border-gdf-accent-primary/30',
        bg: 'bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy',
        text: 'text-gdf-accent-primary',
        icon: Info,
      },
    };
    ```

    Remove all `oklch()` references. Remove all `var(--neon-*)` references.

    Update the toast container classes:
    - `backdrop-blur-md` → `backdrop-blur-glass-heavy` (consistent with design system)
    - `rounded-sm` → `rounded-md` (gdf-radius-md)
    - `shadow-lg` → keep (provides depth)
    - `text-foreground` → `text-gdf-text-primary`
    - `text-muted-foreground` → `text-gdf-text-muted`
    - `hover:text-foreground` → `hover:text-gdf-text-primary`

    **PART B: Clean globals.css**

    Due to the sequential nature of plan execution, some cleanups from plan 01-01 may have been partial. Verify and complete:

    1. Remove `.font-tech` from @layer components (line 219-225) — if still present. It was marked for removal in plan 01-01.
    2. Remove `.neon-glow-red` from @layer components (line 127-129) — if still present.
    3. Remove `.glass-light` from @layer components (line 139-142) — if still present.
    4. Verify the `:root` block no longer has `--neon-fuchsia`, `--neon-cyan`, `--neon-violet`, `--neon-yellow`, `--deep-magenta`, `--charcoal` — should have been removed in plan 01-01.
    5. Remove `--accent-primary-glow: none` and `--accent-secondary-glow: none` from :root — these are now defined in tokens.css with real values.
    6. Remove `--glass-blur: none`, `--glass-blur-heavy: none`, `--glass-blur-light: none` — these are now defined in tokens.css.
    7. Remove `--glass-shadow: none`, `--glass-shadow-heavy: none`, `--glass-shadow-light: none` — never used.

    If any of these were already removed in plan 01-01, skip. The cleanup here is a safety net.

    **PART C: Clean tailwind.config.js backward-compat shims**

    Now that ALL pages are migrated to use `gdf.*` classes (via plan 01-05 and 01-06 mechanical swaps + plan 01-06 manual redesigns), the backward-compat color shims can be removed:

    Remove these color namespaces from `theme.extend.colors`:
    - `surface` (base/deep/raised/overlay/glass) — all pages now use `gdf.surface.*` or GlassPanel component
    - `accent` (primary/primary-dim/secondary/success) — all pages use `gdf.accent.*`
    - `text` (primary/secondary/muted) — all pages use `gdf.text.*`
    - `border` (subtle/DEFAULT/hover/active) — all pages use `gdf.border.*`
    - `brand` (primary/secondary/accent) — all pages use `gdf.accent.*` or `gdf.status.*`
    - `status` (green/red/yellow) — all pages use `gdf.status.*`

    **DO NOT remove:** `background`, `foreground`, `card`, `muted`, `border`, `input`, `ring`, `destructive` — these are shadcn/ui compatibility and are still needed.

    After removal, the config should have only: `gdf`, `background`, `foreground`, `card`, `card-foreground`, `muted`, `muted-foreground`, `border`, `input`, `ring`, `destructive`, `destructive-foreground`.

    **VERIFY before removing backward-compat shims:**
    ```bash
    # Check if any file still uses the old classes
    grep -rn "bg-surface-\|text-surface-\|bg-accent-\|text-accent-\|bg-brand-\|text-brand-\|bg-status-green\|bg-status-red\|bg-status-yellow\|text-status-green\|text-status-red\|text-status-yellow" src/ --include="*.tsx" | grep -v node_modules | grep -v ".planning"
    ```

    If any results remain, do NOT remove those specific shims — it means a page was missed.

    **PART D: Update cspell.json**
    - Remove "WaveBackground", "CyberGrid", "RingMeter", "StatCard", "DataChart", "SkeletonTable", "SkeletonCard", "FileInput" if present
    - Add any new words introduced in toast.tsx or other files that cause spell check failures

  </action>
  <verify>
    <automated>pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    **toast.tsx:**
    - `grep -c "gdf-glass-bg" src/shared/lib/toast.tsx` returns >= 3 (all three variants use gdf glass)
    - `grep -c "gdf-status-danger" src/shared/lib/toast.tsx` returns >= 1 (error uses danger)
    - `grep -c "gdf-accent-secondary" src/shared/lib/toast.tsx` returns >= 1 (success uses cyan)
    - `grep -c "gdf-accent-primary" src/shared/lib/toast.tsx` returns >= 1 (info uses blue)
    - `grep -c "oklch" src/shared/lib/toast.tsx` returns 0 (all oklch removed)
    - `grep -c "var(--neon-" src/shared/lib/toast.tsx` returns 0 (all neon vars removed)

    **globals.css:**
    - `grep -c "font-tech" src/app/styles/globals.css` returns 0
    - `grep -c "neon-glow-red" src/app/styles/globals.css` returns 0
    - `grep -c "glass-light" src/app/styles/globals.css` returns 0
    - `grep -c "glass-blur: none" src/app/styles/globals.css` returns 0
    - `grep -c "glass-shadow: none" src/app/styles/globals.css` returns 0

    **tailwind.config.js:**
    - `grep -c "surface:" src/app/styles/../tailwind.config.js` returns 0 (old surface shim removed)
    - `grep -c "brand:" src/app/styles/../tailwind.config.js` returns 0 (old brand shim removed)
    - Wait — these are in tailwind.config.js not app/styles. Let me correct the check:
    - `grep -c "\"surface\":" tailwind.config.js` returns 0 or 1 (0 if removed, 1 if preserved for shadcn compat — check context)

    Actually, let me simplify:

    **Build:**
    - `pnpm run build` exits 0
    - `pnpm run lint` exits 0
    - `pnpm run spell` exits 0

</acceptance_criteria>
<done>Toast system with glass styling, globals.css dead code purged, tailwind.config.js backward-compat shims removed</done>
</task>

<task type="auto">
  <name>Task 3: Final validation — full pnpm check + residual import audit</name>
  <files>(verification only)</files>
  <read_first>
    - Task 1 and Task 2 output
    - tailwind.config.js (verify final state)
    - src/app/styles/globals.css (verify final state)
  </read_first>
  <action>
    **Step 1: Run full pnpm check**
    ```bash
    pnpm check
    ```
    This runs: `pnpm run lint && pnpm run spell && pnpm run build`

    Fix any issues. Common failure points:
    - Unused imports after deletion (ESLint no-unused-vars)
    - New component names not in cspell dictionary
    - Build errors from missing files

    **Step 2: Residual import audit**
    ```bash
    echo "=== cyber/ imports remaining ==="
    grep -r "from '@/components/cyber/" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules
    echo "=== Expected: only StatusBadge + StockBarChart ==="
    ```

    ```bash
    echo "=== navigation/ imports ==="
    grep -r "from.*navigation/" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules
    echo "=== Expected: zero ==="
    ```

    ```bash
    echo "=== temp/ imports ==="
    grep -r "from.*temp/" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules
    echo "=== Expected: zero ==="
    ```

    ```bash
    echo "=== old token references ==="
    grep -rn "var(--accent-primary)\|var(--neon-\|var(--glass-bg)\|oklch(" src/ --include="*.tsx" | grep -v node_modules | grep -v ".planning"
    echo "=== Expected: zero or only in tokens.css/globals.css ==="
    ```

    **Step 3: Verify final file count in cyber/**
    ```bash
    ls src/components/cyber/
    ```
    Expected: StatusBadge.tsx, StockBarChart.tsx (2 files)

    **Step 4: Start dev server and do a quick visual check via CLI**
    ```bash
    pnpm dev &
    sleep 5
    curl -s http://localhost:5173 | head -20  # verify HTML loads
    kill %1
    ```

    **Step 5: Final git status**
    ```bash
    git status --short | head -30
    ```

    This is the final checkpoint. All issues must be resolved before the phase is complete.

  </action>
  <verify>
    <automated>pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 (lint + spell + build)
    - `ls src/components/cyber/` shows exactly 2 files: StatusBadge.tsx and StockBarChart.tsx
    - `grep -r "from '@/components/cyber/" src/ | grep -v "StatusBadge\|StockBarChart"` returns 0 lines (only those 2 have imports)
    - `ls src/components/navigation/ 2>/dev/null` fails (directory does not exist)
    - `ls temp/ 2>/dev/null` fails (directory does not exist)
    - `grep -r "from.*navigation/" src/` returns 0 lines
    - `git status` shows clean working directory (all changes committed)
  </acceptance_criteria>
  <done>Phase 01 complete: pnpm check passes, only StatusBadge + StockBarChart remain in cyber/, all dead code deleted</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                            |
| -------- | ------------------------------------------------------ |
| N/A      | Deletion phase — no new code, no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category          | Component                   | Disposition | Mitigation Plan                                                                                                              |
| --------- | ----------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-01-08   | Denial of Service | File deletion (rm commands) | mitigate    | Build verification ensures no required files are deleted. Import audit (grep) confirms zero broken references before any rm. |
| T-01-SC   | Tampering         | npm/pip/cargo installs      | accept      | No new packages installed.                                                                                                   |

</threat_model>

<verification>
```bash
pnpm check    # lint + spell + build — must pass
```
</verification>

<success_criteria>

- 8 dead cyber/ components deleted
- Panel, GlitchButton, ScreenLoader, TerminalLine deleted (0 imports verified)
- navigation/ directory deleted
- temp/ directory deleted
- Toast system uses gdf-\* glass tokens
- globals.css purged of dead utility classes
- tailwind.config.js backward-compat color shims removed
- pnpm check passes with zero errors
- Only 2 files remain in src/components/cyber/: StatusBadge.tsx, StockBarChart.tsx
  </success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-07-SUMMARY.md` when done
</output>
