---

phase: 01-ui-redesign
plan: 06
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified:

- src/pages/DashboardPage.tsx
- src/pages/LoginPage.tsx
- src/components/ErrorBoundary.tsx
- src/components/cyber/StockBarChart.tsx
  autonomous: true
  requirements: [RNF-03]

must*haves:
truths: - "Dashboard page shows tactical card layout with cyan accents and glass panels" - "Dashboard stat cards use gdf-* tokens instead of inline textShadow styles" - "Login page renders glass card with cyan-accented form" - "ErrorBoundary uses TacticalButton instead of GlitchButton" - "StockBarChart renders with new cyan/amber/red color palette"
artifacts: - path: "src/pages/DashboardPage.tsx"
provides: "Tactical dashboard layout with glass module cards"
contains: "gdf-accent-primary" - path: "src/pages/LoginPage.tsx"
provides: "Glass login card with cyan accents"
contains: "GlassPanel" - path: "src/components/ErrorBoundary.tsx"
provides: "Error boundary with TacticalButton"
contains: "TacticalButton" - path: "src/components/cyber/StockBarChart.tsx"
provides: "Bar chart with new tactical color palette"
contains: "#3b82f6"
key*links: - from: "DashboardPage.tsx"
to: "src/components/tactical/GlassPanel.tsx"
via: "import { GlassPanel } from '@/components/tactical/GlassPanel'"
pattern: "GlassPanel" - from: "StockBarChart.tsx"
to: "src/app/styles/tokens.css"
via: "var(--gdf-status-*) for bar colors"
pattern: "gdf-status"
</objective>

<objective>
Complete the visual migration for the 4 remaining files that weren't covered by the mechanical import swaps in plan 01-05: DashboardPage (most custom-styled page — 364 lines, no component abstraction, needs full rework), LoginPage (independent, no AppShell dependency), ErrorBoundary (still imports GlitchButton), and StockBarChart (Recharts colors need new palette). These are the highest-risk pages per the migration risk assessment — DashboardPage in particular requires replacing inline textShadow styles, terminal syslog, and hardcoded color classes with gdf-* tokens.

Purpose: Close the gap between mechanical import swaps and full visual redesign for the most complex pages. After this plan, all user-facing pages use the new design system.
Output: Redesigned DashboardPage, restyled LoginPage, migrated ErrorBoundary, recolored StockBarChart
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md §8.1 (hardest components: DashboardPage HIGH, LoginPage LOW, StockBarChart MEDIUM), §7.2 (page migration order)
@.planning/phases/01-ui-redesign/01-CONTEXT.md (lines 79-83: dashboard layout spec with central tactical map concept, widget system)
@.planning/codebase/CONVENTIONS.md (Named exports, format: pnpm format)
@AGENTS.md

<interfaces>
<!-- DashboardPage current state (research §1.7): uses glass/glass-heavy/glass-interactive classes, animate-blink, animate-pulse-glow, font-mono-sm, inline textShadow styles, TerminalLine syslog, ScreenLoader (ScreenLoader→HoloLoader swap happens in THIS plan, Task 1 Step 1 — plan 01-05 does NOT touch DashboardPage.tsx) -->

<!-- LoginPage current state: no cyber/ imports, uses bg-surface-base, brand-primary, font-mono, hardcoded zinc borders — completely raw Tailwind -->

<!-- ErrorBoundary current imports GlitchButton for 2 buttons (RETURN TO BASE + RECOVER) -->

<!-- StockBarChart current STATUS_COLORS: CRITICAL=#f43f5e, LOW=#f59e0b, OK=#22d3ee -->
<!-- New palette: OK=#06b6d4 (cyan), LOW=#f59e0b (amber, same), CRITICAL=#ef4444 (red) -->
<!-- StockBarEntry interface unchanged -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DashboardPage with tactical glass layout</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (full file — understand current structure: stat cards, module cards, syslog section)
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §1.7 (DashboardPage line in page-to-component map — note it uses glass/glass-heavy/glass-interactive classes but NO Panel component, uses TerminalLine for syslog)
    - src/components/tactical/GlassPanel.tsx (props interface — use GlassPanel for stat cards instead of divs with glass classes)
  </read_first>
  <action>
    DashboardPage.tsx is the most custom-styled page (364 lines, no Panel component abstraction, inline textShadow styles). This task replaces the brutalist dashboard with a tactical glass layout.

    **Step 1: Replace imports**
    - Add `import { GlassPanel } from '@/components/tactical/GlassPanel'` (wasn't imported before — DashboardPage used raw divs)
    - Replace `import { ScreenLoader } from '@/components/cyber/ScreenLoader'` with `import { HoloLoader } from '@/components/tactical/HoloLoader'` (ScreenLoader→HoloLoader swap — plan 01-05 did NOT touch DashboardPage.tsx, this plan does)
    - Remove `import { TerminalLine } from '@/components/cyber/TerminalLine'` — syslog section gets replaced
    - Replace `<ScreenLoader />` JSX with `<HoloLoader />` JSX

    **Step 2: Replace all hardcoded color/textShadow references**
    Search for and replace every instance of:
    - `textShadow` inline style → remove entirely (glow is now handled by CSS utility `.gdf-glow-text` or via emotional state)
    - `var(--accent-primary)` → `var(--gdf-accent-primary)`
    - `var(--neon-cyan)` / `var(--neon-fuchsia)` / `var(--neon-yellow)` → remove (these were aliases to red/amber; now use gdf-* equivalents)
    - Hardcoded `color: #ef4444` or `color: #f59e0b` → remove
    - `bg-surface-base` → `bg-transparent` (background is from TacticalBackground)
    - `bg-surface-raised` → `bg-gdf-surface-raised`
    - Hardcoded zinc border classes → `border-gdf-glass-border` or `border-gdf-border-subtle`

    **Step 3: Replace stat cards with GlassPanel**
    Current stat cards are raw divs with `.glass-interactive` and flex layouts. Wrap each stat card in `<GlassPanel accent="cyan" variant="default" className="h-full">`. Stat card content (icon, label, value) goes inside GlassPanel children.

    **Step 4: Replace module/navigation cards**
    Current module cards (for roles that see navigation-only dashboard) use `.glass-interactive` and hover effects. Replace with `<GlassPanel accent="amber" variant="subtle">` for navigation cards.

    **Step 5: Replace syslog/TerminalLine section**
    The syslog section at the bottom of DashboardPage uses TerminalLine with typewriter effect. Replace with a GlassPanel containing a static data feed display:
    - Title: "SYSLOG" → "TACTICAL LOG"
    - Content: static log entries styled with `font-mono-data text-gdf-text-secondary` — no typewriter animation (terminal aesthetic is anti-pattern for new design)
    - Each log entry: timestamp + status tag + message, color-coded by severity
    - Remove TerminalLine import and usage entirely

    **Step 6: General class replacements**
    - `font-mono-data` → KEEP (still used and valid)
    - `font-mono-sm` → KEEP
    - `animate-blink` → KEEP (used on status dots)
    - `animate-pulse-glow` → KEEP
    - `.glass`, `.glass-heavy`, `.glass-interactive` → replace with `<GlassPanel>` component usage
    - `text-text-primary` → `text-gdf-text-primary`
    - `text-text-secondary` → `text-gdf-text-secondary`
    - `text-text-muted` → `text-gdf-text-muted`
    - All hardcoded hex colors → remove (use gdf-* tokens via Tailwind classes)

    **Step 7: Add "tactical" to cspell.json** if not already present (it's a common word but might need custom dictionary entry).

    **DO NOT change:** Any TanStack Query hooks, data fetching logic, Zustand store access, role-based conditional rendering, navigation logic, or any business logic. Only the visual/JSX layer changes.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build` exits 0
    - `grep -c "GlassPanel" src/pages/DashboardPage.tsx` returns >= 2 (stat cards + syslog use GlassPanel)
    - `grep -c "from '@/components/cyber/TerminalLine'" src/pages/DashboardPage.tsx` returns 0 (TerminalLine removed)
    - `grep -c "TerminalLine" src/pages/DashboardPage.tsx` returns 0 (no TerminalLine JSX)
    - `grep -c "textShadow" src/pages/DashboardPage.tsx` returns 0 (all inline textShadow removed)
    - `grep -c "style=" src/pages/DashboardPage.tsx` returns 0 or only non-color styles (no inline color styles)
    - `grep -c "var(--accent-" src/pages/DashboardPage.tsx` returns 0 (old variable references removed)
    - `grep -c "gdf-" src/pages/DashboardPage.tsx` returns >= 5 (uses new tokens)
    - `grep -c "TACTICAL LOG" src/pages/DashboardPage.tsx` returns >= 1 (syslog section renamed)
    - `grep -c "\.glass\b" src/pages/DashboardPage.tsx` returns 0 (raw glass classes removed — using GlassPanel now)
    - `grep -c "from '@/components/cyber/ScreenLoader'" src/pages/DashboardPage.tsx` returns 0 (ScreenLoader removed)
    - `grep -c "from '@/components/tactical/HoloLoader'" src/pages/DashboardPage.tsx` returns >= 1 (HoloLoader imported)
    - `grep -c "ScreenLoader" src/pages/DashboardPage.tsx` returns 0 (no ScreenLoader JSX)
    - `grep -c "HoloLoader" src/pages/DashboardPage.tsx` returns >= 1 (HoloLoader used in JSX)
    - All business logic hooks unchanged (useCamps, useDashboardStats, etc.)
  </acceptance_criteria>
  <done>DashboardPage redesigned with GlassPanel stat cards, tactical log feed, gdf-* tokens throughout</done>
</task>

<task type="auto">
  <name>Task 2: Restyle LoginPage and ErrorBoundary, recolor StockBarChart</name>
  <files>src/pages/LoginPage.tsx, src/components/ErrorBoundary.tsx, src/components/cyber/StockBarChart.tsx</files>
  <read_first>
    - src/pages/LoginPage.tsx (full file — independent page, no cyber/ imports, uses hardcoded zinc/brand classes)
    - src/components/ErrorBoundary.tsx (full 62 lines — imports GlitchButton for 2 buttons)
    - src/components/cyber/StockBarChart.tsx (full 78 lines — STATUS_COLORS map, TOOLTIP_STYLE, chart YAxis colors)
    - src/components/tactical/GlassPanel.tsx (to use for LoginPage card wrapping)
    - src/components/tactical/TacticalButton.tsx (to replace GlitchButton in ErrorBoundary)
  </read_first>
  <action>
    **PART A: LoginPage.tsx** — Full restyle of the independent login page.

    1. Add imports: `import { GlassPanel } from '@/components/tactical/GlassPanel'` and `import { TacticalButton } from '@/components/tactical/TacticalButton'`
    2. Wrap the login form card in `<GlassPanel accent="cyan" variant="heavy" bracketed>`
    3. Replace all hardcoded color classes:
       - `bg-surface-base` → `bg-transparent` (background from TacticalBackground in App.tsx)
       - The main container currently has `bg-surface-base min-h-screen` → change to `bg-transparent min-h-screen relative`
       - Form card bg: remove (GlassPanel provides bg)
       - Input fields: replace `terminal-input` with new glass input styling: `bg-gdf-surface-base/50 border border-gdf-border-subtle text-gdf-text-primary font-mono text-sm p-3 focus:border-gdf-accent-primary focus:ring-1 focus:ring-gdf-accent-primary/20 outline-none rounded-md placeholder:text-gdf-text-muted`
       - Labels: `text-zinc-400` → `text-gdf-text-secondary`, keep `font-mono text-xs uppercase tracking-wider`
       - Error text: `text-[var(--neon-yellow)]` → `text-gdf-status-warning`
       - Submit button: use `<TacticalButton variant="primary" type="submit">` instead of raw styled button
       - App title: `text-brand-primary` → `text-gdf-accent-secondary`, increase size, add letter-spacing
       - GF logo square: `bg-brand-primary` → `bg-gdf-accent-primary`, text `text-surface-base` → `text-gdf-text-inverse`
       - Link text: `text-zinc-400 hover:text-brand-primary` → `text-gdf-text-muted hover:text-gdf-accent-secondary`
    4. Replace "OPERATOR AUTHENTICATION" header → "TACTICAL COMMAND // AUTHENTICATION"
    5. Replace "INITIALIZE SESSION" button text → "AUTHENTICATE"
    6. Replace "INVALID CIPHER KEY" error text → "INVALID CREDENTIALS"
    7. Keep all form logic: react-hook-form, zodResolver, authService.login, onSubmit handler
    8. Keep the session timeout note at the bottom — change styling only

    **PART B: ErrorBoundary.tsx** — Swap GlitchButton→TacticalButton.

    1. Replace import: `import { GlitchButton } from '@/components/cyber/GlitchButton'` → `import { TacticalButton } from '@/components/tactical/TacticalButton'`
    2. Replace JSX: `<GlitchButton variant="ghost">` → `<TacticalButton variant="ghost">`, `<GlitchButton variant="primary">` → `<TacticalButton variant="primary">`
    3. Update text: "RETURN TO BASE" → "RETURN TO DASHBOARD", "RECOVER" → "RETRY"
    4. Update error styling:
       - `text-red-400` → `text-gdf-status-danger`
       - `text-zinc-400` → `text-gdf-text-secondary`
       - `border-zinc-800` → `border-gdf-border-subtle`
       - `bg-zinc-950` → `bg-gdf-surface-base`
       - `text-red-300` → `text-gdf-status-danger/80`
       - "/// SYSTEM FAULT ///" → "/// TACTICAL FAULT ///"
       - "A critical render error was intercepted. The application has been contained." → "A critical render error was intercepted. The interface has been contained."

    **PART C: StockBarChart.tsx** — Recolor Recharts chart.

    1. Update STATUS_COLORS map:
       - `CRITICAL: '#f43f5e'` → `CRITICAL: '#ef4444'` (gdf-status-danger)
       - `LOW: '#f59e0b'` → `LOW: '#f59e0b'` (gdf-status-warning — same color)
       - `OK: '#22d3ee'` → `OK: '#06b6d4'` (gdf-accent-secondary/cyan)

    2. Update TOOLTIP_STYLE:
       - `backgroundColor: 'oklch(0.1 0.03 320 / 0.95)'` → `backgroundColor: 'var(--gdf-surface-overlay)'`
       - `border: '1px solid oklch(0.68 0.32 340 / 0.3)'` → `border: '1px solid var(--gdf-border-default)'`
       - `borderRadius: '2px'` → `borderRadius: 'var(--gdf-radius-md)'`
       - Keep fontFamily and fontSize, change `color: 'var(--text-primary, #e0e0e0)'` → `color: 'var(--gdf-text-primary)'`

    3. Update chart axis colors:
       - YAxis tick fill: `'oklch(0.68 0.32 340 / 0.6)'` → `'var(--gdf-text-muted)'`
       - CartesianGrid stroke: `'oklch(0.55 0.22 210 / 0.08)'` → `'var(--gdf-border-subtle)'`
       - Tooltip cursor fill: `'oklch(0.68 0.32 340 / 0.05)'` → `'var(--gdf-accent-primary-glow)'`

    4. Update empty state: `text-muted-foreground` → `text-gdf-text-muted`, `NO STOCK DATA AVAILABLE` → `NO STOCK DATA AVAILABLE` (text stays, just color change)

    5. StockBarEntry interface: UNCHANGED.

    **DO NOT change:** Any Recharts component props, BarChart layout logic, ResponsiveContainer configuration, or data flow. Only colors change.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    **LoginPage:**
    - `grep -c "GlassPanel" src/pages/LoginPage.tsx` returns >= 1 (uses GlassPanel for card)
    - `grep -c "TacticalButton" src/pages/LoginPage.tsx` returns >= 1 (uses TacticalButton for submit)
    - `grep -c "TACTICAL COMMAND" src/pages/LoginPage.tsx` returns >= 1 (header text updated)
    - `grep -c "gdf-" src/pages/LoginPage.tsx` returns >= 5 (uses new tokens)
    - `grep -c "brand-primary" src/pages/LoginPage.tsx` returns 0 (old red removed)
    - `grep -c "zinc-" src/pages/LoginPage.tsx` returns 0 (hardcoded zinc removed)
    - `grep -c "CIPHER" src/pages/LoginPage.tsx` returns 0 (terminal language removed)

    **ErrorBoundary:**
    - `grep -c "TacticalButton" src/components/ErrorBoundary.tsx` returns >= 2 (two buttons)
    - `grep -c "GlitchButton" src/components/ErrorBoundary.tsx` returns 0 (old button removed)
    - `grep -c "gdf-" src/components/ErrorBoundary.tsx` returns >= 3 (new tokens)

    **StockBarChart:**
    - `grep -c "'#06b6d4'" src/components/cyber/StockBarChart.tsx` returns >= 1 (cyan OK color)
    - `grep -c "'#ef4444'" src/components/cyber/StockBarChart.tsx` returns >= 1 (red CRITICAL color)
    - `grep -c "oklch" src/components/cyber/StockBarChart.tsx` returns 0 (all oklch removed)
    - `grep -c "var(--gdf-" src/components/cyber/StockBarChart.tsx` returns >= 3 (uses CSS variables)
    - `StockBarEntry` interface unchanged (verify via grep)

    **Build:**
    - `pnpm run build` exits 0

</acceptance_criteria>
<done>LoginPage glass restyle, ErrorBoundary TacticalButton migration, StockBarChart tactical color palette</done>
</task>

<task type="auto">
  <name>Task 3: Final audit — verify zero old design references remain in migrated pages</name>
  <files>src/pages/DashboardPage.tsx, src/pages/LoginPage.tsx, src/components/ErrorBoundary.tsx</files>
  <read_first>
    - Results of Task 1 and Task 2
  </read_first>
  <action>
    Run comprehensive grep audits to verify all old design references are purged from the 4 files modified in this plan:

    ```bash
    # Verify no old token references in any migrated file
    grep -rn "var(--accent-primary)" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx src/components/ErrorBoundary.tsx
    grep -rn "var(--neon-" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx
    grep -rn "oklch" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx src/components/ErrorBoundary.tsx
    grep -rn "brand-primary\|brand-secondary" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx
    grep -rn "textShadow" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx
    ```

    All should return zero matches.

    Verify the full cyber/ import audit across ALL source files:
    ```bash
    grep -r "from '@/components/cyber/" src/ | sort
    ```

    Expected remaining: StockBarChart (in InventoryPage), StatusBadge (in various feature pages), TerminalLine (should be 0 — removed from DashboardPage in Task 1). If TerminalLine has residual imports, remove them.

    Run `pnpm check` to verify lint + spell + build.

  </action>
  <verify>
    <automated>pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "var(--accent-)" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx` returns 0
    - `grep -rn "var(--neon-" src/pages/` returns 0
    - `grep -rn "oklch" src/pages/` returns 0
    - `grep -rn "textShadow" src/pages/DashboardPage.tsx` returns 0
    - `grep -rn "brand-primary" src/pages/DashboardPage.tsx src/pages/LoginPage.tsx` returns 0
    - `grep -r "from '@/components/cyber/TerminalLine'" src/` returns 0 (TerminalLine fully removed)
    - `grep -r "from '@/components/cyber/GlitchButton'" src/` returns 0 (only ErrorBoundary had it, now migrated)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Audit complete: all old design references purged, TerminalLine and GlitchButton fully migrated out</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                           |
| -------------------- | --------------------------------------------------------------------- |
| LoginPage form → API | JWT credentials sent over HTTPS. Visual only — no auth logic changes. |

## STRIDE Threat Register

| Threat ID | Category  | Component               | Disposition | Mitigation Plan                                                                                      |
| --------- | --------- | ----------------------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-01-07   | Spoofing  | LoginPage (visual only) | accept      | Login form logic unchanged — JWT token handling unchanged. Visual restyle cannot introduce spoofing. |
| T-01-SC   | Tampering | npm/pip/cargo installs  | accept      | No new packages installed.                                                                           |

</threat_model>

<verification>
```bash
pnpm check    # lint + spell + build — must pass
```
</verification>

<success_criteria>

- DashboardPage: glass stat cards, tactical log, no inline textShadow or old color references
- LoginPage: glass card layout, TacticalButton submit, cyan accents
- ErrorBoundary: TacticalButton, updated error text
- StockBarChart: cyan/amber/red color palette, CSS variable references
- pnpm check passes
  </success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-06-SUMMARY.md` when done
</output>
