---

phase: 01-ui-redesign
plan: 05
type: execute
wave: 4
depends_on: [01-02]
files_modified:

- src/features/camps/pages/CampsPage.tsx
- src/features/camps/pages/CampDetailPage.tsx
- src/features/people/pages/PeopleListPage.tsx
- src/features/people/pages/PersonDetailPage.tsx
- src/features/people/pages/PersonCreatePage.tsx
- src/features/inventory/pages/InventoryPage.tsx
- src/features/inventory/pages/InventoryAuditPage.tsx
- src/features/explorations/pages/ExplorationsPage.tsx
- src/features/explorations/pages/ExplorationDetailPage.tsx
- src/features/admission/pages/AdmissionsPage.tsx
- src/features/admission/components/AdmissionDetailPanel.tsx
- src/features/admission/components/AIAnalysisPanel.tsx
- src/features/inventory/components/StockAlertBanner.tsx
- src/features/transfers/pages/TransfersPage.tsx
- src/features/resources/pages/ResourcesPage.tsx
- src/features/professions/pages/ProfessionsPage.tsx
- src/features/users/pages/UsersPage.tsx
- src/features/rations/pages/RationsPage.tsx
- src/routes/AppRoutes.tsx
- src/routes/ProtectedRoute.tsx
- src/App.tsx
  autonomous: true
  requirements: [RNF-03]

must_haves:
truths: - "All feature pages import GlassPanel instead of Panel" - "All feature pages import TacticalButton instead of GlitchButton" - "All loading sites use HoloLoader instead of ScreenLoader" - "App.tsx, AppRoutes.tsx, and ProtectedRoute.tsx use HoloLoader"
artifacts: - path: "src/features/_/pages/_.tsx (16 files)"
provides: "Feature pages with swapped component imports" - path: "src/routes/AppRoutes.tsx"
provides: "Route definitions using HoloLoader" - path: "src/App.tsx"
provides: "App bootstrap using HoloLoader"
key_links: - from: "every feature page"
to: "src/components/tactical/GlassPanel.tsx"
via: "import { GlassPanel } from '@/components/tactical/GlassPanel'"
pattern: "GlassPanel.*tactical" - from: "every feature page"
to: "src/components/tactical/TacticalButton.tsx"
via: "import { TacticalButton } from '@/components/tactical/TacticalButton'"
pattern: "TacticalButton.*tactical"
</objective>

<objective>
Swap all component imports across every feature page, route file, and App.tsx. Replace Panel → GlassPanel (18 import sites), GlitchButton → TacticalButton (14 feature page sites + ErrorBoundary in plan 01-06), and ScreenLoader → HoloLoader (17 feature page sites + App.tsx + AppRoutes + ProtectedRoute = 20 sites). Pages with the old component names 'Panel' and 'GlitchButton' in JSX will use aliased imports where needed. This is a mechanical find-and-replace operation — no visual redesign of individual pages (that's plan 01-06 for Dashboard/Login).

Purpose: After this plan, ALL pages load the new glass components. The old cyber/ components still exist but have zero imports (enabling Wave 6 deletion).
Output: 18 feature page files, 3 route/app files updated with swapped imports
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md §1.7 (Page-to-Component Usage Map — exact import counts per page, lines 228-254)
@.planning/codebase/CONVENTIONS.md (Named exports, no default exports, import order)
@AGENTS.md

<interfaces>
<!-- GlassPanel props (same as Panel) -->
From src/components/tactical/GlassPanel.tsx:
```typescript
export function GlassPanel({ title, tag, status, children, className, accent, variant, bracketed }: GlassPanelProps)
// AccentColor: 'cyan' | 'amber' | 'green' | 'red' (purple→amber rename!)
// variant: 'default' | 'heavy' | 'subtle' (elevated→heavy, flat→subtle rename!)
// New prop: bracketed?: boolean
```

<!-- TacticalButton props (same as GlitchButton) -->

From src/components/tactical/TacticalButton.tsx:

```typescript
export function TacticalButton({ children, variant, ...rest }: TacticalButtonProps);
// variant: 'primary' | 'ghost' | 'warning' | 'danger' (same names!)
```

<!-- HoloLoader (same usage as ScreenLoader — no props) -->

From src/components/tactical/HoloLoader.tsx:

```typescript
export function HoloLoader(); // no props, self-closing
```

<!-- IMPORTANT: Panel 'cyan' accent maps to GlassPanel 'cyan' (same name, same behavior). Panel 'purple' accent maps to GlassPanel 'amber' (semantic rename). Panel 'green' → 'green', 'red' → 'red' (same names). -->

<!-- IMPORTANT: Panel 'elevated' variant → GlassPanel 'heavy'. Panel 'flat' variant → GlassPanel 'subtle'. -->

<!-- Search patterns to replace:
  import { Panel } from '@/components/cyber/Panel'
    → import { GlassPanel } from '@/components/tactical/GlassPanel'

  import { GlitchButton } from '@/components/cyber/GlitchButton'
    → import { TacticalButton } from '@/components/tactical/TacticalButton'

  import { ScreenLoader } from '@/components/cyber/ScreenLoader'
    → import { HoloLoader } from '@/components/tactical/HoloLoader'
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Panel→GlassPanel and GlitchButton→TacticalButton in all feature pages</name>
  <files>src/features/camps/pages/CampsPage.tsx, src/features/camps/pages/CampDetailPage.tsx, src/features/people/pages/PeopleListPage.tsx, src/features/people/pages/PersonDetailPage.tsx, src/features/people/pages/PersonCreatePage.tsx, src/features/inventory/pages/InventoryPage.tsx, src/features/inventory/pages/InventoryAuditPage.tsx, src/features/explorations/pages/ExplorationsPage.tsx, src/features/explorations/pages/ExplorationDetailPage.tsx, src/features/admission/pages/AdmissionsPage.tsx, src/features/admission/components/AdmissionDetailPanel.tsx, src/features/admission/components/AIAnalysisPanel.tsx, src/features/inventory/components/StockAlertBanner.tsx, src/features/transfers/pages/TransfersPage.tsx, src/features/resources/pages/ResourcesPage.tsx, src/features/professions/pages/ProfessionsPage.tsx, src/features/users/pages/UsersPage.tsx, src/features/rations/pages/RationsPage.tsx</files>
  <read_first>
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §1.7 (lines 228-254 — the exact page-to-component map showing which pages import what)
    - src/components/tactical/GlassPanel.tsx (props interface — note accent 'purple'→'amber' rename, variant 'elevated'→'heavy' + 'flat'→'subtle')
    - src/components/tactical/TacticalButton.tsx (props interface — variant names unchanged)
  </read_first>
  <action>
    This is a mechanical find-and-replace across 18 feature files. Process each file:

    **Step 1: Replace imports**
    Replace import { Panel } from '@/components/cyber/Panel' → import { GlassPanel } from '@/components/tactical/GlassPanel'
    Replace import { GlitchButton } from '@/components/cyber/GlitchButton' → import { TacticalButton } from '@/components/tactical/TacticalButton'

    If a file imports both Panel and GlitchButton from cyber/, replace both lines with the two new import lines from tactical/. Follow CONVENTIONS.md import order: @/components/tactical/ imports go after third-party libraries and before @/features/ imports.

    **Step 2: Replace JSX usage**
    Replace `<Panel` → `<GlassPanel` and `</Panel>` → `</GlassPanel>`
    Replace `<GlitchButton` → `<TacticalButton` and `</GlitchButton>` → `</TacticalButton>`

    **Step 3: Fix accent prop values (CRITICAL)**
    Search each file for `accent=` prop on GlassPanel. The old Panel used 'cyan'/'purple'/'green'/'red'. The new GlassPanel uses 'cyan'/'amber'/'green'/'red'.

    For every occurrence of `accent="purple"` or `accent='purple'` → change to `accent="amber"` or `accent='amber'`

    **Step 4: Fix variant prop values**
    For every occurrence of `variant="elevated"` or `variant='elevated'` → change to `variant="heavy"` or `variant='heavy'`
    For every occurrence of `variant="flat"` or `variant='flat'` → change to `variant="subtle"` or `variant='subtle'`

    **Step 5: Remove any other cyber/ imports**
    If any file imports StatusBadge, ScreenLoader, or other cyber/ components — leave those for now (StatusBadge is already restyled in-place, ScreenLoader is handled in Task 2).

    **Files to process (18 total):**
    1. CampsPage.tsx — imports Panel + GlitchButton + ScreenLoader
    2. CampDetailPage.tsx — imports Panel + GlitchButton + ScreenLoader
    3. PeopleListPage.tsx — imports Panel + GlitchButton + ScreenLoader
    4. PersonDetailPage.tsx — imports Panel + GlitchButton + ScreenLoader (HEAVY neon CSS var usage — note for plan 01-06)
    5. PersonCreatePage.tsx — imports Panel + GlitchButton + ScreenLoader
    6. InventoryPage.tsx — imports Panel + GlitchButton + ScreenLoader + StatusBadge
    7. InventoryAuditPage.tsx — imports Panel + GlitchButton + ScreenLoader
    8. ExplorationsPage.tsx — imports Panel + GlitchButton + ScreenLoader
    9. ExplorationDetailPage.tsx — imports Panel + GlitchButton + ScreenLoader
    10. AdmissionsPage.tsx — imports Panel + GlitchButton + ScreenLoader
    11. AdmissionDetailPanel.tsx — imports Panel only (feature component)
    12. AIAnalysisPanel.tsx — imports Panel + StatusBadge (feature component)
    13. StockAlertBanner.tsx — imports Panel + StatusBadge (feature component)
    14. TransfersPage.tsx — imports Panel + GlitchButton + ScreenLoader + StatusBadge
    15. ResourcesPage.tsx — imports Panel + GlitchButton + ScreenLoader
    16. ProfessionsPage.tsx — imports Panel + GlitchButton + ScreenLoader
    17. UsersPage.tsx — imports Panel + GlitchButton + ScreenLoader + StatusBadge
    18. RationsPage.tsx — imports Panel + GlitchButton + ScreenLoader

    **DO NOT change:** Any business logic, TanStack Query hooks, Zustand store access, form logic, Zod schemas, API calls, or react-hook-form usage. JSX structure preserved — only component names and import paths change.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build` exits 0
    - `grep -r "from '@/components/cyber/Panel'" src/features/` returns 0 lines (all Panel imports replaced)
    - `grep -r "from '@/components/cyber/GlitchButton'" src/features/` returns 0 lines (all GlitchButton imports replaced in features)
    - `grep -r "from '@/components/tactical/GlassPanel'" src/features/` returns >= 17 lines (all feature files import GlassPanel)
    - `grep -r "from '@/components/tactical/TacticalButton'" src/features/` returns >= 13 lines (all feature files import TacticalButton)
    - `grep -rn "accent.*purple" src/features/` returns 0 lines (no purple accent remaining — all renamed to amber)
    - `grep -rn "variant.*elevated" src/features/` returns 0 lines (no elevated variant — all renamed to heavy)
    - `grep -rn "variant.*flat" src/features/` returns 0 lines (no flat variant — all renamed to subtle)
    - `grep -rn "<Panel[ >]" src/features/ | grep -v "GlassPanel\|AIAnalysisPanel\|AdmissionDetailPanel"` returns 0 lines (no bare Panel JSX usage)
    - `grep -rn "<GlitchButton" src/features/` returns 0 lines (no GlitchButton JSX usage)
    - All business logic unchanged: verify by checking a sample file (CampsPage.tsx) that TanStack Query hooks, store access, form handlers are intact
  </acceptance_criteria>
  <done>18 feature files: Panel→GlassPanel + accent/variant prop migrations, GlitchButton→TacticalButton</done>
</task>

<task type="auto">
  <name>Task 2: Swap ScreenLoader→HoloLoader in all loading sites and route files</name>
  <files>src/features/camps/pages/CampsPage.tsx, src/features/camps/pages/CampDetailPage.tsx, src/features/people/pages/PeopleListPage.tsx, src/features/people/pages/PersonDetailPage.tsx, src/features/people/pages/PersonCreatePage.tsx, src/features/inventory/pages/InventoryPage.tsx, src/features/inventory/pages/InventoryAuditPage.tsx, src/features/explorations/pages/ExplorationsPage.tsx, src/features/explorations/pages/ExplorationDetailPage.tsx, src/features/admission/pages/AdmissionsPage.tsx, src/features/transfers/pages/TransfersPage.tsx, src/features/resources/pages/ResourcesPage.tsx, src/features/professions/pages/ProfessionsPage.tsx, src/features/users/pages/UsersPage.tsx, src/features/rations/pages/RationsPage.tsx, src/routes/AppRoutes.tsx, src/routes/ProtectedRoute.tsx, src/App.tsx</files>
  <read_first>
    - src/components/tactical/HoloLoader.tsx (created in plan 01-02 — same pattern: no props, self-closing)
    - Any one feature page (e.g., CampsPage.tsx) to see the ScreenLoader import + usage pattern
    - src/routes/AppRoutes.tsx (line 6: import ScreenLoader for Suspense fallback)
    - src/routes/ProtectedRoute.tsx (line 5: import ScreenLoader for init loading)
    - src/App.tsx (line 6: import ScreenLoader for boot loading)
  </read_first>
  <action>
    This is a mechanical find-and-replace across 16 feature pages + 3 route/app files.

    **In each feature page file (16 files):**
    Replace import { ScreenLoader } from '@/components/cyber/ScreenLoader' → import { HoloLoader } from '@/components/tactical/HoloLoader'
    Replace `<ScreenLoader />` → `<HoloLoader />`

    **In AppRoutes.tsx:**
    Replace import { ScreenLoader } from '@/components/cyber/ScreenLoader' → import { HoloLoader } from '@/components/tactical/HoloLoader'
    The import is on line 6 and used as `<ScreenLoader />` in the Suspense fallback for lazy-loaded routes. Replace the JSX usage.

    **In ProtectedRoute.tsx:**
    Replace import { ScreenLoader } from '@/components/cyber/ScreenLoader' → import { HoloLoader } from '@/components/tactical/HoloLoader'
    Used on line 5 when `isInitializing` (Zustand rehydration in progress). Replace the JSX usage.

    **In App.tsx:**
    Replace import { ScreenLoader } from '@/components/cyber/ScreenLoader' → import { HoloLoader } from '@/components/tactical/HoloLoader'
    Used when `!hasHydrated` (Zustand store rehydration). Replace the JSX usage: `<ScreenLoader />` → `<HoloLoader />`

    **After all replacements, run grep to verify no residual ScreenLoader imports:**
    ```bash
    grep -r "from '@/components/cyber/ScreenLoader'" src/
    ```
    Should return zero results (all 20 sites migrated).

    **DO NOT change:** The Suspense fallback structure, the ProtectedRoute guard logic, the AppBootstrap hydrating logic. Only the import path and component name change.

  </action>
  <verify>
    <automated>bash -c 'grep -r "from.*cyber/ScreenLoader" src/ | wc -l'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from '@/components/cyber/ScreenLoader'" src/` returns 0 lines (all ScreenLoader imports replaced)
    - `grep -r "from '@/components/tactical/HoloLoader'" src/` returns >= 19 lines (all 19 sites import HoloLoader)
    - `grep -rn "<ScreenLoader" src/` returns 0 lines (no residual JSX usage)
    - `grep -rn "<HoloLoader" src/` returns >= 19 lines (all 19 sites use HoloLoader in JSX)
    - `pnpm run build` exits 0
  </acceptance_criteria>
  <done>ScreenLoader→HoloLoader swapped across 19 import sites (16 feature pages + AppRoutes + ProtectedRoute + App.tsx)</done>
</task>

<task type="auto">
  <name>Task 3: Verify zero cyber/ imports remain and build passes</name>
  <files>src/features/camps/pages/CampsPage.tsx, src/routes/AppRoutes.tsx, src/App.tsx</files>
  <read_first>
    - Task 1 and Task 2 output
    - .planning/phases/01-ui-redesign/01-RESEARCH.md §1.1 — list of 6 active cyber/ components: Panel, GlitchButton, StatusBadge, ScreenLoader, StockBarChart, TerminalLine
  </read_first>
  <action>
    Run a comprehensive audit to confirm the migration state:

    ```bash
    # Count remaining cyber/ imports
    grep -r "from '@/components/cyber/" src/ | grep -v "node_modules" | sort
    ```

    Expected remaining cyber/ imports after this plan:
    - StatusBadge: still imported by feature pages (it was restyled in-place, no rename needed — it stays in cyber/ until Wave 6)
    - StockBarChart: imported by InventoryPage (restyled in plan 01-06)
    - TerminalLine: imported by DashboardPage (replaced in plan 01-06)
    - GlitchButton: imported by ErrorBoundary (replaced in plan 01-06)
    - ScreenLoader: imported by DashboardPage (replaced in plan 01-06)
    - Panel: imported by DashboardPage (replaced in plan 01-06)

    These 6 remaining imports in DashboardPage.tsx and ErrorBoundary.tsx are expected — they're handled in plan 01-06.

    Verify build: `pnpm run build` must pass.
    Verify lint: `pnpm run lint` must pass.

    Run a quick typecheck on a sample of migrated files:
    ```bash
    npx tsc --noEmit 2>&1 | head -20
    ```

  </action>
  <verify>
    <automated>bash -c 'grep -r "from.*cyber/Panel\|from.*cyber/GlitchButton\|from.*cyber/ScreenLoader" src/features/ | wc -l'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from.*cyber/Panel" src/features/` returns 0 lines (0 Panel imports in features)
    - `grep -r "from.*cyber/GlitchButton" src/features/` returns 0 lines (0 GlitchButton imports in features)
    - `grep -r "from.*cyber/ScreenLoader" src/features/` returns 0 lines (0 ScreenLoader imports in features)
    - `grep -r "from.*cyber/ScreenLoader" src/routes/` returns 0 lines (0 in routes)
    - `grep -r "from.*cyber/ScreenLoader" src/App.tsx` returns 0 lines (0 in App.tsx)
    - `pnpm run build` exits 0
    - `pnpm run lint` exits 0
  </acceptance_criteria>
  <done>Verified: Panel/GlitchButton/ScreenLoader imports fully migrated from features/routes/App; only DashboardPage + ErrorBoundary remain for plan 01-06</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                   |
| -------- | ------------------------------------------------------------- |
| N/A      | Mechanical import swaps — no logic changes, no new data flow. |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                 |
| --------- | --------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-01-06   | Tampering | Page component imports | mitigate    | Build verification ensures all imports resolve. TypeScript strict mode catches prop mismatches. |
| T-01-SC   | Tampering | npm/pip/cargo installs | accept      | No new packages installed.                                                                      |

</threat_model>

<verification>
```bash
pnpm check    # lint + spell + build — must pass
```
</verification>

<success_criteria>

- All 18 feature pages use GlassPanel, TacticalButton, HoloLoader
- AppRoutes, ProtectedRoute, App.tsx use HoloLoader
- All accent='purple' → accent='amber' and variant='elevated'/'flat' → 'heavy'/'subtle' renames applied
- Zero Panel, GlitchButton, or ScreenLoader imports remain in src/features/ or src/routes/ or src/App.tsx
- Build passes with zero type errors
  </success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-05-SUMMARY.md` when done
</output>
