---

phase: 01-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:

- src/components/tactical/GlassPanel.tsx
- src/components/tactical/TacticalButton.tsx
- src/components/tactical/HoloLoader.tsx
- src/components/cyber/StatusBadge.tsx
- src/shared/lib/motion.ts
  autonomous: true
  requirements: [RNF-03]

must_haves:
truths: - "GlassPanel renders with backdrop-filter blur, cyan border, and optional corner brackets" - "TacticalButton has 4 variants (primary/ghost/danger/warning) consuming --gdf-_ tokens" - "StatusBadge shows colored dot + label with new cyan/amber/green palette" - "HoloLoader spins with cyan ring animation replacing red spinning borders"
artifacts: - path: "src/components/tactical/GlassPanel.tsx"
provides: "Glass card wrapper — replaces cyber/Panel.tsx"
exports: ["GlassPanel"]
contains: "backdrop-blur-glass" - path: "src/components/tactical/TacticalButton.tsx"
provides: "4-variant button system — replaces cyber/GlitchButton.tsx"
exports: ["TacticalButton"]
contains: "gdf-accent-primary" - path: "src/components/tactical/HoloLoader.tsx"
provides: "Holographic loading spinner — replaces cyber/ScreenLoader.tsx"
exports: ["HoloLoader"] - path: "src/components/cyber/StatusBadge.tsx"
provides: "Restyle in-place with new status colors"
contains: "gdf-status-"
key_links: - from: "GlassPanel.tsx"
to: "src/app/styles/tokens.css"
via: "Tailwind gdf-_ classes resolved to --gdf-\* CSS variables"
pattern: "bg-gdf-|border-gdf-|text-gdf-" - from: "TacticalButton.tsx"
to: "tailwind.config.js"
via: "bg-gdf-accent-primary → var(--gdf-accent-primary)"
pattern: "gdf-"
</objective>

<objective>
Create the core glass component library that replaces the brutalist cyber/ primitives. Build GlassPanel (replaces Panel), TacticalButton (replaces GlitchButton), HoloLoader (replaces ScreenLoader), and restyle StatusBadge in-place. All components consume the --gdf-* design tokens from plan 01-01 and use backdrop-filter for true glassmorphism. Props interfaces preserved for drop-in replacement compatibility. Create shared motion variants file for Framer Motion reuse across components.

Purpose: Establish the visual foundation that all pages will consume. Without these primitives, no page migration can proceed.
Output: 3 new tactical/ components, 1 restyled cyber/ component, 1 new motion.ts
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md §4 (Glassmorphism strategy — blur values, panel structure), §7 (Pattern 1: GlassPanel code, Pattern 2: TacticalButton code)
@.planning/phases/01-ui-redesign/01-CONTEXT.md (Glass specificity: bg-white/[0.03-0.08] dark / bg-white/[0.6-0.8] light, blur 12-20px, subtle borders)
@.planning/codebase/CONVENTIONS.md (Named exports, Props interfaces, component structure: hooks→derived→handlers→JSX)
@AGENTS.md

<interfaces>
<!-- Panel props interface to preserve -->
From src/components/cyber/Panel.tsx:
```typescript
type AccentColor = 'cyan' | 'purple' | 'green' | 'red';
interface PanelProps {
  title?: string; tag?: string; status?: string; children: ReactNode;
  className?: string; accent?: AccentColor; variant?: 'default' | 'elevated' | 'flat';
}
```

<!-- GlitchButton props interface to preserve -->

From src/components/cyber/GlitchButton.tsx:

```typescript
interface Props extends ButtonHTMLAttributes<HTMLButtonElement> {
  children: ReactNode;
  variant?: 'primary' | 'ghost' | 'warning' | 'danger';
}
```

<!-- StatusBadge current interface -->

From src/components/cyber/StatusBadge.tsx:

```typescript
export interface StatusBadgeProps {
  status: string;
  variant?: 'red' | 'amber' | 'green';
  className?: string;
}
```

Current hardcoded colors: red=border-red-500/30 text-red-400, amber=border-amber-500/30 text-amber-400, green=border-emerald-500/30 text-emerald-400

<!-- ScreenLoader: no props, 23 import sites, always used as <ScreenLoader /> -->

From src/components/cyber/ScreenLoader.tsx: receives no props, always self-closing.

<!-- Glass panel spec from RESEARCH.md §4.2 -->

New AccentColor: 'cyan' | 'amber' | 'green' | 'red' (purple→amber renames)
New variant: 'default' | 'heavy' | 'subtle' (elevated→heavy, flat→subtle)
New prop: bracketed?: boolean (corner bracket accents)
Glass classes: backdrop-blur-glass (16px), backdrop-blur-glass-heavy (24px)
Blur values from tokens: --gdf-glass-blur (16px), --gdf-glass-blur-heavy (24px)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlassPanel and TacticalButton components</name>
  <files>src/components/tactical/GlassPanel.tsx, src/components/tactical/TacticalButton.tsx</files>
  <read_first>
    - .planning/phases/01-ui-redesign/01-RESEARCH.md (lines 1467-1585 — complete GlassPanel + TacticalButton code patterns)
    - src/components/cyber/Panel.tsx (full 109 lines — props interface and ACCENT_CLASSES pattern to replicate)
    - src/components/cyber/GlitchButton.tsx (full 27 lines — props interface and variant styles to replicate)
    - src/lib/utils.ts (cn utility import path: import { cn } from '@/lib/utils')
    - src/app/styles/tokens.css (gdf-* variable names to reference in Tailwind classes)
  </read_first>
  <action>
    Create directory `src/components/tactical/` if it does not exist.

    **GlassPanel.tsx** — Drop-in replacement for cyber/Panel.tsx. Same props shape: title, tag, status, children, className, accent, variant. Same AccentColor type but 'purple'→'amber' (semantic rename). New optional `bracketed?: boolean` prop for corner bracket accents.

    Accent color map (ACCENT_CLASSES) for 4 accents:
    - cyan: text-gdf-accent-secondary, border-gdf-accent-secondary/20, dot-bg-gdf-accent-secondary
    - amber: text-gdf-status-warning, border-gdf-status-warning/20, dot-bg-gdf-status-warning
    - green: text-gdf-status-success, border-gdf-status-success/20, dot-bg-gdf-status-success
    - red: text-gdf-status-danger, border-gdf-status-danger/20, dot-bg-gdf-status-danger

    Variant rendering (variantClass):
    - 'default': `backdrop-blur-glass bg-gdf-glass-bg border border-gdf-glass-border rounded-md`
    - 'heavy': `backdrop-blur-glass-heavy bg-gdf-glass-bg-heavy border border-gdf-glass-border rounded-md`
    - 'subtle': `backdrop-blur-glass bg-gdf-glass-bg/40 border border-gdf-glass-border rounded-md`

    Header section (title/tag/status): same structure as Panel — flex row with tag badge (px-2.5 py-1 text-[10px] font-mono font-bold uppercase tracking-wider, accent border+text+bg), title (text-sm font-semibold tracking-tight text-gdf-text-primary), status dot (w-1.5 h-1.5 animate-blink + accent dot color). Separator: border-b border-gdf-border-subtle pb-4 mb-5.

    Corner brackets (when bracketed=true): use the `.gdf-glass-bracketed` CSS utility class from globals.css (added in plan 01-01). This class provides ::before/::after pseudo-elements with 12px cyan border accents.

    Use `cn()` from @/lib/utils for className merging. Wrap in relative container.

    **TacticalButton.tsx** — Drop-in replacement for cyber/GlitchButton.tsx. Same props interface: children, variant ('primary'|'ghost'|'warning'|'danger'), all HTMLButtonElement attributes via rest spread. Same variant names preserved (warning not danger-rename).

    Variant styles consuming gdf-* tokens:
    - primary: `bg-gdf-accent-primary text-gdf-text-inverse hover:bg-gdf-accent-primary-dim active:scale-[0.98]`
    - warning: `bg-gdf-status-warning text-gdf-text-inverse hover:brightness-110 active:scale-[0.98]`
    - ghost: `bg-transparent border border-gdf-border-default text-gdf-text-secondary hover:border-gdf-accent-primary hover:text-gdf-accent-primary`
    - danger: `bg-transparent border border-gdf-status-danger/30 text-gdf-status-danger hover:bg-gdf-status-danger/10 hover:border-gdf-status-danger`

    Base classes on all variants: `font-mono text-xs tracking-wider uppercase px-5 py-2.5 rounded-md transition-all duration-150 disabled:opacity-40 disabled:cursor-not-allowed disabled:scale-100`

    Do NOT use `rounded-none` (brutalist). Use `rounded-md` (4px — gdf-radius-md). Do NOT use `shadow-glow` or inline box-shadows — let the glass background provide depth.

    Both components: named export, no default export.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tactical/GlassPanel.tsx` exists
    - `src/components/tactical/TacticalButton.tsx` exists
    - `grep -c "export function GlassPanel" src/components/tactical/GlassPanel.tsx` returns 1 (named export)
    - `grep -c "export function TacticalButton" src/components/tactical/TacticalButton.tsx` returns 1 (named export)
    - `grep -c "backdrop-blur-glass" src/components/tactical/GlassPanel.tsx` returns >= 1 (glass blur class used)
    - `grep -c "gdf-accent-primary" src/components/tactical/TacticalButton.tsx` returns >= 1 (consumes new tokens)
    - `grep -c "rounded-md" src/components/tactical/TacticalButton.tsx` returns >= 1 (not brutalist rounded-none)
    - `grep -c "AccentColor" src/components/tactical/GlassPanel.tsx` returns >= 1 (accent type defined)
    - `grep -c "'amber'" src/components/tactical/GlassPanel.tsx` returns >= 1 (purple→amber rename)
    - `grep -c "gdf-glass-bracketed" src/components/tactical/GlassPanel.tsx` returns >= 1 (bracketed support)
    - `grep -c "HTMLButtonElement" src/components/tactical/TacticalButton.tsx` returns >= 1 (extends button attrs)
    - `grep -c "default export" src/components/tactical/GlassPanel.tsx` returns 0 (no default export)
    - `grep -c "default export" src/components/tactical/TacticalButton.tsx` returns 0 (no default export)
    - `pnpm run build` exits 0
  </acceptance_criteria>
  <done>GlassPanel and TacticalButton created with gdf-* token consumption, backdrop-filter blur, preserved props interfaces</done>
</task>

<task type="auto">
  <name>Task 2: Restyle StatusBadge in-place and create HoloLoader</name>
  <files>src/components/cyber/StatusBadge.tsx, src/components/tactical/HoloLoader.tsx, src/shared/lib/motion.ts</files>
  <read_first>
    - src/components/cyber/StatusBadge.tsx (full 35 lines — STATUS_CLASSES, DOT_CLASSES patterns)
    - src/components/cyber/ScreenLoader.tsx (full 23 lines — current spinner structure)
    - .planning/phases/01-ui-redesign/01-RESEARCH.md (Pattern 3: HoloLoader not explicitly in research, but ScreenLoader replacement spec says "holographic loading indicator with scanner sweep")
    - src/app/styles/tokens.css (gdf-* status colors to use)
    - tailwind.config.js (verify gdf.status.* classes are available)
  </read_first>
  <action>
    **StatusBadge.tsx** — Restyle in-place. Keep the same component interface: status: string, variant: 'red' | 'amber' | 'green', className: string. All 11 import sites use it with these props.

    New STATUS_CLASSES consuming gdf-*:
    - red: `border-gdf-status-danger/30 text-gdf-status-danger`
    - amber: `border-gdf-status-warning/30 text-gdf-status-warning`
    - green: `border-gdf-status-success/30 text-gdf-status-success`

    New DOT_CLASSES:
    - red: `bg-gdf-status-danger`
    - amber: `bg-gdf-status-warning`
    - green: `bg-gdf-status-success`

    Base classes: `inline-flex items-center gap-1.5 border px-2.5 py-0.5 font-mono-sm font-bold uppercase tracking-[0.1em] bg-gdf-surface-overlay/50 rounded-md`

    Change `rounded-none` (brutalist) → `rounded-md`. Change `bg-zinc-900/50` → `bg-gdf-surface-overlay/50`. Keep `animate-blink` on the dot. Keep `font-mono-sm`.

    **HoloLoader.tsx** — Create in src/components/tactical/. Replaces ScreenLoader with holographic aesthetic. Same usage pattern: no props, self-closing `<HoloLoader />`.

    Structure:
    - Fixed overlay: `fixed inset-0 z-50 flex flex-col items-center justify-center bg-gdf-surface-root`
    - Outer ring: 48px cyan spinning ring (`border-2 border-transparent border-t-gdf-accent-secondary animate-spin`, animationDuration 0.8s)
    - Inner ring: 36px counter-spinning (`border border-transparent border-b-gdf-accent-primary animate-spin`, animationDuration 1.2s, animationDirection reverse)
    - Center dot: `w-2 h-2 bg-gdf-accent-secondary animate-pulse rounded-full`
    - Scanner line across the rings: a horizontal `<div>` with `animate-scanner-sweep` (slow 3s sweep) — positioned absolutely, 2px height, bg linear-gradient from transparent through gdf-accent-secondary/20 to transparent, opacity 0.4
    - Text: `font-mono text-xs text-gdf-text-muted tracking-[0.2em] uppercase` with text "SYSTEM ONLINE" flanked by cyan `▸` characters with animate-blink
    - Use `rounded-full` on rings (not rounded-none square). The ScreenLoader had square borders.

    **motion.ts** — Create src/shared/lib/motion.ts for shared Framer Motion variants used across tactical/ components. This file does NOT exist yet (verified in RESEARCH.md §1.5). Create with these variants:
    - `fadeIn`: { hidden: { opacity: 0 }, visible: { opacity: 1, transition: { duration: 0.3 } } }
    - `slideUp`: { hidden: { opacity: 0, y: 8 }, visible: { opacity: 1, y: 0, transition: { duration: 0.35 } } }
    - `slideInRight`: { hidden: { opacity: 0, x: 12 }, visible: { opacity: 1, x: 0, transition: { duration: 0.3 } } }
    - `staggerContainer`: { hidden: { opacity: 0 }, visible: { opacity: 1, transition: { staggerChildren: 0.05 } } }
    - `staggerItem`: { hidden: { opacity: 0, y: 8 }, visible: { opacity: 1, y: 0 } }
    - `modalEnter`: { hidden: { opacity: 0, scale: 0.95 }, visible: { opacity: 1, scale: 1, transition: { duration: 0.2 } } }
    - `scannerLine`: { hidden: { top: '-2px' }, visible: { top: '100%', transition: { duration: 3, repeat: Infinity, ease: 'linear' } } }
    All named exports. No default export. Add `/* eslint-disable react-refresh/only-export-components */` at top since motion variants aren't components.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    **StatusBadge:**
    - `grep -c "gdf-status-danger" src/components/cyber/StatusBadge.tsx` returns >= 1 (uses new tokens)
    - `grep -c "gdf-status-warning" src/components/cyber/StatusBadge.tsx` returns >= 1 (amber variant)
    - `grep -c "gdf-status-success" src/components/cyber/StatusBadge.tsx` returns >= 1 (green variant)
    - `grep -c "rounded-md" src/components/cyber/StatusBadge.tsx` returns >= 1 (soft corners)
    - `grep -c "bg-zinc-900" src/components/cyber/StatusBadge.tsx` returns 0 (no hardcoded zinc)
    - `grep -c "animate-blink" src/components/cyber/StatusBadge.tsx` returns >= 1 (dot blink preserved)
    - `grep -c "StatusBadgeProps" src/components/cyber/StatusBadge.tsx` returns >= 1 (interface preserved)

    **HoloLoader:**
    - `src/components/tactical/HoloLoader.tsx` exists
    - `grep -c "export function HoloLoader" src/components/tactical/HoloLoader.tsx` returns 1
    - `grep -c "gdf-accent-secondary" src/components/tactical/HoloLoader.tsx` returns >= 1 (cyan accent)
    - `grep -c "SYSTEM ONLINE" src/components/tactical/HoloLoader.tsx` returns >= 1 (text matches tactical theme)
    - `grep -c "rounded-full" src/components/tactical/HoloLoader.tsx` returns >= 1 (soft circles)

    **motion.ts:**
    - `src/shared/lib/motion.ts` exists
    - `grep -c "export const fadeIn" src/shared/lib/motion.ts` returns 1
    - `grep -c "export const staggerContainer" src/shared/lib/motion.ts` returns 1
    - `grep -c "export const modalEnter" src/shared/lib/motion.ts` returns 1
    - `grep -c "default export" src/shared/lib/motion.ts` returns 0

    **Build:**
    - `pnpm run build` exits 0

</acceptance_criteria>
<done>StatusBadge restyled with gdf-\* tokens, HoloLoader created with holographic spinner, motion.ts created with shared variants</done>
</task>

<task type="auto">
  <name>Task 3: Verify component rendering with visual smoke test</name>
  <files>src/components/tactical/index.ts</files>
  <read_first>
    - src/components/tactical/GlassPanel.tsx (Task 1 output)
    - src/components/tactical/TacticalButton.tsx (Task 1 output)
    - src/components/tactical/HoloLoader.tsx (Task 2 output)
    - src/components/cyber/StatusBadge.tsx (Task 2 output)
  </read_first>
  <action>
    Create barrel export `src/components/tactical/index.ts`:
    ```typescript
    export { GlassPanel } from './GlassPanel';
    export { TacticalButton } from './TacticalButton';
    export { HoloLoader } from './HoloLoader';
    ```

    Add the word "HoloLoader" to cspell.json custom words array if not present (it's a new compound word).

    Verify all components are importable by running a quick typecheck:
    ```bash
    npx tsc --noEmit src/components/tactical/index.ts 2>&1
    ```

    Run `pnpm run build` to ensure all components compile and tree-shake correctly. The build must succeed — these new components aren't imported by any pages yet, so they'll be tree-shaken from the bundle but must not cause compilation errors.

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tactical/index.ts` exists with 3 named re-exports
    - `pnpm run build` exits 0
    - `pnpm run lint` exits 0 (no ESLint errors in new files)
    - `grep -c "HoloLoader" cspell.json` returns >= 1 (spell check passes)
  </acceptance_criteria>
  <done>All 4 components barrel-exported, build passes, spell check updated</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                             |
| -------- | --------------------------------------------------------------------------------------- |
| N/A      | Pure presentational components — no data processing, no network, no user input storage. |

## STRIDE Threat Register

| Threat ID | Category  | Component                       | Disposition | Mitigation Plan                                                                           |
| --------- | --------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-01-02   | Spoofing  | GlassPanel (children rendering) | accept      | React's built-in JSX escaping prevents XSS via children. No dangerouslySetInnerHTML used. |
| T-01-SC   | Tampering | npm/pip/cargo installs          | accept      | No new packages installed. All dependencies pre-existing.                                 |

</threat_model>

<verification>
```bash
pnpm check    # lint + spell + build — must pass
```
</verification>

<success_criteria>

- 4 components created/restyled: GlassPanel, TacticalButton, StatusBadge, HoloLoader
- All consume --gdf-\* CSS variables (no hardcoded zinc/red/oklch colors)
- Props interfaces match originals for drop-in replacement compatibility
- glass backdrop-filter blur active (not solid backgrounds)
- Build passes with zero errors
  </success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-02-SUMMARY.md` when done
</output>
