---

phase: 01-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:

- src/layouts/AppShell.tsx
- docs/DESIGN_SYSTEM.md
- docs/AGENT.md
  autonomous: true
  requirements: [RNF-03, RNF-04]

must_haves:
truths: - "AppShell sidebar renders with glass panel styling and cyan active indicators" - "Header shows server clock with glass backdrop-blur" - "Camp selector dropdown uses gdf-\* styled select element" - "Documentation reflects new holographic tactical OS direction"
artifacts: - path: "src/layouts/AppShell.tsx"
provides: "Glass-styled application shell with cyan accents"
contains: "gdf-glass" - path: "docs/DESIGN_SYSTEM.md"
provides: "Updated visual design documentation for tactical OS"
contains: "holographic" - path: "docs/AGENT.md"
provides: "Updated agent context for tactical command center"
contains: "tactical"
key_links: - from: "AppShell.tsx sidebar"
to: "NavLink isActive styles"
via: "gdf-accent-primary for active state, gdf-surface-raised for sidebar bg"
pattern: "gdf-" - from: "AppShell.tsx"
to: "useEmotionalSyncer"
via: "emotional state attribute on main content area"
pattern: "useEmotionalSyncer"
</objective>

<objective>
Migrate the AppShell layout shell from brutalist to glass tactical OS aesthetic. Replace all hardcoded zinc-800 borders, brand-primary red accents, and solid surface backgrounds with gdf-* glass tokens and cyan accents. Integrate the emotional UI syncer into AppShell so the `data-emotional-state` attribute is applied to the main content area. Update project documentation to reflect the new visual direction. Keep all business logic, routing, and auth intact.

Purpose: AppShell is the visual foundation of every authenticated page. Until it's migrated, no page-level migration can be visually verified in context.
Output: Restyled AppShell.tsx, updated DESIGN_SYSTEM.md, updated AGENT.md
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md §7.1 (migration sequence: AppShell is Wave 3)
@.planning/phases/01-ui-redesign/01-CONTEXT.md (lines 77-85: layout spec, lines 206-220: navigation spec)
@.planning/codebase/ARCHITECTURE.md (AppShell component hierarchy, route structure)
@.planning/codebase/CONVENTIONS.md (AGENTS.md conventions, component patterns)
@AGENTS.md

<interfaces>
<!-- Current AppShell.tsx (215 lines) — all hardcoded zinc colors to replace -->
From src/layouts/AppShell.tsx:
- Sidebar: `bg-surface-raised border-r border-zinc-800` (line 164)
- Header: `bg-surface-base/80 backdrop-blur-sm border-b border-zinc-800` (line 171)
- Logo: `bg-brand-primary` → needs `bg-gdf-accent-primary`, text `text-surface-base` → `text-gdf-text-inverse`
- NavLink active: `bg-zinc-800 text-brand-primary border-r-2 border-brand-primary` (line 85)
- NavLink inactive: `text-zinc-400 border-r-2 border-transparent hover:text-zinc-200 hover:bg-zinc-800/50` (line 86)
- Camp selector: `bg-surface-base border border-zinc-700 text-zinc-300 ... focus:border-brand-primary` (line 107)
- User avatar: `bg-zinc-800`, initial `text-brand-secondary` (lines 120-121)
- Logout button: `text-zinc-500 hover:text-brand-primary hover:bg-zinc-800/50` (line 150)
- Section borders: `border-zinc-800` throughout (lines 60, 97, 119, 147)
- Header text: `text-zinc-500`, `text-zinc-400`, `text-zinc-600` (lines 180-198)
- Status dot: `bg-brand-accent animate-pulse` (line 194)
- Status text: `text-brand-accent` (line 195)
- Sidebar collapse button: `text-zinc-600 hover:text-zinc-300` (line 138)
- Mobile Sheet: `bg-surface-raised border-r border-zinc-800` (line 209)

<!-- Emotional syncer import path -->

From src/features/ui/index.ts (created in plan 01-03):

```typescript
export { useEmotionalSyncer } from './hooks/useEmotionalSyncer';
```

<!-- Theme toggle already in App.tsx (plan 01-03) — no need to add here -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AppShell with glass tactical OS aesthetic</name>
  <files>src/layouts/AppShell.tsx</files>
  <read_first>
    - src/layouts/AppShell.tsx (full 215 lines — every hardcoded zinc class must be replaced)
    - src/app/styles/tokens.css (gdf-* variable values for surface, text, border, accent)
    - tailwind.config.js (verify gdf.* Tailwind classes are available)
    - src/features/ui/index.ts (useEmotionalSyncer import path)
    - .planning/phases/01-ui-redesign/01-CONTEXT.md (lines 206-220: navigation spec with glass sidebar + cyan active indicator)
  </read_first>
  <action>
    Import `useEmotionalSyncer` from `@/features/ui` at the top of AppShell.tsx alongside existing imports. Call `useEmotionalSyncer(activeCamp?.id ?? null)` inside the component function (after all existing hooks). The syncer sets `data-emotional-state` on document.documentElement.

    Replace ALL hardcoded color classes in AppShell.tsx. Here is the complete mapping:

    **Sidebar container** (line 164):
    - `bg-surface-raised` → `bg-gdf-surface-raised backdrop-blur-glass border-r border-gdf-glass-border`
    - `border-zinc-800` → `border-gdf-glass-border`
    - Keep `transition-all duration-200` and collapse width logic

    **Logo area** (lines 60-72):
    - Section border: `border-b border-zinc-800` → `border-b border-gdf-glass-border`
    - Logo square: `bg-brand-primary` → `bg-gdf-accent-primary`
    - Logo text GF: `text-surface-base` → `text-gdf-text-inverse`
    - "END TIMES / MGMT" text: `text-zinc-400` → `text-gdf-text-muted`

    **Navigation items** (lines 74-95):
    - Active NavLink: `bg-zinc-800 text-brand-primary border-r-2 border-brand-primary` → `bg-gdf-surface-hover text-gdf-accent-primary border-r-2 border-gdf-accent-secondary` (cyan border on active)
    - Inactive NavLink: `text-zinc-400 border-r-2 border-transparent hover:text-zinc-200 hover:bg-zinc-800/50` → `text-gdf-text-muted border-r-2 border-transparent hover:text-gdf-text-secondary hover:bg-gdf-surface-hover/50`
    - Keep `font-mono font-bold uppercase tracking-wider text-xs`

    **Camp selector section** (lines 97-117):
    - Section border: `border-t border-zinc-800` → `border-t border-gdf-glass-border`
    - Label: `text-zinc-500` → `text-gdf-text-muted`
    - Select: `bg-surface-base border border-zinc-700 text-zinc-300 ... focus:border-brand-primary` → `bg-gdf-surface-base border border-gdf-border-subtle text-gdf-text-secondary font-mono text-[11px] py-1.5 px-2 focus:border-gdf-accent-primary outline-none`
    - Keep `outline-none`, don't add rounded-md (glass inputs handle rounding at a higher level)

    **User info section** (lines 119-145):
    - Section border: `border-t border-zinc-800` → `border-t border-gdf-glass-border`
    - Avatar bg: `bg-zinc-800` → `bg-gdf-surface-hover`
    - Avatar initial: `text-brand-secondary` → `text-gdf-accent-secondary` (cyan)
    - Username: `text-zinc-300` → `text-gdf-text-primary`
    - Role: `text-zinc-500` → `text-gdf-text-muted`
    - Collapse button: `text-zinc-600 hover:text-zinc-300` → `text-gdf-text-muted hover:text-gdf-text-secondary`

    **Logout button** (lines 147-155):
    - Section border: `border-t border-zinc-800` → `border-t border-gdf-glass-border`
    - Button: `text-zinc-500 hover:text-brand-primary hover:bg-zinc-800/50` → `text-gdf-text-muted hover:text-gdf-status-danger hover:bg-gdf-surface-hover/50`
    - Keep `transition-colors uppercase tracking-wider`

    **Header bar** (lines 171-200):
    - Container: `bg-surface-base/80 backdrop-blur-sm border-b border-zinc-800` → `bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy border-b border-gdf-glass-border`
    - Left text: `text-zinc-600` and `text-zinc-500` → `text-gdf-text-muted`
    - Left icon LayoutGrid: `text-zinc-600` → `text-gdf-text-muted`
    - "OPERATIONAL SECTOR 04 // ONLINE": keep text, change `text-zinc-500` → `text-gdf-text-muted`
    - Server time: `text-zinc-400` → `text-gdf-text-secondary`, icon `text-zinc-500` → `text-gdf-text-muted`
    - Status dot: `bg-brand-accent animate-pulse` → `bg-gdf-status-success animate-breathe` (use new breathe animation from plan 01-01 tailwind config)
    - Status text: `text-brand-accent` → `text-gdf-status-success`
    - Change "NOMINAL SYSTEM" → "SYSTEM NOMINAL" for clearer military brevity

    **Mobile Sheet** (lines 207-212):
    - `bg-surface-raised border-r border-zinc-800` → `bg-gdf-surface-raised border-r border-gdf-glass-border`

    **Main content area** (line 202):
    - `bg-surface-base` → `bg-transparent` (background is now provided by TacticalBackground component from App.tsx)
    - The main area should be transparent so the dynamic background shows through. Glass panels on individual pages provide their own backgrounds.

    **What NOT to change:**
    - All business logic: handleCampChange, handleLogout, query invalidation
    - All hook calls: useAuth, useAuthStore, useNavItems, useCamps, useCampStore, useServerTime, useIsMobile
    - All component structure: sidebar content, Sheet, Outlet
    - Import paths (except adding useEmotionalSyncer)
    - The server time display logic
    - Collapse/expand logic
    - Mobile detection logic
    - Any `onClick` handlers
    - All `font-mono`, `font-sans`, `font-mono-data` class usage — these are preserved
    - The `showLabels` logic

  </action>
  <verify>
    <automated>pnpm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build` exits 0
    - `grep -c "useEmotionalSyncer" src/layouts/AppShell.tsx` returns >= 1 (emotional hook integrated)
    - `grep -c "gdf-glass" src/layouts/AppShell.tsx` returns >= 2 (glass styling applied to sidebar + header)
    - `grep -c "gdf-accent-primary" src/layouts/AppShell.tsx` returns >= 1 (cyan accent on active nav)
    - `grep -c "gdf-accent-secondary" src/layouts/AppShell.tsx` returns >= 1 (cyan on user avatar initial)
    - `grep -c "gdf-status-danger" src/layouts/AppShell.tsx` returns >= 1 (logout hover danger styling)
    - `grep -c "bg-transparent" src/layouts/AppShell.tsx` returns >= 1 (main content transparent for background)
    - `grep -c "border-zinc-800" src/layouts/AppShell.tsx` returns 0 (all hardcoded zinc removed)
    - `grep -c "text-zinc-" src/layouts/AppShell.tsx` returns 0 (all hardcoded zinc text removed)
    - `grep -c "bg-brand-primary" src/layouts/AppShell.tsx` returns 0 (red logo removed)
    - `grep -c "text-brand-primary" src/layouts/AppShell.tsx` returns 0 (red text removed)
    - `grep -c "text-brand-secondary" src/layouts/AppShell.tsx` returns 0 (amber text removed)
    - `grep -c "text-brand-accent" src/layouts/AppShell.tsx` returns 0 (green text removed)
    - `grep -c "bg-brand-accent" src/layouts/AppShell.tsx` returns 0 (green dot removed)
    - `grep -c "SYSTEM NOMINAL" src/layouts/AppShell.tsx` returns >= 1 (header text updated)
    - `grep -c "animate-breathe" src/layouts/AppShell.tsx` returns >= 1 (new breathe animation on status dot)
    - All import statements intact (verify count unchanged from original)
    - All business logic functions unchanged: handleCampChange, handleLogout
  </acceptance_criteria>
  <done>AppShell fully restyled with glass panels, cyan accents, emotional UI syncer, and transparent content area</done>
</task>

<task type="auto">
  <name>Task 2: Update project documentation to new tactical OS direction</name>
  <files>docs/DESIGN_SYSTEM.md, docs/AGENT.md</files>
  <read_first>
    - docs/DESIGN_SYSTEM.md (full file — update the cyberpunk neon doc to holographic tactical OS)
    - docs/AGENT.md (full file — update phosphor CRT agent context)
    - .planning/phases/01-ui-redesign/01-CONTEXT.md (lines 28-33: aesthetic direction, anti-inspirations)
    - src/app/styles/tokens.css (token names and values to document)
    - AGENTS.md (project conventions — don't change this file, it's authoritative)
  </read_first>
  <action>
    **docs/DESIGN_SYSTEM.md** — Rewrite to reflect the new holographic tactical OS design system:

    Replace the title and opening: `# Design System — Gestión del Fin · Tactical Command Interface`

    New sections:
    1. **Visual Philosophy:** "Holographic tactical OS — alive, reactive, atmospheric, elegant, technological. NOT retro terminal, NOT cyberpunk RGB, NOT Fallout green CRT."
    2. **Color Palette:** Document the gdf-* token system with dark + light mode pairs. Include table: Token | Dark | Light | Usage
    3. **Typography:** Inter for UI text and headings (400-900 weights). JetBrains Mono for data/metrics (400-700 weights). NO retro pixel fonts, NO CRT typefaces, NO Orbitron/Rajdhani.
    4. **Glassmorphism:** backdrop-filter blur 16-24px, RGBA backgrounds, subtle borders. Include browser compatibility note (96.3% support, @supports fallback).
    5. **Dynamic Background:** CSS gradient waves, cursor-reactive radial glow, scanner sweep, light particles (deferred). Performance budget: GPU-composited, 60fps target.
    6. **Emotional UI States:** stable (cyan/blue, slow breathing), alert (amber pulses, faster scanners), critical (red accents, subtle glitch overlay). Document data-emotional-state attribute.
    7. **Components:** GlassPanel, TacticalButton, StatusBadge, HoloLoader — with props interfaces and usage examples.
    8. **Theme:** Dark mode default, light mode via toggle, localStorage key 'gdf.theme', prefers-color-scheme honored for first visit.

    Remove all sections describing: cyberpunk neon system, fuchsia/cyan neon variables, phosphor CRT scanlines, Orbitron/Rajdhani fonts, CRT effects, wave blob backgrounds, cyber-grid overlays, brutalist borders.

    **docs/AGENT.md** — Rewrite to describe the tactical command center:
    - Change title from "AGENT CONTEXT — Gestión del Fin" to "AGENT CONTEXT — Gestión del Fin · Tactical Command Interface"
    - Replace "phosphor green terminal" language with "holographic tactical command center"
    - Replace CRT/terminal references with tactical OS references
    - Keep all technical specs that are still accurate (stack, tooling, architecture references)
    - Add note: "This document was updated during Phase 01 Tactical UI Redesign (2026-05-23)"

    **Do NOT change:** AGENTS.md (project root — authoritative, stays as-is). The DESIGN_SYSTEM.md and AGENT.md are descriptive docs showing current design direction; AGENTS.md is prescriptive for agents.

  </action>
  <verify>
    <automated>pnpm run spell 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    **DESIGN_SYSTEM.md:**
    - `grep -c "holographic\|tactical" docs/DESIGN_SYSTEM.md` returns >= 2 (new aesthetic documented)
    - `grep -c "gdf-" docs/DESIGN_SYSTEM.md` returns >= 3 (token system documented)
    - `grep -c "Orbitron\|Rajdhani\|CRT\|phosphor\|scanlines" docs/DESIGN_SYSTEM.md` returns 0 (old references removed)
    - `grep -c "glassmorphism\|backdrop-filter" docs/DESIGN_SYSTEM.md` returns >= 1 (glass documented)
    - `grep -c "emotional\|data-emotional-state" docs/DESIGN_SYSTEM.md` returns >= 1 (emotional UI documented)

    **AGENT.md:**
    - `grep -c "holographic\|tactical\|command" docs/AGENT.md` returns >= 1 (new aesthetic)
    - `grep -c "phosphor\|CRT\|terminal green" docs/AGENT.md` returns 0 (old aesthetic removed)
    - `grep -c "Phase 01" docs/AGENT.md` returns >= 1 (update note present)

    **Build:**
    - `pnpm run spell` exits 0 (no new spelling errors from doc changes)

</acceptance_criteria>
<done>DESIGN_SYSTEM.md and AGENT.md rewritten for holographic tactical OS direction; old cyberpunk/CRT references removed</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                 |
| -------- | ------------------------------------------------------------------------------------------- |
| N/A      | AppShell is a layout component — no new trust boundaries. Documentation is static markdown. |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-01-05   | Information Disclosure | AppShell (emotional state data) | accept      | Emotional state is derived from already-visible TanStack Query data (stock levels, people counts). No new information exposed. |
| T-01-SC   | Tampering              | npm/pip/cargo installs          | accept      | No new packages installed.                                                                                                     |

</threat_model>

<verification>
```bash
pnpm check    # lint + spell + build — must pass
```
</verification>

<success_criteria>

- AppShell renders with glass sidebar + header, cyan active nav indicators
- All hardcoded zinc/red/amber color classes removed from AppShell
- Emotional state syncer integrated into AppShell
- Documentation reflects new holographic tactical OS design system
- Build passes, all routes render, no business logic changed
  </success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-04-SUMMARY.md` when done
</output>
