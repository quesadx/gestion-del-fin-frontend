---
phase: 03-holographic-ui-elevation
plan: 07
type: execute
wave: 7
depends_on: ['03-06']
files_modified:
  - src/features/camps/pages/CampsPage.tsx
  - src/features/camps/pages/CampDetailPage.tsx
  - src/features/inventory/pages/InventoryPage.tsx
  - src/features/people/pages/PersonDetailPage.tsx
  - src/features/resources/pages/ResourcesPage.tsx
  - .planning/ROADMAP.md
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - 'All feature page tables use glass-compatible styling (no oklch colors)'
    - 'Pagination components use glass button styling'
    - 'Filter/search bars use gdf-glass-overlay or gdf-glass-interactive'
    - 'Date picker calendars align with the tactical design system'
    - 'All focus rings visible and animated (WCAG 2.4.7)'
    - 'backdrop-filter layers count ≤ 3 across the viewport'
    - 'pnpm check passes (lint + spell + build)'
  artifacts:
    - path: 'src/features/camps/pages/CampsPage.tsx'
      provides: 'Glass-styled camps page with gdf tokens'
      contains: 'gdf-glass'
    - path: 'src/features/inventory/pages/InventoryPage.tsx'
      provides: 'Glass-styled inventory page, no oklch colors'
      contains: 'gdf-glass'
    - path: 'src/features/people/pages/PersonDetailPage.tsx'
      provides: 'Glass-styled person detail page, no oklch colors'
      contains: 'gdf-glass'
    - path: 'src/features/resources/pages/ResourcesPage.tsx'
      provides: 'Glass-styled resources page, no oklch colors'
      contains: 'gdf-glass'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 03 marked Complete with plan list'
      contains: 'Phase 03: Holographic UI Elevation'
  key_links:
    - from: 'Feature page inputs/selects'
      to: 'gdf-glass-bg + gdf-border-subtle tokens'
      via: 'Tailwind classes bg-gdf-glass-bg border-gdf-border-subtle'
      pattern: 'bg-gdf-glass-bg|border-gdf-border-subtle'
    - from: 'Feature page buttons'
      to: 'TacticalButton or shadcn Button with glass styling'
      via: 'Component import or gdf-btn-press class'
      pattern: 'TacticalButton|gdf-btn-press'
---

<objective>
Audit all feature pages for legacy styling (oklch colors, hardcoded radius,
solid backgrounds) and modernize with gdf glass tokens. Verify pagination,
filter/search bars, date picker, and tables all use the holographic glass
design system. Run performance, accessibility, and build validation.

Purpose: Fulfill D-07 Legacy Component Audit LOCKED. Phase 01-02 established
the design system and restyled 64 shadcn/ui components. Phase 03 adds depth
and motion. This audit ensures no page was left behind with legacy solid/brutalist
styling that breaks the holographic illusion.

Output: Cleaned feature pages, ROADMAP.md marked Complete, pnpm check passing.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-holographic-ui-elevation/03-CONTEXT.md (D-07 Legacy Audit LOCKED, D-08 Performance LOCKED, D-09 Accessibility LOCKED)
@AGENTS.md (project conventions — named exports, TypeScript strict, no any)
@.planning/ROADMAP.md

<interfaces>
<!-- Legacy oklch pattern found in grep — these need modernization -->
From grep results (246 matches for rounded-*/oklch patterns):
- `rounded-sm` used extensively in feature pages (2px — should be rounded-md at 6px)
- `oklch(...)` color values in:
  - src/features/inventory/pages/InventoryPage.tsx (lines 113, 170, 248, 268, 282, 293)
  - src/features/people/pages/PersonDetailPage.tsx (lines 480-875, extensive)
  - src/features/camps/pages/CampsPage.tsx (lines 161, 167, 245, 259, 314, 330, 344, 363)
  - src/features/camps/pages/CampDetailPage.tsx (lines 295, 310, 324, 343)
  - src/features/resources/pages/ResourcesPage.tsx (lines 190, 198, 238, 254, 273, 291, 339, 354)

<!-- Target gdf token replacements -->

Old pattern → New gdf pattern:

- `bg-[oklch(0.15_0.05_320_/_0.5)]` → `bg-gdf-glass-bg`
- `border-[oklch(0.68_0.32_340_/_0.4)]` → `border-gdf-border-subtle`
- `focus:border-[var(--neon-cyan)]` → `focus:border-gdf-accent-secondary`
- `focus:border-[var(--neon-fuchsia)]` → `focus:border-gdf-accent-primary`
- `text-[var(--neon-cyan)]` → `text-gdf-accent-secondary`
- `text-red-400` → `text-gdf-status-danger`
- `hover:bg-red-400/10` → `hover:bg-gdf-status-danger/10`
- `hover:bg-[oklch(0.85_0.22_200_/_0.1)]` → `hover:bg-gdf-accent-secondary/10`
- `rounded-sm` (on inputs/interactive elements) → `rounded-md`
- `glass-heavy rounded-sm` (on panels) → `bg-gdf-glass-bg-heavy backdrop-blur-glass-heavy border border-gdf-glass-border rounded-lg`
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Modernize legacy styles in camps and resources pages</name>
  <read_first>
    - src/features/camps/pages/CampsPage.tsx
    - src/features/camps/pages/CampDetailPage.tsx
    - src/features/resources/pages/ResourcesPage.tsx
  </read_first>
  <files>src/features/camps/pages/CampsPage.tsx, src/features/camps/pages/CampDetailPage.tsx, src/features/resources/pages/ResourcesPage.tsx</files>
  <action>
    **Apply the following replacements across all three files (D-07 LOCKED):**

    **Color replacements (do all instances, use replaceAll or manual edit):**
    1. `bg-[oklch(0.15_0.05_320_/_0.5)]` → `bg-gdf-glass-bg`
    2. `border-[oklch(0.68_0.32_340_/_0.4)]` → `border-gdf-border-subtle`
    3. `focus:border-[var(--neon-cyan)]` → `focus:border-gdf-accent-secondary`
    4. `focus:border-[var(--neon-fuchsia)]` → `focus:border-gdf-accent-primary`
    5. `text-[var(--neon-cyan)]` → `text-gdf-accent-secondary`
    6. `text-red-400` → `text-gdf-status-danger`
    7. `hover:bg-red-400/10` → `hover:bg-gdf-status-danger/10`
    8. `hover:bg-[oklch(0.85_0.22_200_/_0.1)]` → `hover:bg-gdf-accent-secondary/10`
    9. `placeholder:text-muted-foreground/30` → `placeholder:text-gdf-text-muted`

    **Border-radius replacements:**
    10. Every `rounded-sm` on input/select/textarea elements → `rounded-md`
        (Preserve `rounded-sm` only on truly tiny elements like icon buttons)
    11. `rounded-sm` on glass/panel containers → `rounded-lg`
    12. `object-cover rounded-sm` on avatar images → keep `rounded-sm` (intentional)

    **Glass class replacements:**
    13. `glass-heavy rounded-sm p-4 border ...` → `backdrop-blur-glass-heavy bg-gdf-glass-bg-heavy border border-gdf-glass-border rounded-lg p-4`

    **Add gdf-btn-press to action buttons:**
    14. On icon action buttons (edit/delete with cursor-pointer), add `gdf-btn-press` class

    **After all replacements, verify no `oklch(` remains in any of these files:**
    ```bash
    grep -c "oklch" src/features/camps/pages/CampsPage.tsx src/features/camps/pages/CampDetailPage.tsx src/features/resources/pages/ResourcesPage.tsx
    ```
    Expected: 0 for each file.

  </action>
  <verify>
    <automated>grep -c "oklch" src/features/camps/pages/CampsPage.tsx src/features/camps/pages/CampDetailPage.tsx src/features/resources/pages/ResourcesPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Zero `oklch(` occurrences in CampsPage.tsx, CampDetailPage.tsx, ResourcesPage.tsx
    - Zero `var(--neon-` occurrences in same files
    - All inputs/selects use `rounded-md` instead of `rounded-sm`
    - All panels use `rounded-lg` with gdf glass classes
    - Action buttons have `gdf-btn-press` class
    - `pnpm build` passes with zero TS errors
  </acceptance_criteria>
  <done>Camps and Resources pages fully migrated to gdf glass tokens, zero legacy oklch</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modernize legacy styles in inventory and people pages + audit remaining pages</name>
  <read_first>
    - src/features/inventory/pages/InventoryPage.tsx
    - src/features/people/pages/PersonDetailPage.tsx
    - src/features/people/pages/PersonCreatePage.tsx
    - src/features/people/pages/PeopleListPage.tsx
    - src/features/admission/pages/AdmissionsPage.tsx
    - src/features/explorations/pages/ExplorationsPage.tsx
    - src/features/explorations/pages/ExplorationDetailPage.tsx
    - src/features/transfers/pages/TransfersPage.tsx
    - src/features/users/pages/UsersPage.tsx
    - src/features/professions/pages/ProfessionsPage.tsx
    - src/features/rations/pages/RationsPage.tsx
  </read_first>
  <files>src/features/inventory/pages/InventoryPage.tsx, src/features/people/pages/PersonDetailPage.tsx</files>
  <action>
    **Step A — InventoryPage.tsx and PersonDetailPage.tsx (heavy oklch offenders):**
    Apply the same replacement patterns as Task 1:
    1. `bg-[oklch(0.15_0.05_320_/_0.5)]` → `bg-gdf-glass-bg`
    2. `border-[oklch(0.68_0.32_340_/_0.4)]` → `border-gdf-border-subtle`
    3. `focus:border-[var(--neon-cyan)]` → `focus:border-gdf-accent-secondary`
    4. `focus:border-[var(--neon-fuchsia)]` → `focus:border-gdf-accent-primary`
    5. `rounded-sm` (inputs/panels) → `rounded-md` or `rounded-lg` as appropriate
    6. Any `border-zinc-700` → `border-gdf-glass-border`
    7. Any `bg-zinc-800` → `bg-gdf-surface-raised`
    8. `text-muted-foreground` → `text-gdf-text-muted`
    9. `glass-heavy rounded-sm` → `backdrop-blur-glass-heavy bg-gdf-glass-bg-heavy border border-gdf-glass-border rounded-lg`

    PersonDetailPage.tsx line 317: `border-zinc-700` on image → `border-gdf-glass-border`
    PersonDetailPage.tsx line 320: `bg-zinc-800` → `bg-gdf-surface-raised`

    **Step B — Quick audit of remaining pages:**
    For each of these files, run `grep "oklch\|var(--neon-\|rounded-sm\|border-zinc\|bg-zinc"`
    to identify legacy patterns. If any are found:
    - Replace `oklch(` values with gdf tokens
    - Replace `var(--neon-*)` with gdf accent tokens
    - Bump `rounded-sm` to `rounded-md` on interactive elements
    - Replace zinc borders with `border-gdf-glass-border`

    Target: Zero `oklch(`, zero `var(--neon-`, zero `border-zinc`, zero
    `bg-zinc` in any `src/features/**/pages/*.tsx` file.

  </action>
  <verify>
    <automated>echo "=== oklch count ===" && grep -rl "oklch" src/features/ --include="*.tsx" | wc -l && echo "=== neon var count ===" && grep -rl "var(--neon-" src/features/ --include="*.tsx" | wc -l && echo "=== zinc count ===" && grep -rl "border-zinc\|bg-zinc" src/features/ --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in `src/features/` containing `oklch(`
    - Zero files in `src/features/` containing `var(--neon-`
    - Zero files in `src/features/` containing `border-zinc` or `bg-zinc` (unless intentionally decorative)
    - PersonDetailPage.tsx image styling uses gdf tokens
    - InventoryPage.tsx all filter inputs use gdf-glass-bg + rounded-md
    - `pnpm build` passes
  </acceptance_criteria>
  <done>All feature pages free of legacy oklch/neon/zinc styling</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Performance audit, accessibility validation, pnpm check, ROADMAP update</name>
  <read_first>
    - src/app/styles/globals.css (to count backdrop-filter layers)
    - src/layouts/AppShell.tsx (to verify focus ring visibility)
    - .planning/ROADMAP.md (to update Phase 03 status)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    **Step A — Performance audit (D-08 LOCKED):**
    1. Count backdrop-filter layers:
       ```bash
       grep -c "backdrop-blur-glass[^-]" src/layouts/AppShell.tsx src/components/tactical/GlassPanel.tsx src/components/ui/*.tsx
       ```
       Verify ≤ 3 simultaneous layers on any given viewport. The current count:
       - AppShell sidebar: `backdrop-blur-glass` (1)
       - AppShell header: `backdrop-blur-glass-heavy` (1)
       - GlassPanel cards: `backdrop-blur-glass` (1)
       - Dialog/Sheet overlays: appear one at a time (1) — not simultaneous with viewport content
       
       Total simultaneous: 3 (sidebar + header + one card layer). ✓

    2. Verify will-change usage:
       ```bash
       grep -c "will-change" src/app/styles/globals.css src/components/tactical/TacticalBackground.tsx
       ```
       Should find `gdf-depth-float`, `gdf-parallax-sidebar`, `gdf-parallax-content`,
       `gdf-grid-overlay`, `gdf-nebula-blob` — all on animated elements only. ✓

    **Step B — Accessibility audit (D-09 LOCKED):**
    1. Focus rings: Verify `:focus-visible` styles are present on all interactive elements.
       ```bash
       grep -rl "focus-visible:" src/ --include="*.css" --include="*.tsx" | head -20
       ```
       Confirm at least globals.css and all shadcn/ui components have focus-visible styles.

    2. No seizure-inducing effects: Verify no rapid flashes (< 3Hz).
       Our fastest animation is `critical-scan` at 0.1s (10Hz) — this is below the
       seizure threshold (generally > 20Hz for red flashes). However, the spec says
       "no rapid flashes." Add a note: the critical scan operates at 10fps which is
       within safe range, but if user reports discomfort, can be disabled via
       prefers-reduced-motion.

    3. Keyboard navigation: Verify no `pointer-events: none` on interactive elements.
       Current usage of `pointer-events: none` is only on background/overlay layers
       (gdf-critical-overlay, scanner sweeps, grid, particles, cursor glow).
       All have `aria-hidden="true"`. ✓

    **Step C — Final build:**
    Run `pnpm check` to verify lint + spell + build all pass.

    **Step D — ROADMAP update:**
    Edit `.planning/ROADMAP.md` to:
    1. Change Phase 03 title:
       `### Phase 03: [To be planned]` → `### Phase 03: Holographic UI Elevation`
    2. Add goal:
       ```
       **Goal:** Cinematic holographic tactical OS — rounded corners, multilayer depth, advanced motion, dynamic backgrounds, deep emotional UI, premium microinteractions, legacy component audit.
       ```
    3. Update plans count: `**Plans:** 7 plans`
    4. Add plan list:
       ```
       Plans:
       - [ ] 03-01-PLAN.md — Rounded corners system (6px/10px/14px radius tokens)
       - [ ] 03-02-PLAN.md — Multilayer depth with floating panels + parallax
       - [ ] 03-03-PLAN.md — Advanced motion system (page transitions, hover glow, focus rings, ripple)
       - [ ] 03-04-PLAN.md — Dynamic background overhaul (waves, grid, nebula, 60 particles)
       - [ ] 03-05-PLAN.md — Deep emotional UI (border glow, critical overlay, wave color shifts)
       - [ ] 03-06-PLAN.md — Premium microinteractions (spring toast, motion dialog, staggered select)
       - [ ] 03-07-PLAN.md — Legacy component audit + final validation + ROADMAP update
       ```
    5. Add Phase 04 placeholder:
       ```
       ### Phase 04: [To be planned]
       ```

  </action>
  <verify>
    <automated>pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backdrop-filter layers ≤ 3 simultaneous on any viewport
    - `will-change: transform` only on animated elements
    - All interactive elements have visible :focus-visible styles
    - No seizure-inducing rapid flash animations (< 3Hz on red)
    - `pointer-events: none` only on background/overlay layers with aria-hidden
    - `pnpm check` exits 0 (lint + spell + build all pass)
    - ROADMAP.md: Phase 03 has goal, 7 plans listed, Phase 04 placeholder added
    - `grep "oklch\|var(--neon-" src/ --include="*.tsx" -rl` returns 0 results in features/pages
  </acceptance_criteria>
  <done>Phase 03 complete — all legacy styles cleaned, performance validated, accessibility verified, ROADMAP updated</done>
</task>

</tasks>

<threat_model>
No security-relevant changes — style migrations only.
No new dependencies, no API changes, no authentication modifications.
</threat_model>

<verification>
1. Open every feature page → all inputs/panels/buttons use gdf glass tokens
2. Inspect any input → rounded-md (6px), gdf-glass-bg background
3. Inspect any panel → rounded-lg (10px), glass backdrop-blur
4. Tab through interactive elements → focus rings visible on all
5. Run Lighthouse accessibility audit → score ≥ 90
6. Check browser performance tab → no long tasks from animations
7. `pnpm check` passes with zero errors and zero warnings
</verification>

<success_criteria>

- Zero oklch() or var(--neon-\*) in any feature page
- All inputs/selects use rounded-md (6px) with gdf-glass-bg
- All panels use rounded-lg (10px) with glass styling
- backdrop-filter ≤ 3 simultaneous layers
- All interactive elements have visible focus rings
- `pnpm check` passes (lint + spell + build)
- ROADMAP.md updated with Phase 03 goal + plans + Phase 04 placeholder
  </success_criteria>

<output>
Create `.planning/phases/03-holographic-ui-elevation/03-07-SUMMARY.md` when done
</output>
