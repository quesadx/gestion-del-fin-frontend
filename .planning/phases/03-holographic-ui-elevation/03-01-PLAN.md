---
phase: 03-holographic-ui-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/styles/tokens.css
  - src/app/styles/globals.css
  - tailwind.config.js
autonomous: true
requirements: [RNF-03, RNF-04]

must_haves:
  truths:
    - 'Radius tokens resolve to correct pixel values when inspected in devtools'
    - 'All .gdf-glass-* utilities use CSS variable references (no hardcoded px)'
    - 'New --gdf-radius-xl token exists at 14px'
  artifacts:
    - path: 'src/app/styles/tokens.css'
      provides: '--gdf-radius-md: 6px, --gdf-radius-lg: 10px, --gdf-radius-xl: 14px'
      contains: '--gdf-radius-xl'
    - path: 'src/app/styles/globals.css'
      provides: 'Updated .gdf-glass-* border-radius to use var(--gdf-radius-lg) for overlays'
      contains: 'var(--gdf-radius-lg)'
    - path: 'tailwind.config.js'
      provides: 'Tailwind radius scale mapping for gdf radius tokens'
      contains: 'gdf-radius-xl'
  key_links:
    - from: 'src/app/styles/tokens.css :root block'
      to: 'All gdf-glass-* utilities in globals.css'
      via: 'CSS variable resolution'
      pattern: "var\\(--gdf-radius-"
    - from: 'tailwind.config.js borderRadius'
      to: 'All Tailwind rounded-* classes in components'
      via: 'Tailwind theme resolution'
      pattern: 'rounded-md|rounded-lg|rounded-xl'
---

<objective>
Update the CSS custom property radius scale, add a new --gdf-radius-xl token,
and ensure all .gdf-glass-* utility classes in globals.css reference the CSS
variables (not hardcoded Tailwind classes) so that future radius adjustments
propagate automatically.

Purpose: Establish the rounded corners design-token foundation that every
subsequent Phase 03 plan builds on. All panels, cards, inputs, and overlays
will inherit the new radius scale through CSS variable resolution.

Output: Updated tokens.css, globals.css, and tailwind.config.js with the
new 6px / 10px / 14px radius scale fully wired.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 03 goal)
@.planning/phases/03-holographic-ui-elevation/03-CONTEXT.md (LOCKED radius values: md=6px, lg=10px, xl=14px)
@AGENTS.md (project conventions)

<interfaces>
<!-- Current radius tokens from tokens.css:37-41 -->
From src/app/styles/tokens.css:
```css
--gdf-radius-none: 0;
--gdf-radius-sm: 2px;
--gdf-radius-md: 4px;    /* → must become 6px */
--gdf-radius-lg: 6px;    /* → must become 10px */
/* --gdf-radius-xl: 14px  ← NEW, must be added */
```

<!-- Current gdf-glass utility border-radius usage in globals.css -->

From src/app/styles/globals.css:

```css
/* Lines 207-208: .gdf-glass uses var(--gdf-radius-md) — correct, will auto-update */
border-radius: var(--gdf-radius-md);

/* Lines 214-215: .gdf-glass-heavy uses var(--gdf-radius-md) — 
   should use --gdf-radius-lg (10px) for heavy glass panels */
border-radius: var(--gdf-radius-md);

/* Lines 220-221: .gdf-glass-interactive uses var(--gdf-radius-md) — correct */
border-radius: var(--gdf-radius-md);

/* Lines 263-264: .gdf-glass-overlay uses var(--gdf-radius-md) — 
   should use --gdf-radius-lg (10px) for modals/dialogs */
border-radius: var(--gdf-radius-md);
```

<!-- Current tailwind.config.js borderRadius -->

From tailwind.config.js:
No custom borderRadius defined. Tailwind defaults apply:

- rounded-md = 0.375rem (6px)
- rounded-lg = 0.5rem (8px)
- rounded-xl = 0.75rem (12px)
  Tailwind defaults are close but not exact matches for our new tokens.
  Components using Tailwind classes will still get reasonable (not broken) radii.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update radius tokens in tokens.css</name>
  <read_first>
    - src/app/styles/tokens.css (full file, 222 lines)
  </read_first>
  <files>src/app/styles/tokens.css</files>
  <action>
    In the `:root` block (lines 37-41), make three changes per D-01 (Rounded Corners System LOCKED):

    1. Change `--gdf-radius-md: 4px` → `--gdf-radius-md: 6px`
    2. Change `--gdf-radius-lg: 6px` → `--gdf-radius-lg: 10px`
    3. Add new line after `--gdf-radius-lg`:
       `--gdf-radius-xl: 14px;`

    Also add the same three tokens to the `:root[data-theme='light']` block
    (after line 201, the last light-mode surface variable) to ensure light mode
    uses the same radius scale:

    ```
    /* ── RADIUS (light — same scale) */
    --gdf-radius-md: 6px;
    --gdf-radius-lg: 10px;
    --gdf-radius-xl: 14px;
    ```

    Update the header comment on line 39 from:
    `/* ── RADIUS ──────────────────────────────────────────────────────────── */`
    to include the new scale values in the comment for readability:
    `/* ── RADIUS (md=6px, lg=10px, xl=14px) ───────────────────────────────── */`

  </action>
  <verify>
    <automated>grep -n "gdf-radius" src/app/styles/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gdf-radius-md.*6px" src/app/styles/tokens.css` returns 2 (dark + light)
    - `grep -c "gdf-radius-lg.*10px" src/app/styles/tokens.css` returns 2
    - `grep -c "gdf-radius-xl.*14px" src/app/styles/tokens.css` returns 2
    - `pnpm build` succeeds (no CSS parsing errors)
  </acceptance_criteria>
  <done>--gdf-radius-md=6px, --gdf-radius-lg=10px, --gdf-radius-xl=14px in both :root and :root[data-theme='light']</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update gdf-glass utilities to use new radius tiers</name>
  <read_first>
    - src/app/styles/globals.css (lines 202-266, the @layer components block with .gdf-glass-* utilities)
  </read_first>
  <files>src/app/styles/globals.css</files>
  <action>
    In `src/app/styles/globals.css`, update four `.gdf-glass-*` utility classes to use
    appropriate radius variables per the LOCKED specification (D-01):

    1. `.gdf-glass` (line 207): Keep `var(--gdf-radius-md)` — panels/cards
       at 6px. No change needed.

    2. `.gdf-glass-heavy` (line 214): Change `border-radius: var(--gdf-radius-md)`
       → `border-radius: var(--gdf-radius-lg)` — heavy glass cards should be
       10px to match the "Card/Panel: rounded-lg (10px)" spec.

    3. `.gdf-glass-interactive` (line 221): Keep `var(--gdf-radius-md)` —
       interactive elements at 6px. No change needed.

    4. `.gdf-glass-overlay` (line 264): Change `border-radius: var(--gdf-radius-md)`
       → `border-radius: var(--gdf-radius-lg)` — overlays/dialogs should be
       10px to match the "Dialog/Modal: rounded-lg (10px)" spec.

    Verify each `.gdf-glass-*` class uses exactly one `border-radius` declaration
    and that it references a `var(--gdf-radius-*)` token (no raw px values).

  </action>
  <verify>
    <automated>grep -A1 "border-radius" src/app/styles/globals.css | grep -c "var(--gdf-radius-"</automated>
  </verify>
  <acceptance_criteria>
    - `.gdf-glass` border-radius stays `var(--gdf-radius-md)`
    - `.gdf-glass-heavy` border-radius is now `var(--gdf-radius-lg)`
    - `.gdf-glass-interactive` border-radius stays `var(--gdf-radius-md)`
    - `.gdf-glass-overlay` border-radius is now `var(--gdf-radius-lg)`
    - `grep -c "var(--gdf-radius-" src/app/styles/globals.css` returns at least 4
    - Zero raw `px` values inside `border-radius` declarations within `.gdf-glass-*` classes
  </acceptance_criteria>
  <done>All .gdf-glass-* utilities resolved via CSS variables at correct radius tiers</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add radius scale to tailwind.config.js and verify build</name>
  <read_first>
    - tailwind.config.js (full file, 126 lines)
  </read_first>
  <files>tailwind.config.js</files>
  <action>
    Add a `borderRadius` key inside `theme.extend` in tailwind.config.js
    (between the `backdropBlur` block on line 64 and `keyframes` on line 65):

    ```javascript
    borderRadius: {
      'gdf-md': 'var(--gdf-radius-md)',
      'gdf-lg': 'var(--gdf-radius-lg)',
      'gdf-xl': 'var(--gdf-radius-xl)',
    },
    ```

    This exposes three new Tailwind utility classes:
    - `rounded-gdf-md` → 6px
    - `rounded-gdf-lg` → 10px
    - `rounded-gdf-xl` → 14px

    These classes are available for future plans to use when Tailwind's
    default rounded-* scale (6px/8px/12px) doesn't match our design tokens.

    After adding, run `pnpm build` to verify the config change is valid.

  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gdf-md.*var(--gdf-radius-md)" tailwind.config.js` returns 1
    - `grep -c "gdf-lg.*var(--gdf-radius-lg)" tailwind.config.js` returns 1
    - `grep -c "gdf-xl.*var(--gdf-radius-xl)" tailwind.config.js` returns 1
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Tailwind config exposes rounded-gdf-md/lg/xl utilities, build passes</done>
</task>

</tasks>

<threat_model>
No security-relevant changes in this plan — purely CSS design tokens.
No user input, no API calls, no authentication changes.
</threat_model>

<verification>
After all tasks complete:
1. Open browser devtools, inspect any element using `.gdf-glass` → computed border-radius should be 6px
2. Inspect any element using `.gdf-glass-overlay` → computed border-radius should be 10px
3. `pnpm build` passes
4. `pnpm check` passes
</verification>

<success_criteria>

- `--gdf-radius-md` resolves to 6px in devtools
- `--gdf-radius-lg` resolves to 10px in devtools
- `--gdf-radius-xl` resolves to 14px in devtools
- All `.gdf-glass-*` utilities resolve their border-radius through CSS variables
- `pnpm build` passes with zero errors
  </success_criteria>

<output>
Create `.planning/phases/03-holographic-ui-elevation/03-01-SUMMARY.md` when done
</output>
